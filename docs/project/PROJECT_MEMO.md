# RoboMaster Control — Project Memory

更新于 2026-09-25。每次任务先读本文件；结束时更新受影响的结论、证据与待办，
合并已被替代的记录，不追加逐轮流水账。运行状态只对对应采样窗口有效。

本次yaw排障会话每项任务还必须先读[YAW_DIAGNOSTIC_MEMO.md](YAW_DIAGNOSTIC_MEMO.md)，
收尾同步更新专项结论和验证；AGENTS.md已登记。同步logger v9已在晚间实测记录中出现。

## 有效约束

- **先不启用 RTOS**，保持裸机启动。已有 FreeRTOS 代码不代表调度器正在运行。
- 底层默认冻结：`Inc/`、`Src/`、`Drivers/`、`Middlewares/`、`.ioc`、启动汇编、
  链接脚本、`cmake/stm32cubemx/CMakeLists.txt`。历史 RTOS/遥控合并的局部授权已结束；
  09-11灯语接入的一次性授权也已使用完毕，仅main增加报警头文件并替换恒绿灯调用；
  后续修改继续须有用户明确授权。注释要求见 [COMMENTING_STANDARD.md](COMMENTING_STANDARD.md)。
- 摩擦轮沿用已恢复的下档斜坡、零速闭环和故障联锁，用户冻结要求继续有效。
- 用户明确 yaw 沿用此前成功通信的电流指令：CAN1/ID5、TX2FE槽0、RX209，电调电流环
  开启；后续排障保持此模式。已验证成功的是零电流收发，不是已调好的非零出力参数。
- 默认烧录工具为 **OpenOCD**，不再推荐 CubeProgrammer；新安装选最新发行，
  日常构建不自动升级工具。不恢复旧版固定版本要求，不维护双烧录后端。
- 不猜未知电机协议、CAN ID、波特率、底盘几何或 Jetson 接口参数；缺资料时显式不支持。
- 保留工作区已有未提交改动；构建、源码检查、主机测试不能替代目标镜像核对或实车验证。

## 当前 GM6020 故障

### 已确认事实与判断

- 09-25最新logger颤动及0刻度核查：162737日志固化4076帧，头4帧后有453685ms/
  19810序号缺口，不跨缺口；有效控制MCU486428～516446ms共1310帧连续，运行与
  源码内300/60/0/6000/2000、仅速度环。四次跨零8149→12、8126→13、0→8157、
  45→8168展开+55/+79/-35/-69，连续角无360°跳变；首次尖峰6269刻度，距第一次
  过零约0.865秒，第二/第四也在过零前，第三过零速度-78→-72正常。不支持缺少
  过零展开为直接原因；67个|速度|≥200样本中61个在270～360°，只作角区相关证据。
  连续零目标513.993～514.954秒反馈-570～492、18/44帧请求触±6000，P项幅值达
  28500，I仅-594～-162；后段零目标I趋零仍有-648～780反馈及饱和。不是目标未回中
  或积分积至2000；所采回调/PID dt3～5ms、反馈年龄1～2ms，无采到失联/长停顿。
  优先核对角区扰动与P300的闭环放大，机械/内部反馈或出力/闭环因素尚未分离；
  09-17机械归因不自动代表本次已修复，修复与固定朝向询问待回复。候选单降P300→200
  对照，其余保持，未应用/保证稳定。约23ms遥测不能定真实高频或排除帧间异常。
  证据build/diagnostics/yaw-chatter-20260925/含快照/脚本/summary/zero-crossings/图/报告；
  日志状态/P项/序号/展开断言、Python检查、yaw_reference GCC/Werror及图/文档检查
  通过。仅分析和记忆更新，无控制源码/接口/参数修改、ARM构建或硬件操作，Flash未核对。

- 09-25最新logger `172928_304264`（源码/运行yaw内{120,0,0,6000,2000}）显示Ki关闭后
  不是周期振荡，而是在两个方向固定位置停转：正向连续角423.501°/raw1445，
  持续4278ms速度0、命令/实际电流中位约+2000；反向连续角291.094°/raw6624，
  持续6118ms速度0、命令/实际电流中位约-2000。两处反馈年龄1～2ms，控制仍有效，
  输出没有掉零；因此当前P120在满杆误差下只给约2000原始电流，无法区分角区负载
  超过该转矩与真实机械限位/线束张紧。手动断电转动顺畅不能排除带电、特定方向和
  姿态下的线束/结构阻力。两个停止位置相差约132.4°，不像主控0刻度处理问题。
  候选判别：固定低速/短时逐步提高P或电流，若停止角随出力越过则是转矩不足/负载；
  若电流明显增加仍锁在同一raw，应优先查机械端点、线束、支架和减速机构。未修改/烧录。

- 09-25用户补充：Ki关闭后yaw转不动，约2000原始调节量可使其启动；只考虑P时，
  满杆目标100°/s=16.667RPM，静止反馈下Kp=2000/16.667≈120可产生约2000。
  该值只针对启动瞬间；电机转动后误差变小，P输出会下降，不能保证恒速仍有2000。
  若实际速度约60°/s（误差6.667RPM）仍要求P项约2000，则Kp≈300，但这会提高动态
  过冲/振荡风险。用户要求只算Kp，本轮未修改或烧录参数。

- 09-25最新logger `165823_123823` 验证Kp降10倍后的周期调节：运行/源码均为
  内{30,60,0,6000,2000}，有效flags15共2117帧，dt约4ms、反馈年龄1～2ms。
  同一+100°/s目标平台中，Iout多次到2000，实际速度先0后约60～72°/s并出现
  0～228°/s周期起落；请求约620～2500，未触总输出限。Kp30使静止误差16.67RPM
  的P项只有500，Ki60按dt约每秒积1000刻度，约2秒顶积分限；超过目标后P负项仍小，
  积分需数秒反向消退，形成积累→启动→过冲→回落循环。相较此前{300,60}，Kp/Ki
  由5秒变0.5秒，积分相对P增强10倍；P降10倍同时保留Ki并未保持原PI动态。
  建议Kp30时Ki临时降至6，或先Ki0确认P30出力是否足够；均为待试参数，未修改/烧录。
  证据来自`monitor/logger_20260925_165823_123823.txt`，本轮仅日志分析，未改控制代码/接口。

- 09-18 yaw/pitch调参解释：当前磁盘yaw外{1,0,0,12000,0}旁路、内
  {300,60,0,6000,2000}；pitch外{1,0,.1,10000,15000}、内{20,5,0,10000,0}。
  数组为Kp/Ki/Kd/总输出限/I项限，dt用秒；位置误差编码刻度→RPM，内RPM→协议
  原始命令，yaw电流/pitch电压不可互抄。pitch积分限0使Ki5无效；外环actual恒0
  使测量D无效，仍每回调累加60*杆量未乘dt。建议yaw先保留现300/60观察恒速及回中，
  持续误差且无饱和/振荡才小增Ki，振荡先降P，不继续盲增限幅；pitch先限外环速度
  候选60RPM、外I/D0，再在稳定前提仅开放内积分限候选300，P20/Ki5保持，均未代改。
  175142日志读取7173帧仅4帧enabled/startup1，yaw参数300/60且无高目标运动，pitch
  误差-1.514°/目标-206.733°/s/实际0；窗口太短不认定当前动态根因或已调稳。
  现存174839记录也非新的运动验证；建议不据昨日短平台保证当前增益。源码/单位算术、
  MathWorks先内后外原则及文档差异检查通过，摘要/tmp/pid-guidance-20260918.json。
  仅解释/记忆更新，无参数/接口修改、构建或探针连接/烧录；候选效果与实车动态待验证。

- 09-17后续图参考135.582°/实际125.508°匹配210550日志；源码及运行已为
  {200,30,0,6000,2000}，积分确已开放。末三段±100平台长2.691/1.426/0.828秒，
  去首300ms速度中位+66/-60/+66°/s；首段Iout从-54升435、末300ms速度中位72，
  反向段Iout仍+362→+46，旧正积分抵消部分负向P。MCU75019～86013ms的479帧
  请求-2223～2789、|Iout|≤441，均未触总限6000/积分限2000；是短平台跟随不足，
  不能据此认定长时间恒速也停在同一比例。Ki30在40°/s误差仅积200刻度/秒；候选
  下一步仅Ki30→60，其余不变，并用同向恒目标/回中记录评估，不再增加未触及的限幅。
  候选未应用或保证稳定，完整Flash未核对。证据build/diagnostics/yaw-pi30-followup-20260917/
  含958帧较宽快照及三平台摘要；更早67～69秒有尖峰/饱和，未混入最新三平台统计。
  参数/状态/序号/P项/限幅断言、积分秒单位及源码/NI原理、文档差异检查通过；
  本轮仅日志分析和记忆更新，未改控制源码/参数/接口、构建或操作硬件。

- 09-17最新慢速图匹配205842日志末参考153.061°/实际125.991°；当前运行与源码均
  内环{200,30,0,6000,0}。Ki虽30，积分限0使956帧Iout全部为0，仍相当于纯P。
  固化MCU118978～140937ms，序号连续/最大间隔23ms；正常±100°/s平台去首300ms后
  速度幅值中位60～72°/s、请求幅值最大1977，未触6000；另127694～128448ms平台
  有振动尖峰并触6000，不以此代表正常恒速负载。当前先解决跟随误差，非先提高目标；
  位置环不是速度到速的必要条件。建议保持用户P200/Ki30/D0/总限6000，仅将积分限
  试设2000，属于待验证候选，替代前轮P190/Ki50建议，未实际修改或烧录。
  logger文件持续追加（seq7555→12382，最新文件年龄约0.005秒），此前读不到的原因
  本轮未复现；沙箱进程/USB查询受限不能当作硬件故障。证据
  build/diagnostics/yaw-integral-disabled-20260917/{snapshot.txt,summary.json}；参数/状态/
  P项单位/Iout/序号断言、源码积分限逻辑及文档差异检查通过。仅记录分析，未改接口/
  控制源码、构建或操作硬件；完整Flash身份未核对，开放积分后的稳定性与跟随待实测。

- 09-17用户已找到振动的机械原因，具体部位/修复方式未提供；后续重点转为速度跟随，
  不再把此前角区尖峰推定为编码器或电调故障。上一轮连续旋转只读分析已完成：201121日志
  20:47:24～20:49:04共4394帧、约15.8圈，15完整圈中11圈有|速度|≥200尖峰，98/114
  尖峰在原生210～300°，16次跨零邻点-72～-54°/s；仅说明角区相关，非硬件根因证明。
  证据build/diagnostics/yaw-continuous-20260917/，参数/序号/状态/P项断言、绘图检查通过。
  本次半速图匹配201121末角-2504.399°/参考-2531.928°；固化652帧，末个-100°/s平台
  MCU2467.356～2469.127秒，去前300ms后64帧实际中位-57（-72～-48）°/s，请求中位
  -1356.5/反馈电流-1357.5，运行190/0/0/限6000，未饱和、控制有效。源码仍用户P200，
  不当作已运行参数。该短平台符合纯P负载下持续误差，非位置环关闭或软件半速限幅；
  速度环应先独立调PI，位置环用于位置追踪/锁位。机械问题处理后候选保持实测P190、
  加Ki50/积分限2000（总限6000、D0、speed_loop_only=true），只是起调建议未应用。
  证据build/diagnostics/yaw-speed-offset-20260917/；状态/序号/P项/无饱和、源码秒单位、
  NI积分原理及文档差异检查通过；未改控制参数/接口、构建或操作硬件，速度跟随待实测。

- 09-17过零处理复查：yaw_reference.c按相邻编码差±8192展开连续位置，8190→2为+4、
  反向-4；位置误差取连续坐标之差，单圈Wrap不进入该误差。当前速度单环直接用CAN RPM，
  无编码零点专用换向/减速/PID重置；共用PID输出RC2ms低通存在，专门的加减速/S曲线未实现。
  跨零连续不等于实车运动已平滑。随后固定位置振动截图匹配201121日志参考-816.585°/
  实际约-847.05°；固化79409帧，末次运动附近1023帧（最大间隔23ms）运行P190/I/D0。
  三组|速度|≥200尖峰编码5435～5497、4839～7364、5275～5432，均远离零点；后两组
  请求触±6000，末组目标已0。三次实际跨零8179→17/8179→18/8171→8角度连续，
  采样速度54→54/60→66/48→54°/s。现有证据不支持缺少主控过零展开为本次直接原因；
  仍需分离固定角区负载/电机反馈或出力异常与闭环放大，不能直接判定某个硬件故障。
  当前磁盘用户已改P200，勿把P190日志当P200实测。证据build/diagnostics/yaw-fixed-position-20260917/。
  yaw_reference GCC/Werror测试、日志哈希/连续序号/状态/P项单位/跨零角增量断言及差异检查
  通过；未改参数/接口/控制源码、构建ARM或连接硬件，未独立核对当前Flash。

- 09-17双图跟随方法核对：先独立速度PI（候选190/50/0，限6000/积分限2000），确认无卡阻且
  稳定后，再以普通手动低速30°/s、模式限10RPM、外{0.02,0,0,10,0}及speed_loop_only=false
  试位置收敛，不能直接启用现存外P1。外环输入编码刻度/输出RPM，内环输出为电流刻度。
  理想速度环下外P0.02对应2.7307/s，匀速30°/s仍需约11°误差维持出速；要进一步减少
  运动滞后，可加入受约束目标轨迹的速度前馈+位置校正，随后速度PI。现有feedforward字段
  是RPM→电流，不能代替前述速度前馈。串级后右图目标为外环最终速度需求，含角度追赶；
  不保证任意角度/速度目标或阶跃瞬间精确重合。全部仅建议，未代改/构建/烧录；源码/遥测
  语义、单位算术、官方串级前馈资料及文档差异检查通过，实车效果与17°阻挡原因仍待验证。

- 09-17下一步调参建议：201121日志固化20973帧，最新运动422030ms前20秒至后1秒913有效帧，
  P190/I/D0、无输出饱和；195帧|目标|≥99，实际速度幅值中位60、范围12～90°/s，无零速帧，
  末角171.5625°对应最新图。仍不能排除之前17°停转的物理原因，询问待回复。
  无卡阻/外力前提下建议速度内环PI试调起点{190,50,0,6000,2000}，不是已验证稳定参数；
  40°/s误差积分约333刻度/秒，观察速度误差与回中漂移/振荡，再按日志决定下一档。
  本轮未代改，源码仍{190,0,0,6000,0}，无构建/接口修改/硬件操作。
  证据build/diagnostics/yaw-next-tuning-20260917/；运行参数/P项单位/无饱和断言、
  积分算术、NI积分原理及文档差异检查通过；跟随目标尚未完成。

- 09-17 P190新图/201121 v9日志已证实运行P190：固化3990帧，有效3986、服务OK、无6000饱和，
  活动平台速度幅值中位48～60°/s，未跟上100。MCU41.150～41.863/42.645～43.749秒
  目标-100、实际0，编码387～390/401～403（约17.0～17.7°）；请求中位-3166，
  电流反馈中位-3169/-3170。原始输入/路由均要求负满速，控制使能、反馈新鲜、序号连续。
  P180高目标零速样本也集中17.3～18.0°，提示同角区阻力或出力异常，未证明机械卡死。
  已询问是否挡块/线束/手扶，待回复；暂保留P190，未继续提高P或加I，基本重合目标未达到。
  证据build/diagnostics/yaw-p190-review-20260917/；逐帧P项/参数/状态/无饱和、
  停转窗口连续性/反馈时效/角度断言与文档差异检查通过；未改控制接口/源码、构建或操作硬件。

- 09-17要求yaw目标/实际速度基本一致：200428 v9日志20:04:30.555～20:06:20.916固化4803帧，
  4799有效帧均P180/I/D0/限6000/积分限0，服务OK，无采样输出饱和。目标±100°/s的平台
  去前200ms后，多数速度幅值中位54～60°/s、请求约1200～1400；另有约0.5秒负目标平台
  实际0、请求约-3000/反馈电流约-2994，不把全部误差归因增益或限幅。
  按只调P要求，源码180→190（相同误差出力+5.56%），其他参数保持；历史P200有振荡，
  不按速度比例大幅放大。P190只是待实测下一档，尚未实现或验证基本重合；纯P持续误差
  可能需要有限积分，未擅加I。参考角仍不参与闭环，不以角度线重合为速度调参目标。
  证据build/diagnostics/yaw-tracking-p180-20260917/；快照哈希/逐帧P项单位、状态、无饱和
  断言通过。步兵构建/ELF、全套GCC主机回归通过，FLASH152272/RAM57696B；ELF
  bf794b3832063c3349d44d7950a5545f49281591dcc6e7c45ab9bf2d80dcfa05，日志
  /tmp/yaw-p190-{build,host}-20260917.log。无接口/冻结底层修改，未烧录或驱动硬件；
  需P190双向推杆/回中新记录评估速度误差及振荡，不能用构建通过代替实车跟随验证。

- 09-17用户再次要求提高yaw P以改善速度跟随：已将步兵速度内环150→180，
  I/D0、PID限6000/积分限0、协议12000、满杆100°/s及其他控制参数保持；相同误差P出力+20%。
  最新195811 v9日志固化前3446帧运行P全150，确认此记录已使用前轮增益；
  最新截图目标±100°/s、实际约±30～60°/s，尚不作为P180稳定性或根因证明。
  步兵构建/ELF及全套GCC主机回归通过，FLASH152272/RAM57696B，ELF SHA256
  a475ed5ae1feca14f7f27fe20e9ee330d7fc6ba676a786484df070251b11740b。
  日志/tmp/yaw-p180-{build,host}-20260917.log；基线证据build/diagnostics/yaw-p180-20260917/。
  参数/差异检查通过；本轮仅改yaw P及记忆，无接口/冻结底层修改或烧录，P180实车待验证。

- 09-17用户称启动已可以但转动慢，检查目标与限幅：源码满杆100°/s=16.667RPM，
  manual_speed_rpm30=180°/s尚不裁此目标；内PID后两项6000/0分别为总输出/积分项限幅，
  单位为电流刻度，不是转速，Ki0时提高积分限本身无效。最新保存194343 v9日志
  19:43:45.298～19:48:45.740共13068帧，13064有效帧运行P仍120/I/D0/限6000，
  非零高目标361帧目标-100～-90.303°/s、实际-48～0，PID输出-2000～-1093，
  电流反馈-2026～-1156；全部有效帧未触6000。约2.23秒平台实际中位0、请求-2000/
  电流反馈-2003，说明该记录为已有电流下跟随不足，非输出限幅卡住，机械原因仍未分离。
  后续19:52:02用户烧录日志已VERIFY_OK/FLASH_OK；旧采样早于此烧录，不能称现在仍P120。
  先用P150新日志复核跟随；若实际已跟上100仍嫌慢，调整manual_rate_deg_s才改变目标，
  如150°/s仍低于现有180°/s限速。纯P有持续误差时不能仅增限幅补力，是否加有限I待新数据。
  证据build/diagnostics/yaw-speed-limit-20260917/；逐帧P项单位/限幅/参数/状态断言与
  源码、NI积分原理、文档差异检查通过。未再改参数/接口、构建、读板或烧录。

- 09-17用户再次贴图并明确要求提高yaw P，已将步兵速度内环P120→150，I/D0、
  PID限6000/积分限0、协议12000、速度单环/100°/s及其他轴参数保持；只改该配置和相邻注释。
  同误差滤波前比例出力+25%，满杆静止2000→2500刻度。截图还含目标-100°/s而实际0的
  片段，未采同步电流，不能将全部迟缓/停转归因P不足；历史P200振荡故本轮不跳高增益。
  步兵just build/ELF检查及全套GCC主机回归通过；FLASH152272/RAM57696B，ELF SHA256
  f16ecc01318116aa7725f4c036e8963a5afe676d1980fd4eb875f4998c7062fa。
  日志/tmp/yaw-p150-{build,host}-20260917.log；差异检查通过，未改接口/冻结底层或烧录。
  P150仅已保存并构建，实车响应/稳定性待验证；烧录前须退出占用探针的logger。

- 09-17随后yaw响应慢截图已显示速度环有效，目标约±100°/s、实际多数±30～50°/s，
  不沿用前次启动联锁状态。当前磁盘内P120/I/D0/限6000、协议12000、速度单环；
  截图未携运行PID/电流，不当作板上参数确认。若板上同值，建议只将内P改150，
  双向起停无振荡且仍慢才试180；历史P200有实测回中振荡，不直接跳200/300/360。
  其他参数保持，参考角不参与闭环；满杆静止P项2000→2500→3000，非稳定性保证。
  源码/算术、MathWorks调参权衡和文档差异检查完成；仅建议，未代改参数/接口、
  构建或操作硬件。稳定增益、出力/负载与跟随不足原因仍待同窗口实测，详见专项memo。

- 09-17晚“底盘能动、yaw不动”，用户确认左拨杆下档。最新191326 logger固化13178帧，
  截止19:18:18.523；918帧右水平非零（±660）均enabled1/startup0、双轴flags7。
  最新连续10秒455帧仍未启动，pitch2582～2583，超过当前源码1566～2205范围；
  capture_startup_position会因pitch越界归零双轴，与现象吻合。启动阻断已实测，
  板上限位/Flash未独立核验；优先禁用输出后核对pitch姿态/安装标定，未代改PID/限位。
  实际遥测仍v8/300B，117列TXT的v9扩展无效，不能当作已更新固件或实测电流。
  证据`build/diagnostics/yaw-no-motion-20260917-evening/`；日志状态/序号连续/单位与
  NaN断言、源码门控及文档差异检查通过。未改控制接口、构建/连接硬件/烧录，详见专项memo。

- 09-17 按用户要求扩展同步logger，接口和验证详见必读[YAW_DIAGNOSTIC_MEMO.md](YAW_DIAGNOSTIC_MEMO.md)。
  GimbalCmd尾加24字节trace，携带实际路由ch0/RC与路由序号时间/来源状态；回调统一采输入、
  PID项/运行参数、最终应用请求/服务状态/电流反馈/控制dt。GimbalMonitor v2为216B且保留v1前缀，
  SWD工具兼容v1/v2；Dashboard v9保留v8前缀，载荷412/帧422B，Python/网页兼容v3～9。
  TXT新增39列共117列；源无效/PID未运行用nan，禁用零请求仍记录；20ms非阻塞发布频率未改。
  新RC早于旧命令派发也不混淆来源；Cmd消息计数不是UART帧数，请求接受不是CAN发送确认。
  当前用户已注释router的>1000跳变过滤，本轮保留；未改参数/控制规则/冻结底层或操作硬件。
  全套GCC回归、17项Python/Node/RTT、13项SWD测试、本地ELF准备与双车型构建/ELF检查通过。
  FLASH步兵152272/哨兵153104B、RAM均57696B；摘要和/tmp/yaw-sync-*验证路径在专项memo。
  需更新固件/重启logger后采v9实车日志；时序开销和实际丢帧率仍未知，不把本机构建当作已烧录。

- 09-17 按用户要求以logger实测重新分析，见[YAW_LOGGER_ANALYSIS_20260917.md](YAW_LOGGER_ANALYSIS_20260917.md)。
  固化16:03/16:29/16:32三个日志53493/2710/2116帧；最新有效2110帧，目标±100、反馈-474～534度/秒。
  最新MCU27.897～28.821秒43帧目标恒-100、反馈-456～534；29.041～29.415秒18帧目标0、
  反馈-474～234；所选窗口enabled/startup1、flags15、遥控新鲜且seq连续，不能解释为未使能。
  69.279～71.061秒82帧速度目标/反馈全0，但参考角778.081度、实际约747.949度；位置环未启用，
  约30度参考残差符合回中停止积分/领先限制，不是PID仍追旧角度。正负零目标切换已被速度环采用，
  但TXT无原始ch0/路由量，不能证明此前过滤滞留实际触发或排除局部发生；二进制有杆量而TXT未保存。
  无PID/固件身份/电流/dt，不能认定后续日志仍P360、饱和或某次调参改善。最新初段23.865秒缺口，
  选取窗口均连续；通常22ms遥测不排除帧间异常。快照/哈希/脚本/summary/曲线在
  build/diagnostics/yaw-logger-review-20260917/，解析/窗口断言/图像与文档差异检查通过。
  本轮无控制/参数/接口修改或硬件操作，仍需同步原始输入、采用目标、最终电流及控制时序定位根因。

- 09-17 按用户要求专项检查目标传入，详见[YAW_COMMAND_CHAIN_AUDIT.md](YAW_COMMAND_CHAIN_AUDIT.md)。
  确认route_gimbal的>1000跳变过滤会持续保留旧杆量：ch0=-660→+660保持1000ms，
  真生产链PID目标仍+16.667RPM而非-16.667；回中被接收后恢复。匹配板上b3843d5f反汇编
  也有该分支。需接收/路由输入实际跳变>1000才触发，平滑中间值不触发；非每次反打必现。
  因而不能仅凭目标平稳排除输入生成错误，但目标已0的剧烈反馈不由卡非零旧目标单独解释。
  专项直接链接Cmd/Router/消息/云台/PID/驱动/服务/DJI/CAN，3382回调及CAN发送通过；
  1321合法ch0逐值映射、回中、反馈-120～115RPM不改目标、400普通事件/轮拥挤、按值复制、
  16ms遥控保持、超时/恢复、spin强制0、负反馈/低通/限幅及TX2FE槽0均验证。main先Task后
  Dispatch导致新RC下一轮参与目标（测试4ms一轮，非实机固定周期）；冻结main未改。
  当前P360/I/D0/限6000及协议12000保留。本轮仅检查、未修改目标过滤/控制接口或参数。
  证据build/diagnostics/yaw-command-chain-20260917/；全套GCC主机回归及专项、源码哈希/
  Python语法/文档差异检查通过。未构建ARM/连接硬件/烧录，需同步原始ch0与路由/PID目标
  才能确认实车是否触发；合法跳变直接接收或按新遥控帧有限确认的修复策略尚未实施。
  随后补测完整原始18字节DBUS链：50新帧/700ms均正确解码ch0=+660且在线，yaw_rate
  仍+1而非-1，直接定位到路由过滤。严格>1000边界、回中被接受即解除、越界700归0均通过。
  同一派发前收到回中和反向帧，latest合并中间回中可触发大跳变；未证明实车发生此合并。
  离线分支只清输出/gimbal_memory、不清previous_yaw_input，200ms超时停机后相反端点重连
  可再用旧方向（CAN保持armed的主机实验）；掉线期间仍零输出，不能混淆残留历史与继续出力。
  上游约100ms清解码缓存不主动发布零RC，应用200ms超时及一轮主循环延迟另行解释。
  证据同目录run_retention.py/retention_cases.inc.c/retention-result.log，原始遥控专项、
  生产源码哈希/Python语法/文档差异检查通过；仍仅分析，无控制修改/硬件操作。

- 09-17 用户报告yaw短时-400～600抖动、怀疑遥控发布：新发现logger_20260917_160339_979273.txt，
  固化读取14598行，其中控制有效1663行，目标0～96.061度/秒、实际-720～690度/秒（-120～115RPM）。
  主机接收约16:06:08.117开始198ms/10帧目标全0，实际-720～480；约16:06:13.684开始
  286ms/14帧目标95.606～96.061，实际-528～690。52个|反馈|>=400度/秒样本均enabled/startup1、
  yaw flags15，其中31个目标0。明确捕获启用后的抖动反馈，不能沿用15:59静止采样判当前未使能；
  日志时段晚于此前SWD窗口，未证实同次启动/镜像，logger本身不携PID身份或实际电流请求。
  Dashboard实际/目标速度均RPM*6，目标来自PID已采用target，不是仅遥控发布前值；现行RC与
  GIMBAL_CMD走独立latest槽，常规CAN队列不覆盖。所采片段支持目标平稳时闭环/反馈异常，
  不支持遥控目标反复换向为直接原因；22ms遥测仍不能排除帧间短暂命令异常/控制时序问题。
  若仍为最近匹配板上的P360/I/D0/限6000，则100度/秒误差的滤波前比例项就到6000；
  回中反馈+582度/秒时P=-34920，易产生交替限幅，但本日志无电流，不宣称已实测本轮饱和。
  优先单改P360→120对照，仍需同步目标/反馈/最终电流/回调dt及编码角速度分离反馈与真实振动；
  不能单凭相邻22ms角度差和瞬时RPM不同认定反馈损坏。询问用户读数单位及目标线，尚待回复。
  固化日志/summary在build/diagnostics/yaw-logger-20260917/；源码链路、窗口状态/目标断言、
  单位/增益算术、NI原理及文档差异检查通过。未改参数/控制接口、构建或重新连接/驱动硬件。

- 09-17 用户就绪后yaw运行中只读采样：45秒385组一致快照，首尾44.821秒，全部
  enabled/startup_ready=0、yaw flags7、请求0/实际RPM0，编码3186～3187，反馈电流
  -43～33原始刻度、反馈年龄4～9ms。回调计数+10187，所采回调间隔4～6ms/中位4；
  内环未激活，不能把这些回调间隔当作有效PID运行周期。未捕获推杆/颤动，不据此认定故障消失。
  Flash逐字节匹配ELF b3843d5f822ea109ac3188ccfd28174da5f440b7c81ef75db9593156f1b80332，
  离线GDB确认板上内360/0/0/限6000、外1/0/0/限12000、协议12000、速度单环、手动100度/秒。
  所采快照命令enabled为0，但未读遥控原始状态，禁用原因未定位；已提示遥控解锁，用户随后表示停止。
  采样进程已正常退出，无参数/控制接口修改、构建、暂停/复位/恢复CPU或烧录。
  证据build/monitor/yaw-feedback-20260917-live.*，匹配ELF/配置/summary在
  build/diagnostics/yaw-feedback-20260917/；镜像哈希、快照偶数序号、解析与文档差异检查通过。
  使用既有poll刷新及批量RPC包装；首次沙箱禁止本机socket，未连硬件，授权环境重试成功。
  用户随后明确电机正常工作，与采样现象不一致，不能把“所采命令未使能”推广为整个操作期间
  电机未工作。文件时间推算实际采样约北京时间15:59:41～16:00:26，启动连接有准备延迟，
  是否覆盖用户运动时段待确认。匹配ELF结构大小148/轴56、enabled偏移28/startup32/yaw36
  与解析器一致；对应反汇编确认enabled来自s_last_cmd.enabled，原始OpenOCD输出重解385行
  全部匹配CSV，未发现字段错位。该位是应用命令允许位，不是电调上电/在线位；源码遥控超时
  或CAN未armed可导致0，未采上游状态不能认定其中某项。复核只用既有文件，未再连接硬件；
  布局审查存同目录layout-audit.txt，解析断言及文档差异检查通过，用户观察与采样对应仍待解决。

- 09-17 修复yaw角度/速度target不可见：logger_20260917_154627_522576.txt的3626帧中，
  角度目标全NaN（speed_loop_only不启用位置环），速度目标3620帧有效、范围±100度/秒；
  其中3207帧目标等于实际，网页原先后画同宽实际线会遮住目标。该日志不能证明当前板上PID。
  Dashboard在CONTROL_ACTIVE时通过既有gimbal_cmd_yaw_deg发布回调连续角度参考（度），
  gimbal_yaw_target_deg仍仅POSITION_ACTIVE时有效；失效参考保持NaN，v8布局/大小不变。
  参考源为s_yaw_reference.target_ticks→monitor.yaw.position_target_ticks，按360/8192换算；
  普通手动由YawReference_Advance按杆量×manual_rate_deg_s×dt推进，推杆领先受target_lead_deg
  约束，回中冻结。当前仅速度环实际使用杆量×manual_rate_deg_s/6并按manual_speed_rpm限幅，
  参考角误差不进入PID；它是本次新增的辅助显示，不是修复了一个有效位置闭环目标。
  该字段/分支/单位源码复核及文档差异检查通过；本次解释未改实现或操作硬件，板上状态未重采。
  后续target speed N/A核查：160339日志截至主机16:12:06.240读取22933行，有限目标与
  CONTROL_ACTIVE逐行一致；此前控制有效1663行均有目标。末尾flags7/enabled0/startup0、
  实际0/目标NaN、遥控新鲜位未置，说明该窗口仍有遥测/电机反馈而控制未激活，并非RTT整链断流。
  最后有效目标采于16:06:16.034；不能据末尾停用状态否定此前运动，遥控为何过期及是否对应用户
  当前屏幕仍待确认。源码门控/日志断言/文档差异检查通过；未改实现或重新连接硬件。
  16:16:47.847～16:16:53.565按用户要求复查正在追加的同一logger：新增260帧、MCU tick
  前进5720ms，末帧读取年龄646ms；全段enabled/startup1、yaw flags15、目标/实际均0度/秒，
  遥控新鲜位均置位，确认该窗口控制已启用且速度目标有效，不沿用16:12的NaN状态。
  证据build/diagnostics/yaw-target-status-20260917/summary.json；时效/递增/有效性断言及
  文档差异检查通过，仅读取既有日志，未改参数、连接额外探针或验证浏览器实景。
  随后按用户要求修改网页显示（UI 2026-09-17.2）：IMU & Gimbal新增yaw当前目标/实际速度、
  enabled/startup/flags、收帧时间/年龄及MCU帧号；状态区与速度图例取同一最新帧，有效零值明确
  显示0.00度/秒，不可用显示—并区分禁用/等待对齐/快照过期/断流/有效位与NaN不一致。
  重连清收帧时间，首帧前不把旧值视为当前。实际HTTP检查旧服务已含上轮曲线修复，未复现
  有效零值变N/A；新增可核对状态，不宣称已证明用户原屏幕根因。桥接此前启动时缓存HTML，
  现单/双端口每次请求读磁盘、no-store，文件失效503；旧进程须重启一次，后续刷新即可加载保存。
  15项Python/Node测试通过，含真实网页WS onmessage拆帧/禁用→启用/负值/复位序号、渲染状态、
  两HTTP入口保存刷新/失败，日志/tmp/rm-yaw-web-display-tests-20260917.log；文档差异检查通过。
  本次仅网页/桥接/测试/文档，未改控制或协议、重启现用logger/烧录/操作电机；未做浏览器实景验证。
  网页仅在有效速度环且位置环未启用时显示独立reference angle，不冒充位置闭环目标/误差；
  yaw目标/参考改为上层虚线，实际为实线，图例显示最新数值/N/A并提示控制状态。
  真实C帧→Python/Node网页解码、实际曲线存储/裁剪/清空、重合绘制顺序/虚线、日志语义
  回归通过，覆盖停用/过期/负角度/旧v7；14项RTT测试、全套GCC主机测试及双车型构建通过。
  FLASH步兵151000/哨兵151824、RAM均57544B；验证日志/tmp/rm-yaw-target-*-20260917.log。
  本任务未改控制/PID和冻结底层；并发工作曾将内P改360，收尾磁盘已恢复120，相关记忆保留。
  未连接硬件/烧录或做实景浏览器
  验证；角度参考需更新固件、重启logger并刷新网页，仍需实车确认，说明见logger-guide.md。

- 09-17 用户再次报告调yaw PID后颤动：磁盘diff仅该轴内P120→360，I/D0、PID限6000、
  协议12000、speed_loop_only=true、手动100度/秒及前馈0不变；外P1仍旁路。
  同误差P出力增为3倍；满杆静止误差16.667RPM对应滤波前6000，回中实际±1RPM对应∓360。
  优先怀疑增益提高后速度闭环反复驱动/制动，建议仅恢复P120作对照，非已证明唯一根因或稳定值；
  不盲加I/D、外环或输出限。固定角区异常仍需分离反馈与角度相关负载/出力。
  现有logger_20260917_154627_522576.txt本次读取3626行，flags有效且速度有限3620行，
  目标±100度/秒、实际-54～102；零目标3229行实际-42～30，不能由范围证明持续振荡，
  日志未携带PID/镜像身份，不能认定对应P360。未读板或改参数/控制接口、构建/烧录。
  源码控制路径、增益/限幅算术、日志解析及文档差异检查通过，NI比例增益振荡说明复核；
  当前板上参数和本次颤动波形仍待确认。

- 09-16 “无法控制、logger双CAN OFF但无报警”的运行中只读采样：Flash匹配b95c83b7，
  15组/3829ms均rc_count/rx_events/received_bytes/remote_online/armed=0；两轴反馈持续推进，
  motor_offline_mask=0、CAN ESR均0，历史CAN故障计数189/190窗口内未增加。因此该窗口的
  控制阻断是无遥控有效输入；旧dashboard未填CAN位，页面OFF不是实测bus-off，无电机失联报警
  与反馈在线一致。历史UART错误2次/末码4，当前BUSY_RX、ErrorCode0，不能据此定位接收机/
  接线/串口具体故障；DT7和DR16状态尚待用户确认。证据build/diagnostics/control-off-20260916/。
  未暂停/复位/烧录；普通SWD快照可能因4ms发布发生一致性重试失败，不能据无快照判CPU停机。

- 09-16 用户贴yaw外10/0/0/限12000、内300/0/0/限6000、协议12000，问转动颤动是否上限问题。
  当前磁盘与贴值一致，speed_loop_only仍true、前馈0，外环不参与；未重新读板。
  有效命令限为min(6000,12000)=6000，不存在旧PID30000大于协议12000的滤波历史倒挂。
  仅把协议12000改6000在当前路径不改变输出；提高PID限幅可能放大振荡，不能据饱和就加限。
  P300对应1RPM误差300刻度，恒定20RPM误差时滤波收敛后到6000；当前满杆目标100/6=
  16.667RPM，静止P5000，未必饱和。此前P200/同限幅已实测回中反转与±6000饱和，证明
  当前限幅组合下也能振荡，但不能唯一归因P或排除特定角区的反馈/出力/负载异常。
  建议保留速度/模式/两限幅、I/D0，仅内P300降120作对照，非保证消震；若仍集中固定方向，
  应继续核对原始角度、独立角速度与CAN RPM，避免反复仅换P。暂不恢复外P10。
  源码限幅链、历史summary、静态限幅等价与算术/文档差异检查通过，NI比例增益说明复核。
  仅检查/解释并更新记忆，未改用户参数/控制接口、构建、烧录或连接硬件；本轮实时根因未确认。

- 09-15 用户强调特定方向振动，专项查GM6020过零：上轮P200日志26个|RPM|>25样本原始编码
  5055～6106，16个饱和样本5137～6029，远离8191/0；7408→324跨零落在0.803s采样空窗，
  不能宣称实车边界瞬间已正常。源码相邻差先float再±8192，8190→2为+4/反向-4，速度单环
  使用独立CAN RPM、不由原始角度差分，当前无角度分段出力。真实应用/PID/消息回调专项测试
  8192起点×双方向×8步，相同速度输入的输出历史一致、控制有效/外环0。初版host测试误接
  发射测试结束后的失效队列导致Bus error，移入有效云台测试上下文后通过，非固件故障。
  补采30秒812组Flash仍匹配af348933，raw1216～2356/RPM-15～13/请求-2922～3530，全部
  所采快照enabled/startup1、flags15、服务OK；未覆盖零点及故障角区，不否认固定方向现象。
  无证据认定主控跨零Bug；电机内部反馈/出力与角度相关负载尚未分离，不能只认定P过大，
  CAN机械零点不等于内部电角度零点。官方v1.4/FAQ确认机械编码/RPM独立与出厂标定。
  证据build/monitor/yaw-zero-cross-20260915.*及build/diagnostics/yaw-zero-cross-20260915/，
  详见[YAW_PID_CHAIN_AUDIT.md](YAW_PID_CHAIN_AUDIT.md)。专项主机/解析断言/Python语法/文档
  差异检查通过；采样退出，无生产代码/参数修改、构建ARM、烧录/复位/暂停或目标内存写入。

- 09-15 随后运行中只读复测捕获振动，用户确认仍抖：20秒取得242组一致快照/首尾18.752秒。
  Flash逐字节匹配ELF af348933c082b872790f35f642378d662b46c712abcc99d8ad4264419f818289，
  板上及最新源码yaw内200/0/0/限6000/积分限0、外10/0/0/限12000、speed-only=true、协议12000，
  手动100度/秒/限30RPM；是用户后续参数，不能混同此前P300。全部有效快照enabled/startup1、
  yaw flags15/服务OK、反馈年龄4～7ms，回调及内环dt3～5ms/中位4。回中目标0的71组RPM
  -72～+68，电流请求触±6000、反馈-6542～+6393；推杆也有超速/反向，约第12～13秒零速收敛。
  确认速度闭环反复驱动/制动及饱和，非外环追赶、I/D残留或所采快照的失联/启动阻断。
  当前PID6000低于协议12000且前馈0，首轮30000下游截断已不存在，仍抖证明它不是必要条件。
  尚不能由被动稀疏采样唯一分离P增益、反馈延迟/真实性和机械动态，下一步固定其他参数单改P
  比较；不宣称已证明仅P过大或已修复。最大样本间隔0.863s，不据此推振动频率/饱和时间比例。
  证据build/monitor/yaw-root-cause-20260915.*及build/diagnostics/yaw-root-cause-20260915/含
  匹配ELF/配置/分析器/summary。镜像摘要、解析断言/Python语法、文档差异检查通过；采样已退出，
  无控制代码/参数修改、构建ARM、暂停/复位/恢复CPU、烧录或目标内存写入。

- 09-15 yaw全链复核，用户确认推杆/回中均振动，发生时外10/0/0、内300/0/0，
  详见[YAW_PID_CHAIN_AUDIT.md](YAW_PID_CHAIN_AUDIT.md)。检查期间用户保存内P120，须与振动参数分开。
  首轮磁盘speed-only=true，内120/0/0/限30000/积分限0、协议12000，外10/0/0/限120000旁路，
  手动100度/秒/限30RPM、前馈0；该阶段未读板，用户随后称振动时speed-only应该也为true。
  源码配置初始化→目标RPM→
  速度误差→滤波/限幅→服务/适配器→TX2FE槽0及RX209回填未见断环、二次PID或单位/符号误接。
  确认内部last_output可存30000而实际限12000，I/D0时仍有2ms输出滤波和反向延迟。
  真实pid.c/车型构造4ms输入实验：内部先饱和30000后目标0/反馈+1，P=-300，五次输出约
  9800/3066.67/822.22/74.07/-175.31；±1RPM交替反馈输出约∓150，I/D全0。为算法实验，
  非机械仿真或实车根因证明；纯P也可振荡。当前周期/使能与振动波形仍未知，先统一有效限幅、
  核对P300与反馈波动/延迟，不盲加D或恢复外环。外P10约0.132度误差即稳态到手动30RPM上限。
  完整GCC主机回归及专项数值断言通过，证据build/diagnostics/yaw-pid-chain-20260915/，
  后续P120复核满杆静止稳态2000、±1RPM交替输出∓60/I/D0，保存配置快照并重跑主机回归。
  主机日志/tmp/rm-yaw-pid-audit-host-20260915.log；文档差异检查通过。未改控制代码/接口/用户参数、
  构建ARM、连接硬件或烧录；时基仍毫秒分辨率、20ms连续性失效/100ms重对齐需运动窗口排除。

- 09-13 用户要求右杆yaw更安静且响应更快，并确认是电机抖动/咔咔或嗡声，不是蜂鸣器。
  两次运行中只读记录225组/29.848s及335组/49.976s，均enabled/startup0，yaw输出0、
  编码7620～7621、RPM0及-1～0、反馈年龄4～6ms；pitch2189～2190在限位内。
  两次Flash与ELF均匹配72eeb30e1da47f33468d4daebd99415eeeb541e337aba0fe56d7db38f8ca0f6e；
  离线GDB确认板上yaw内120/0/0/限6000/积分限0、协议12000、手动100度/秒/限30RPM、
  speed_loop_only=true。没有捕获启用/推杆/发声窗口，无法据此认定电机噪声根因或已改善。
  已请求用户确认当前能否开启遥控并复现；尚无回复，不盲加P、加反馈滤波或承诺完全无声。
  当前禁用原因未采遥控原始字段，不能直接称接收机离线；先双拨杆下/所有轴回中≥500ms，
  恢复使能后在普通手动下采双向推杆与回中制动，再决定控制改动。保留用户P120配置。
  证据build/monitor/yaw-noise-20260913{,-reproduce}.{csv,json,summary.json,openocd.log}；
  匹配ELF、配置、分析器在build/diagnostics/dt7-gimbal-20260913/。两次采样均已退出，
  镜像/配置核对、解析及Python语法和文档差异检查通过；未修改控制代码/接口、构建、
  烧录、复位/暂停/恢复CPU或写应用内存。静音及起停性能改善仍待有效运动窗口。

- 09-13 用户报告yaw起动响应慢、回中停得慢，本轮仅给调参方法，未采新运动窗口或代改。
  当前源码仍内环100/0/0/限6000、协议12000、手动100度/秒/限30RPM、仅速度环，
  与此前已匹配ELF配置一致。候选先{120,0,0,6000,0}，其余模式/速度/限幅不变；
  双向加速与回中制动无抖动/反向/超速且改善后，才逐级150、200。历史P300曾严重超速，
  不直接跳300/700；候选非稳定性证明。静止满杆目标16.667RPM，P100/120分别1667/2000；
  回中实际+10RPM时比例制动-1000/-1200（滤波前），因此小增P可同时加强加速与制动。
  I当前为0，不能归因为积分拖尾；D是速度测量微分，先0，不按位置环“加D刹车”直接套。
  output_lpf_rc=.002s、dt4ms时新输出权重2/3，旧输出12ms后占1/27；无证据先改通用滤波。
  若回中速度目标已0且使能/对齐持续1才评价制动；非零目标、失联归零或输出饱和分别排查。
  仅速度环不锁角；位置环恢复须待内环稳定且外环output_max从当前12000改为合适RPM限幅。
  源码/静态算术及文档差异检查通过，TI官方PID调参说明复核；未构建/烧录/硬件操作，
  当前起停时间、运动中饱和/使能与目标曲线仍未知。

- 09-13 随后“现在呢”只读复查：5组/800ms，Flash再次匹配同一1b6ad6d7监控ELF。
  enabled/startup_ready均1、双轴控制有效/反馈年龄5～6ms、服务状态OK；pitch2024已在
  1566～2205范围内，旧越界启动联锁已解除。pitch目标1971、速度目标-53RPM、实际0RPM、
  电压请求-530；仍有53刻度/约2.33°静差，不宣称已到位。yaw3054、目标速度0/命令0，
  当前仅速度环，零速不代表锁位；未捕获非零推杆，不能证明右杆运动跟随已恢复。
  首次分RPC读快照持续跨4ms回调导致序号不一致；临时包装将块/序号/tick合并一次RPC，
  保留同偶数校验后取得上述有效记录，不能把首次无一致快照解释为MCU停止派发。
  证据build/monitor/dt7-gimbal-recheck-20260913-batched.*及上述diagnostics目录recheck-*；
  解析断言/Python语法/文档差异检查通过，无控制改参、复位/暂停/恢复CPU或烧录。

- 09-13 用户报告DT7左杆底盘可动、右杆双轴不动，确认左拨杆下档且已改参烧录。
  运行中只读快照3组/400ms：enabled1/startup_ready0，yaw1936、pitch2915，双轴RPM0、
  服务命令均0/状态OK，反馈年龄6～7ms。Flash与本地监控ELF逐字节匹配，ELF SHA256
  1b6ad6d7e2beff417f790e787ad11275996c901601aaf043c55bfdbeb83056cc；离线GDB从该ELF确认
  pitch限位1566～2205、初始1971。当前2915超过上界710刻度/约31.20°，启动捕获拒绝，
  双轴共用联锁导致一起零输出；这是本窗口确定阻断，非右杆模式或仅PID幅值判断。
  已烧录yaw内100/0/0/限6000、手动100度/秒/限30RPM/仅内环；pitch外1/0/.1/限10000、
  内10/0/0/限10000。没有完全采用上轮候选限幅；保留用户修改。未采右杆原始输入。
  待用户确认pitch是否重新拆装：旧标定仍有效则禁用输出后恢复合法机械姿态；安装变化则
  重新测三点，不能直接放宽限位或用2915替换初始角。恢复后的使能/推杆效果尚未验证。
  默认监控首次OpenOCD curstate=unknown被报为未运行；临时诊断包装仅poll刷新后确认
  running/DHCSR0x01010001，不是CPU停机证据；通用监控的初始状态等待仍待修。
  完整GCC主机回归、镜像/ELF配置及快照断言、文档差异检查通过；未改控制代码/接口、
  暂停/复位/恢复CPU、烧录或写应用内存。CSV/元数据/log在build/monitor/dt7-gimbal-check-
  20260913-polled.*，匹配ELF/配置/summary及临时包装在build/diagnostics/dt7-gimbal-20260913/。

- 09-13 用户询问双轴PID并确认要参数，本轮仅给起调候选，未应用：yaw内环
  {100,0,0,3000,0}，电流协议限建议同步3000，手动60度/秒、限10RPM、领先10度，
  保持speed_loop_only=true；双向速度及回中制动稳定后，外环候选{0.02,0,0,10,0}
  再关闭速度单环开关。pitch候选外环{1,0,0,60,0}、内环{10,0,0,3000,0}，
  保持1971启动、1566～2205限位、重力/运动前馈和yaw耦合关闭。数组顺序为
  Kp/Ki/Kd/output_max/integral_max；外环输出RPM，内环为各自电流/电压原始刻度。
  候选依据历史超速及当前单位/限幅取保守初值，不是辨识结果或已验证稳定增益；
  yaw静止10RPM时P1000，pitch静止速度目标最大60RPM时P600，可能不足以克服负载。
  最新磁盘yaw内环35/0/1/限6000、协议12000，手动100度/秒/限30RPM；pitch仍
  外5/0/.1/限10000、内2/0/0/限10000。未读板，不能当作当前已烧录参数。
  pitch目标仍每回调累加60*rate未乘dt，外环actual=0使测量D无效，单调PID不能修复。
  源码字段/单位、候选静态算术及文档差异检查完成；MathWorks官方串级调参顺序复核。
  无控制代码/接口修改、构建、烧录或硬件操作；实车启动出力、速度稳定性及静差待测。

- 09-11 yaw追踪只读180组/10.567秒：Flash匹配145088 B镜像e9070a47c2b29842641bbdd279507fcd796bbb7dc4dbf7a905543e0eeaef018e。
  用户新配置已在板上：速度内环300/0/0、PID/协议限幅均12000，手动60度/秒、限10RPM、仅内环。
  捕获约0.35秒短推杆，目标0.06～5RPM；推杆样本原始RPM-5～63，回中目标0时RPM-57～73，
  内环输出-12000～11463.64，启用90组有4组触负限（稀疏快照比例，不是持续饱和率）。
  回中tick118057/raw3645，随后raw最高4524，约119114起零速稳定在4507～4509，净续转约38度。
  本窗口显示短时严重超速/反向与回中续转，最终零速收敛；没有持续恒速平台，不能量化稳态跟随误差。
  控制有效期间reference/startup均1、反馈在线；PID dt3～5ms/中位4，逐字段采样非原子约18ms，
  不能以跨字段瞬时不同步判定反馈符号错误。位置环关闭，连续位置目标不参与追赶/锁位。
  tick122395后遥控时间停止，122644采到enabled0，之后输出0，电机offline_mask始终0；
  遥控超时发生在已零速收敛之后，不应当成前面超速根因。无修改、复位、暂停或烧录。
  证据build/diagnostics/yaw-tracking-now-20260911/含匹配ELF/bin/Flash、schema和180组日志JSON；
  镜像匹配、解析/统计与文档差异检查完成。当前P300稳定性及编码/RPM高速一致性仍需进一步核对。
- 09-11 遥控失效复查：用户确认期间做过重开/复位类操作，操作前根因无法由恢复后窗口反推。
  当前只读100组/28.413秒RC增加2030完整帧（约71Hz），UART错误/失败启动0，online、armed、
  gimbal enabled/startup均1，摇杆五通道均0，电机offline_mask0。没有捕获推杆窗口。
  Flash逐字节匹配bin7d50ce98f644fbcaff159e678f4c49a10803ef757b3939d774e26b525f6ce935；
  与上一yaw-chatter镜像仅5字节不同：yaw协议限幅10000改50000、内环output_max10000改50000。
  command_limit为int16_t，板上0xc350即-15536，DjiMotor_CommandLimit返回0；全车配置校验失败，
  MotorService_Init返回UNSUPPORTED，各电机CommandCurrent/Configured在初始化处返回，应用丢弃状态。
  再只读确认motor_service.c::s_initialized=0，故该配置错误会阻止整车服务输出，不只是yaw；
  反馈在线/遥控使能不代表电机服务已成功初始化。电机离线灯语不会报告此配置错误。
  当前代码电流模式有效上限16384（协议注明约3A），应先恢复已用10000，再按实测饱和情况
  分步提高且PID output_max与协议限幅保持一致；50000无效。P700、目标10RPM时静止P约7000，
  单纯提高上限不增加未饱和出力。未修改参数、复位或烧录；只读/镜像差异与文档检查完成。
  证据build/diagnostics/remote-loss-latest-20260911/；仍需有效配置烧录后验证推杆与电机服务恢复。
- 09-11 用户报告yaw转到固定角度附近颤动/咔咔声，并确认机械部分排查正常。只读100组/
  5.091秒仅捕获停机：raw恒3951、RPM0、enabled/startup0、PID输出0，反馈电流-20～24；
  CAN1 ESR全0。未捕获发声运动，不能将该静止窗口作为颤动根因证明。
  Flash匹配当前bin9faa2135b6cbf4c52eea4fd47a8f0c4399853c660a828714722014f654196ca8，
  板上yaw速度环700/0/0/限10000、仅内环、手动60度每秒/10RPM，pitch耦合已false。
  源码速度调试的电流计算无绝对角度分段/外环换向；相邻编码跨零仍正常展开，但20ms
  反馈连续性检查仍可作废双轴。高P使1RPM反馈变化对应700刻度P变化（滤波前），可能
  放大速度波动并反复驱动/制动；固定角度原因仍须在发声窗口核对原始编码/RPM及valid。
  证据build/diagnostics/yaw-chatter-20260911/含匹配ELF/bin、Flash、schema、Tcl/log/JSON。
  只读与解析/源码、文档检查完成；未改参数、驱动、暂停、复位或烧录。
- 09-11 按用户要求为旧yaw→pitch视线耦合加入显式开关：
  limits.gm6020.enable_yaw_pitch_compensation，默认false，步兵pitch显式false。
  开关与重力补偿独立，false不计算/应用耦合或显示旧模型；true恢复旧公式，但自瞄仍强制
  禁用。关闭期间继续更新相邻yaw历史，重新开启不补算累计转角；配置头及接口注释已说明。
  完整GCC主机测试通过：最高目标1566在yaw移动/原tan奇异点2048处保持、开启后能补偿、
  自瞄覆盖、关闭/重开不补算及原有限位/失联链。两车型just build/ELF检查通过，FLASH
  145088/145944 B、RAM53992 B；日志/tmp/rm-pitch-coupling-switch-{host,build,sentry-build}.log。
  差异检查通过；未烧录、操作硬件或修改冻结底层。保留用户gravity_compensation=0、零点1971、
  PID及手动目标增量。关闭耦合能排除该目标改写来源，不证明残留位置静差已消除。
  旧耦合模型及机械零点未验证，须验证后才重新开启；配置布局变化，读板重新匹配ELF。
- 09-11 同镜像随后再次限位复查：10组/993ms，pitch1627～1628、RPM0、反馈持续更新，
  目标1578.460，enabled/startup均1。距最高1566仍61～62刻度（约2.68～2.72°），
  实际尚未到最高边界，目标也还差12.46刻度。同目录recheck.tcl/log/json及Flash再次匹配；
  只读、解析/文档检查通过，无控制修改或复位烧录。
  进一步核对该窗口：pitch_rate全0；位置误差-49.54～-48.54刻度，外环P5输出约
  -247.70～-242.70 RPM，内环P2/I0输出约-495.40～-485.40，远未触10000限幅，
  电流反馈约-317/-316（反馈刻度，不与电压命令直接比较）；D因actual固定0仍无效。
  因此当前目标本身未到1566，且非零误差下比例出力固定、轴静止；符合负载/静摩擦
  下残留静差，尚不能排除机械阻挡或确认先前持续推杆窗口。调大output_max本身不增此出力。
  目标路径复核：direction=-1，先target-=60*pitch_rate，再减yaw耦合补偿，最后裁
  1566～2205；回中仅停止摇杆增量，yaw变化仍能把已到1566的目标拉回范围内。
  因此此前“回中保持”仅在无耦合改写时成立；1578.460快照不能区分输入先停止与补偿
  拉回。重力零点1971只接入重力项，尚未用于yaw耦合。未改目标或补偿逻辑。
- 09-11 前次限位位置只读复查：10组/995ms，pitch2167～2168、RPM0，反馈持续更新且
  组尾年龄8～13ms；目标2203.909、enabled/startup均1。距离最低位置2205仍37～38刻度
  （约1.63～1.67°），未到最低边界，也未到最高1566；目标接近边界不等于实际已到。
  Flash与本地最新bin完全匹配249826222cf387f56a937985d6b2a5486ac05bb54ed439f6e7c89ed612c26d57；
  按对应ELF重新生成布局，板上gravity_zero_angle1971、gravity_compensation已为0。
  证据build/diagnostics/pitch-limit-after-gravity-20260911/含Flash、匹配ELF、Tcl/log/JSON。
  解析/镜像匹配与文档差异检查通过，未修改控制代码、暂停、复位或烧录；机械接触未确认。
- 09-11 按用户标定将步兵pitch重力正弦零点设为1971.0f：新增
  limits.gm6020.gravity_zero_angle（绝对编码刻度，默认0兼容原生基准），应用重力项改为
  direction*gravity_compensation*sin((raw-zero)*2π/8192)，在1971处为0；配置头已说明填写。
  零点独立于initial_angle，重力幅值5000、PID、1566～2205目标限位及yaw耦合逻辑保留。
  此参数仅用于云台应用正弦重力项，旧驱动角度模式的恒定补偿未改；步兵pitch为APPLICATION。
  完整GCC主机测试通过，增加零点零输出、两侧±100刻度反号、默认0兼容与车型配置断言。
  两车型just build/ELF检查通过，FLASH144912/145768 B、RAM均53992 B；日志
  /tmp/rm-gravity-zero-{host,build,sentry-build}.log。差异检查通过，未改冻结底层或烧录。
  配置布局已变化，后续读板须重新匹配ELF。该改动未证明停转已消除；补偿幅值/实际重力
  相位效果、遥控超时原因及此前列出的pitch目标dt、无效D、yaw耦合奇异仍待验证/处理。
- 09-11 用户确认pitch约1695为持续推杆时停住；随后只读检查60组并等待复现300组/54.705秒，
  均未捕获启用或推杆：rc_frame_count恒64191、last_remote_ms恒916566、remote_online/
  gimbal enabled/startup均0、armed1，pitch1694～1695反馈持续更新。当前零输出来自遥控
  超时禁用，不能据此断言此前带使能停转同因。Flash仍匹配7c4929d7，无控制写入/复位/烧录。
  源码没有1695提前限位：目标仅裁1566～2205。按板上外P5/内P2、I0、重力-5000*sin(raw)
  静态核算，actual1695/target1566给PID-1290、重力-4817.856、合计-6107.856电压刻度，
  此方向两项同号；target2205则合计约+282，必须采实际目标/输入符号才可区分。
  确认旧逻辑缺陷：pitch仍每回调累加60*rate未乘dt；外环传actual=0使测量微分D无效；
  重力与yaw耦合直接使用原生编码角，后者tan在合法区内2048附近奇异，机械零点未标定。
  这些缺陷尚未证明是本次停转根因，未代改。PID_Reset保留target/actual字段，残留正速度
  目标2513.972不能视为当前有效命令。源码/算术与解析及文档检查完成；同证据目录新增
  pitch-limit-logic、pitch-reproduce日志/JSON/Tcl和pitch-static-calculation.txt。
- 09-11 随后复查pitch最高限位：6次/1.697秒实测1694～1695、RPM0、反馈持续更新，
  距最高位置1566仍有128～129刻度（约5.6～5.7°），编码未到最高边界且已在合法范围。
  此窗口gimbal enabled/startup均0，不能沿用下方2474越界作为当前禁用原因；未确认机械接触。
  Flash重新读回仍完全匹配7c4929d7；证据同目录pitch-now.tcl/log/json及pitch-now-flash.bin。
  只读与解析检查、文档差异检查通过，未改控制代码/接口、复位、暂停或烧录。
- 09-11 此前双轴不动只读诊断：yaw反馈7234、pitch2474，6次/1.69秒反馈时间持续
  更新；遥控帧40915→41034、remote_online/enabled/armed均1，两CAN ESR及离线mask均0。
  pitch2474超过已测最低位置2205（269刻度），capture_startup_position拒绝对齐，startup
  全程0、两轴PID持续重置并走零命令分支；当前阻断是pitch越界联锁，并非掉线或PID幅值。
  尚未确认越界来自断电下垂、人工移动或安装变化；保留1566～2205限位，需先核实机械位置。
  板上144376 B镜像与最新含拨盘恢复逻辑的144888 B构建不同；在诊断目录隔离重建旧版
  符号并与Flash读回逐字节完全匹配（bin SHA256 7c4929d7f3743ae9740a72edd29e4f5be27267084e4f65ae5e605bfdc7e26bc4），
  再读取RAM；证据build/diagnostics/gimbal-stopped-20260911/{matched.elf,flash.bin,baseline.log,baseline.json}。
  全程无暂停/复位/烧录/控制写入，未改实现与接口；CPU运行、CFSR/HFSR均0。
- 09-11 pitch三点绝对位置记录全部完成：第1点“初始位置”1971（1970..1971），
  第2点“最低位置”2204.5（2204..2205，整数参考2205），第3点“最高位置”1566（1566..1566）。
  每点20组、RPM全0、反馈持续更新且年龄≤100ms，CAN2 RX208/软件8、ESR0、CPU运行/fault0。
  用跨零展开后中位数记录；机械高低不等于编码大小。用户随后要求恢复原有限位，并确认
  1971为固定启动位置；旧“暂不开pitch限位”要求已被替代。
  证据目录build/diagnostics/pitch-positions-20260911/，positions.json/md及position-1/2/3.log/json；
  准备时Flash匹配bin595516ba2bece9f33e9e1140d60ec502ed2bb5de9ff4198df3d8b78de0f7b686，
  ELF74629f7b78cfc9807b64b0c336d6ca16035ca0497579f331dea690aa9c9d9bce，地址按此ELF提取。
  三次采样均已正常退出；解析、稳定/反馈时效断言及文档差异检查通过；无控制修改、主动驱动、
  复位/暂停/烧录或应用内存写入。

- 09-11 pitch按用户要求最小恢复：删除MotorConfig_t的angle_limits_disabled字段及应用/
  驱动绕过分支；步兵angle_min=1566（最高）、angle_max=2205（最低）、initial_angle=1971。
  遥控增量和耦合补偿之后沿用原有目标裁剪；启动反馈或初始目标越界时双轴保持零输出。
  capture_startup_position改为尊重pitch非负initial_angle，避免被当前反馈覆盖；每次启动/
  失联恢复重新对齐均使用1971，负数仍锁存当前位置，yaw保持原有锁存行为。
  未新增制动/裕量/实际越界保护策略，未调整PID、重力/耦合补偿或冻结底层。
  该逻辑限制的是目标，不能保证机械实际位置不因惯性/补偿越界；实车边界表现仍待验证。
  配置结构字段已删除，后续诊断须按匹配新ELF重新提取布局，不能复用旧结构偏移。
  完整GCC主机测试通过：覆盖固定初始角、两端越界拒绝启动、边界目标裁剪及反向离开、
  非法初始角、负数初始角兼容和失联停机。速度/底盘方向测试固定各自输入，避免实车60度/秒、
  yaw_ccw_sign=-1调参污染预期；生产配置保留。`just build`、`just build sentry_swerve`
  及ELF检查通过，FLASH分别144376/145248 B，RAM均53976 B，无未解析符号，未链接RTOS启动。
  日志/tmp/rm-pitch-limits-{host,inf-build,sentry-build}.log；差异检查通过，未烧录或连接硬件。

- 09-11 yaw电流限幅采样已结束：1212组/84.476秒，启用186组、推杆147组（仅正向目标），
  实际板上inner700/0/0/output_max5460、协议10000、前馈0，手动60度/秒/限10RPM、仅速度环。
  Flash匹配bin e593ca6c56f84427b648137432b2c01b95072b794c4728ba206dd28ba3780f77；匹配ELF及证据
  存build/diagnostics/yaw-current-limit-20260911/，summary/analysis/decoded及原始capture均已保存。
  PID及TX2FE槽0都到过±5460，各为推杆147组中的6组，CAN正负各3；约4.1%仅是稀疏快照比例，
  不是完整回调/时间饱和率。电流反馈−5430..5582（约−0.994..1.022A），已产生接近1A电流；
  当前限幅来自PID5460，前馈关闭时继续增协议10000无效。目标连续10RPM的139组原始RPM−8..27、
  中位8，存在超速/短时反向，不能只提高电流限；高P/量化/负载作用仍需分离。dt3–6ms/中位4ms。
  每组读取15–23ms，字段和PID结构非原子，不能把瞬时actual/pout不自洽或TX/电流不同号当成算法错误。
  初始遥控过期；tick224859ms起启用，237820ms起RC年龄208ms而禁用，最后输出0；两CAN ESR0、
  全电机在线、CPU运行/fault0。用户结束后stop文件退出只读采样，会话17550已完成。
  解析/证据断言/Python语法及文档差异检查通过；未改参/构建/复位/暂停/烧录或应用内存写入。

- 09-11 用户报告内环P加至700才转、没有锁位；最新磁盘已是manual_rate_deg_s=500，
  manual_speed_rpm=10，speed_loop_only=true，inner={700,0,0,50000,2000}，outer仍{1,0,0,50000,0}，
  协议5460/前馈0。未重新读板，不能把上轮实测5RPM和P50套到当前用户运动窗口。
  当前理论目标clamp(stick*500/6,±10)，约12%杆量就满10RPM；静止满杆P7000最终裁5460。
  解释历史实际改动：目标从每回调70*rate改按dt积分30度/秒、原300/500RPM模式限速统一10，
  推杆领先限10度、按用户要求旁路位置环。该修复当时保留3.3/3.3/.825及协议5460；后来的
  P50、I/D0和当前P700属后续调参，上一轮1000限幅仅建议，当前未采纳。速度环回中0RPM不锁角度。
  当前建议先把manual_rate500降至60，与10RPM上限对应，恢复整段摇杆线性而保持当前最大目标；
  不再盲加P或升50000输出限，PID限应与现有5460或经验证的更低协议限一致，I/D先0。
  P700能启动不等于已调稳，先核对±目标/回中制动与每1RPM误差引起700刻度输出变化的抖动；
  内环确认后才能speed_loop_only=false，并同步用低外环P候选.02、输出限10、积分限0替换旧1/50000。
  1/10度误差该候选给.455/4.551RPM、静止内环P约319/3186，不能保证克服当前负载或精确锁位；
  依据静差与出力再调整外环/有限速度积分，禁止只加I期望仅速度环记住角度。
  源码字段/顺序、旧记录和算术及文档差异检查通过；MathWorks下游饱和说明复核。
  未修改配置、构建、烧录或硬件操作，当前700参数的运动曲线及实际启动电流仍未知。

- 09-11 基于上方250刻度不启动实测给出分阶段调参建议（未应用）：首轮yaw内环
  {100,0,0,1000,0}，并把该轴protocol.dji限幅建议降至1000；speed_loop_only保持true，
  手动30度/秒（满杆5RPM）、manual_speed_rpm建议5，目标领先10度、前馈全0。仅普通手动。
  无振荡且出力不足再单改P为150、200，满杆静止理论输出500/750/1000；触1000且电流跟随
  仍无编码运动时停止继续加P，检查机构/线束阻力和所需启动电流，1000不是已验证安全或足够值。
  双向启动/回中制动稳定后才以已验证P加Ki10、积分限200，必要时Ki20，D继续0；持续1RPM
  误差Ki10每秒只增加10刻度，观察积分残留拖尾，不能照搬外环开启时测得的Ku/ZN。
  内环通过后外环候选{0.02,0,0,5,0}，模式限速初始统一5；1/10度误差对应约0.455/4.551RPM。
  仅内环时target_lead和外环PID不改善启动；速度上限是目标限幅。前馈bias在零速也出力，暂不用于
  双向静摩擦补偿；若将来叠加前馈，须按最终限幅处理积分（MathWorks官方anti-windup说明已核对）。
  真实源码字段/秒单位/限幅顺序及上述算术、文档差异检查通过；未改控制代码、构建、烧录或硬件操作。

- 09-11 最新yaw不动已捕获用户确认的双向推杆：运行中纯只读240组/29.320秒，Flash144352 B
  与当前构建完全一致，bin SHA256=82fbbd576b48fdbe26606d9cf69868c9df654856388b5831af46607d4516d970，
  ELF=3ca69c80bf0a0ebe8e76bc2125c187ae39b02892f720b9334960b74b5b6aa1fc。板上仅速度环50/0/0，
  manual_rate30度/秒→满杆5RPM，限速10RPM；输出限20000、协议限5460。遥控71.42Hz、ch0覆盖
  ±660及回中，双下档、armed/startup/gimbal enabled全1，无mode override，全部电机在线，
  两CAN ESR全0、CAN1提交+14372/错误0。负/正满杆目标∓5RPM、PID∓250，实际TX2FE槽0也为
  ∓250；负满杆电流反馈中位−251，正满杆4组250..296，回中目标/输出/TX回0。全段RPM=0，
  编码730..735（约0.22度），证明当前出力已到电调但未带动轴；不把缓存Flush后清零当成未发。
  满杆250约0.046A，Ki=0使静止推杆不继续加力；单增output_max不改变该值。下一步保持低速/D0，
  用明确小电流上限逐级核定启动出力后调P/有限I；尚不能区分负载静摩擦和额外卡滞或给出稳定增益。
  内环快照dt3–5ms/中位4ms；每组读取17–22ms且非原子，不当作完整周期分布。CPU运行/fault0。
  证据及匹配ELF/bin在build/diagnostics/yaw-no-motion-20260911，解析、证据断言/Python语法与
  文档差异检查通过。无控制源码/参数修改、构建、烧录、复位、暂停或应用内存写入。

- 历史（下述3.3参数已由用户改为上方50/0/0）：09-11用户澄清检查yaw“仅内环”配置，认为电机在线但PID不当。源码当前speed_loop_only=true，
  manual_rate_deg_s=30→满杆5RPM，manual_speed_rpm=10只作上限；外环旁路，速度内环仍实际调用
  3.3/3.3/.825、output_max20000/integral_max2000，最终按TX2FE协议限5460。与上轮已核对
  板上配置一致，本轮未重新连接硬件。源码复核：spin/vision在速度调试时强制目标0RPM，
  只能普通手动模式检验摇杆响应；完整反馈/连续性联锁保留。
  用真实pid.c及当前车型参数作4ms固定零速度反馈实验：满杆P16.5、I每秒约16.5刻度，
  输出0.1s≈18.084、1s≈32.934、5s≈98.935；第2帧反馈0→1RPM使D≈-91.667，最终输出≈-48.576。
  证明低速出力很小且D可盖过P，不能独立证明实车静摩擦门槛或推杆期间唯一根因；此前Ku5.5
  测量外环开启，不作为单速度环有效Ku。建议保留5RPM，I/D先0、P逐级增加并设小限幅；
  P50/100在静止满杆时分别对应250/500刻度，仅初调尺度，非验证后的稳定增益。
  内部20000与下游5460不一致，在大误差时PID无法按真实下游限幅约束积分；不是本次静止
  小输出的直接限流原因。未擅改PID/控制源码、构建ARM、烧录或硬件操作。
  实验源/日志build/diagnostics/yaw-inner-config-20260911/{check.c,result.log}，GCC编译及
  D项数值断言、文档差异检查通过。yaw推杆时目标/实际/最终CAN出力仍待采样确认。

- 09-11 灯语烧录后用户报告遥控无法控制，完成运行中只读复查：Flash144352 B与本地bin
  逐字节一致（bin d24e107af8018b8b86a70b053a523e381b48b73452fadf8bbd0d2223c82ae55b，
  ELF7d51a853），确认用户已烧录灯语/发射联锁/连续yaw/前馈/底盘跟随版本。
  CPU运行、CFSR/HFSR0。完整快照10组每组约392ms，仅用于配置；随后精简50组/10.613s，
  每组13–15ms，yaw/pitch反馈时间持续更新，组尾年龄13–18/12–17ms（含读取耗时）。
  遥控约71.52帧/s，最近命令年龄5–20ms；armed/startup/gimbal enabled均1，CAN1/2 ESR0、
  全车电机offline_mask0、CAN故障/恢复计数0。不是当前反馈失联或输出未解锁。
  全程五通道0、双拨杆下；未采到推杆，yaw运动链阻断尚未定位。yaw4645–4646、RPM-1..0，
  速度调试目标0（满杆配置仍5RPM），静止内环输出约17，PID仍3.3/3.3/.825。
  pitch164/RPM0，位置目标7376.43，外环约-4897.84RPM；板上速度内环Kp/Ki/Kd/output_max
  全0，内环输出恒0，位置闭环不能出力，仅原重力补偿保留（前馈关闭）。这是确定配置缺口，
  与灯语任务无直接控制关系，未擅自填参。问用户采样时是否推杆及具体失效轴，尚待回复。
  证据build/diagnostics/gimbal-remote-20260911/：镜像比较、ELF重取schema、Tcl/log/JSON、
  summary与analysis.md。首次普通用户USB打开失败，随后sudo只读成功；无复位/暂停/烧录、
  verify_image/checksum、寄存器或应用内存写入。解析/诊断脚本检查及文档差异检查通过；
  无控制源码/参数改动，main一次性灯语授权保持结束。

- 09-11 为yaw/pitch补充可配置运动前馈：原先只有pitch的direction*gravity_compensation*
  sin(raw*2pi/8192)重力项及yaw-pitch目标耦合，无统一运动前馈入口。新增
  `GimbalFeedforwardConfig`及`MotorConfig_t.feedforward`，字段velocity_gain/bias/output_max；
  ff=clamp(velocity_gain*速度内环目标RPM+bias, ±output_max)，叠加在速度PID之后，
  pitch另加原重力项，再按现有协议最终限幅。yaw取模式限速后目标，速度调试也有效；
  pitch取位置外环输出；前馈无历史状态，不求目标微分，不额外乘direction。
  0限幅关闭并忽略另两字段；启用后非有限/负限幅/计算溢出停止两轴、清PID并重新等稳定反馈。
  步兵两轴/哨兵yaw均显式全0关闭，配置文件开头已说明填写公式、单位、符号、关闭方式、
  原重力项独立、零速bias仍出力以及示例仅作单位说明。命令是各自协议原始刻度，非A/V，
  步兵yaw电流与pitch/哨兵yaw电压系数不直接互抄；未新增加速度/轨迹速度外环前馈。
  全套GCC主机回归通过：双轴正反向/偏置、限速后目标、PID叠加、前馈/协议双限幅、
  关闭兼容重力、启动等待/禁用/失联/恢复及四类错误双轴归零。测试参数仅在桩车型修改。
  两车型`just build`及ELF检查通过，FLASH142496/143368 B、RAM均53944 B；步兵ELF SHA256=
  41c1d631e337600940154791f0b3a6d9108d2262451f1f088aaa2bf869efb86c。
  日志`/tmp/rm-feedforward-{host,infantry-build,sentry-build}.log`，差异检查通过、冻结底层未改。
  无烧录/复位/实机驱动；PID、原重力幅值、协议及yaw速度调试开关保留，pitch内环仍全0。
  前馈独立于PID，启用即可出力但不补齐速度环参数；实车系数及pitch重力角度标定仍待确认。
- 09-11 按用户要求完成目标生成/连续角度两项修复，步兵yaw暂时**旁路位置外环**，尚未烧录。
  新增`YawControlConfig`及`MotorConfig_t.yaw_control`指针，步兵配置`speed_loop_only=true`；
  普通遥控满杆30°/s直接给±5 RPM、回中0 RPM，仅闭合电机速度环；视觉/spin不介入位置调试，
  对应yaw速度目标0，底盘模式不改变。内环3.3/3.3/.825、外环1/0/0与5460协议限保留，未清I/D。
  `application/gimbal/yaw_reference.c/h`保存连续位置/目标，输入按真实dt积分，推杆新目标领先限10°，
  回中冻结目标；仅反馈增量处理跨零，位置误差不重新选圈，单圈angle_target接口保留。
  普通/视觉/spin显式模式选择限速（步兵均10 RPM），模式切换锁当前目标并按实际测量初始化内环D；
  spin世界航向只在进入时选圈。连续控制/反馈间隔>20ms、越界/位移包络/速度>=500RPM作废，
  两轴归零后重等100ms稳定反馈并锁当前位置；20ms/500RPM为软件跟踪有效性约束，不是硬件额定值。
  哨兵补充配置并保留串级及普通300/视觉500/spin500 RPM限速，手动旧70刻度按200Hz参考换算
  615.234375°/s、领先限180°；换算参考不代表实测周期，哨兵新输入响应仍需验证。
  完整GCC主机回归通过：不同dt积分、领先/回中冻结、多圈误差、速度旁路、普通回中不提速、
  视觉/spin限速/跨半圈/切换、配置/输入错误与连续性丢失双轴停机及重对齐。
  `just build`和`just build sentry_swerve`及ELF检查通过，FLASH141120/142024 B、RAM均53936 B；
  步兵ELF SHA256=f0eada7c87ddad81d5cebbba05cad2f406a18eaa884ba9163cfeeeda9e51cd7d。
  无未解析符号/RTOS链接，冻结底层未改，差异检查通过。未烧录/复位/驱动实机，内存地址须按新ELF提取。
  提前制动、积分消退/抗饱和策略仍未实施；速度调试不锁位置，不能把0RPM目标等同于已稳定停车。
- 09-11 已形成[YAW_CONTROL_PLAN.md](YAW_CONTROL_PLAN.md)，下列为原提案，实施状态以上段为准：
  秒单位目标积分、推杆时限制领先量/回中冻结目标、普通推杆与回中统一限速；yaw应用维护连续角度
  与目标，跨零仅展开反馈增量，控制误差不逐轮重选圈数；反馈过期/圈数不确定走停机重对齐。
  根据实测制动能力与延迟提前减速，超调/背离目标进入零速制动，低速后再追原目标；保护每内环检查。
  速度环先独立P，再有限I，D先关闭；有效限幅抗饱和与制动时旧积分消退分别实现，不每回中重置PID。
  低速候选满杆30°/s、统一10 RPM、领先10°；内环验证后外环P候选0.02 RPM/刻度，均非已验证参数。
  前向4555只作参考，不自动回正或猜机械限位。保持冻结底层、CAN协议和依赖方向。
  源码核对、1/4/5/20ms积分单位算术与文档差异检查完成，无控制代码修改、构建或硬件操作。
- **yaw头朝前参考（用户确认姿态，2026-09-11 11:11北京时间）**：CAN1/软件ID5/RX209
  单圈绝对编码器15次读数4555–4556，中位4555，转速全0、反馈时间持续更新。
  记录前向参考为**4555刻度/8192（编码器原生角约200.171°）**，对应前向相对角0°；
  不是IMU世界航向或多圈位置。当前安装下适用，机械重装后须复核。Flash仍匹配af444433，
  云台禁用、PID输出0；只读记录，未把该值写入控制配置、未复位或烧录。
  证据在`build/diagnostics/yaw-forward-20260911/forward-reference.json`及同目录原始日志；
  15次解析、镜像主机比对与文档差异检查通过。
- 09-11 修复后运动只读记录完成：Flash读回与当前139536 B bin逐字节匹配，SHA256=
  `af444433e3841a99aba8afde62f6d2b3182c528f72a1f44d576c4b4756601f42`，确认队列修复已在板上。
  CPU运行、fault=0；15次静止快照中遥控/反馈在线、输出解锁、启动位置已捕获，内环
  3.3/3.3/.825、外环1/0/0。内环dt为3–5 ms（中位4），外环19–21 ms（中位20）；
  派发约246次/s、普通队列覆盖增量0。此为短时非原子快照，仍未达到1 kHz/200 Hz。
  连续5554组/183.445秒，145.320秒首次推杆、169.199秒禁用，运动窗口727组。运动内环
  dt 3–7 ms（中位4/P95 5）、外环19–23 ms（中位20/P95 22）；约30Hz非原子快照。
  183.010秒内云台命令发布/派发各45177，所有主题覆盖/拒绝增量0，预算触顶0；派发约
  246.86Hz。CAN ESR全零、提交错误无增长，396组编码器/RPM运动方向一致、相关0.998。
  首次约0.43秒推杆目标前进约174°、实际仅约3.3°；每回调70*rate未乘dt，灵敏度随频率变。
  145.719→145.751秒回中使速度目标300→500 RPM。最后167.281–169.167秒回中目标恒
  5055.6816，仍负转约2.006圈；167.698/168.633秒最短误差跨半圈换号，速度目标+500→-500，
  制动重新变顺向加速。168.198秒目标+203.6/实际约-79，P+906/I-1205/D-524，输出仍-591。
  D全段-1925..1741，部分瞬间抵消制动；I最高2000，输出-3402..3887，未采到5460饱和。
  禁用时最近遥控时间停止更新、年龄约228ms，符合遥控过期路径；随后PID/TX2FE均归零，
  171.000秒首次RPM=0。队列修复有效，持续转圈仍涉及目标/限速/制动及环参数，不能只套ZN。
  先按dt积分并限制追赶距离/加速度、统一普通回中限速，再独立调低速内环P→有限I，最后
  低增益位置环；保留跨零语义，不以回中重锁当前位置掩盖超调。具体稳定增益及高频D来源待测。
  脚本、schema、镜像、日志/CSV、summary、overview/release曲线与analysis.md在
  `build/diagnostics/yaw-20260911-after-queue/`。解析/绘图运行、图像检查及文档差异检查通过。
  本轮无控制代码/参数修改、烧录、复位、暂停或应用内存写入，记录已停止。
- 09-11 已修复上述反馈洪水与控制命令共用可覆盖队列的代码路径：消息中心新增
  `MsgCenter_PublishLatest(topic,key,phase,...)`、`MsgCenter_UseLatest(topic,phase)`，固定32槽，
  临界区交接载荷；同键只保留最新状态，容量/phase冲突明确失败，不挤掉其他键。
  CAN管理器按软件电机ID合并DJI解码反馈，不再重复发布其TOPIC_CAN_RX；非DJI/未知帧入口保留。
  Cmd初始化为RC/IMU/视觉目标及三类控制命令保留独立槽；派发顺序为最新状态→最多64条普通
  消息→最新控制命令→发送hook，每槽每轮最多一次，普通队列洪水不覆盖这些控制命令。
  `MsgCenterDiagnostics`新增各主题发布/派发/覆盖/合并/拒绝计数，合并旧状态与丢事件分开统计。
  完整GCC主机回归通过，含90万次反馈状态发布伴10万普通事件、启停命令送达、持续回调补发、
  容量耗尽、真实双CAN九电机9000帧解析/合并、非DJI入口，以及真实云台回调在普通队列溢出下
  失联归零。旧测试已适配5次外环分频；用固定测试PID隔离用户调参，同步当前轮序/可调限速。
  两车型Debug构建及ELF检查通过，FLASH步兵139536/哨兵140448 B、RAM均53912 B，无未解析符号，
  RTOS未链接；初次并行构建触发共享工具锁，顺序重试成功。源码/文档差异检查通过，冻结底层、
  当前PID/目标累加/300与500RPM限速未修改。新内存布局须用新ELF重新取诊断地址。
  上方已运动复测dt、各主题计数及遥控过期停机；不宣称达到1kHz/200Hz或消除振荡。
  回调变快会改变现有每调用70*rate的实际推杆灵敏度，目标按dt积分仍是运动验证前的待办。
- 09-11 用户操作的 yaw 运动已捕获：1417 次运行中只读快照/46.907 秒，约29.962秒
  云台禁用、随后电流归零/转速收敛。Flash与本地138256字节bin逐字节一致，SHA256=
  `0d3639560ceecfb552cb7b9fe47269552943f77012f2a67ced599d89ab3d535d`；内环3.3/3.3/0.825，
  外环1/0/0、5次调用分频。无暂停/复位/烧录或应用内存写入，CPU运行、fault=0。
  启用窗口内环保存dt为7–280ms/中位29ms，外环87–697ms/中位234ms，非1kHz/200Hz。
  正式采样约30Hz、每组读取11–14ms、非原子，dt统计为快照加权而非完整回调周期分布。
  停机后独立低频只读5.948秒：CAN共约9051帧/s，每DJI帧投递两条消息；队列派发约9910条/s、
  覆盖约8811条/s，921次派发全部碰64条预算，队列占120–127/127，确证持续过载。
  控制命令共用满时覆盖最旧项的队列，存在被覆盖风险；无分主题丢帧计数，不能把每次PID
  长间隔唯一归因到某条消息。优先减少重复反馈投递、保证控制定时读取最新反馈，不能无限派发。
  14.819–17.650秒回中目标固定6456.061，速度目标却反复±500；回中时实测-300→-500RPM。
  I最低-2000；16.006秒速度目标+500/实际-88，P+1940.4、I-1968.27、D-32.06，滤波输出仍
  -149.59，积分记忆抵消制动。当前PID输出-3688..1323、D-495..527，未观察到协议±5460
  饱和，不能沿用历史Kd825导致饱和的结论。CAN ESR全零，反馈年龄中位6ms/最大17ms，
  TX2FE邮箱有非零命令、禁用后均0；编码器展开速度与RPM运动符号一致，无视觉/spin模式。
  用户确认Ku5.5测量时外环一直开着，因此不是独立速度环Ku；ZN秒单位换算正确但调参前提
  不成立。建议先修队列/周期及目标dt积分、统一回中限速，再独立调内环、最后闭合位置环。
  未修改控制代码/参数或构建；诊断Python编译及文档差异检查通过。证据、匹配ELF/bin、CSV、
  曲线与详细分析存build/diagnostics/yaw-20260911/。具体耗时来源及修复后稳定性仍待验证。
- 最新“遥控突然失效”只读诊断已分离两个阻断：首次6次采样tick255066→265645ms，
  串口rx_events/TC/完整帧均4264不变，最后命令接收时间60152ms，DMA使能且NDTR=18、
  UART RxState=BUSY_RX/无错误；watchdog重启尝试继续增加但HAL_BUSY，不能据此认定DMA
  卡死。CPU运行、uwTickFreq=1、CFSR/HFSR=0，九台CAN反馈均更新，两路ESR与恢复计数0。
  当前Flash与14:39构建bin061ae6d8逐字节一致；将其yaw Ki/Kd恢复.0033/825后SHA256恰为
  上次已烧录f521fc18，证明这两次bin仅Ki/Kd共7字节变化，没有遥控/底层二进制变化。
  后续UART复核2.02秒增加144帧，用户确认刚重开遥控器（接收机绿灯常亮）；助手没有
  复位/寄存器写入，故不能把恢复归因于诊断。再采10次确认帧8330→8654，
  tick386999→391539ms，约71.4Hz，cmd最后接收时间持续更新，串口→解码→消息回调恢复。
  仍armed=0、gimbal enabled/startup=0，最新拨杆右下/左中[2,3]、五通道全0；当前剩余
  阻断为开机输出未解锁，已告知双拨杆下、所有轴回中≥500ms，再切工作模式。CAN故障计数0，
  不把armed=0直接归因于此次遥控断流。cmd_input.remote_online一次0可为未解锁路由临时
  覆写窗口，不能当作再次掉线；多字段读取非原子。接收机为何停止输出仍未知，未实际核对
  之后解锁/推杆响应。遥控主机链路测试通过（TC/IDLE、解码/消息、BUSY、掉线/重连/解锁）。
  证据存build/diagnostics/remote-loss-20260910/{identify,snapshot,uart-check,restored}日志，
  snapshot/restored JSON、Flash及image-comparison.json；无控制代码修改、烧录或主动驱动，
  文档差异检查通过。后续以恢复窗口为准，不再把4264静止计数当作当前仍断流。
- 用户查询其终端烧录结果：读取最新14:39:20（北京时间）openocd-flash.log，目标两次核对
  0x413/1024KiB，末尾FIRMWARE_VERIFY_OK/FIRMWARE_FLASH_OK，确认本次写入校验成功；
  日志FW_RUN_AFTER=0，结束时保持暂停。DEPRECATED和2000→1800kHz提示未导致失败。
  14:39:11构建manifest与当前ELF哈希一致：227b8ead054d23c45a85b924140fb2cb12f6502de66bd4d45478be5961680097。
  当前磁盘yaw内环已改3.3/0/0、限20000/2000，外环仍1/0/0、限50000/0；未从Flash
  独立读回参数。只查保存日志/本地构建和源码，未读取终端屏幕、执行新构建、烧录或运行；
  文档差异检查通过，无控制代码/接口修改。运行效果及此前发散是否解除仍未知。
- 用户在上述新PID烧录后报告推杆响应慢、松手小振荡逐渐放大到猛烈转圈；说明其后已进行
  运行测试，不沿用烧录结束时“保持暂停”描述当前运行状态。本轮未连接板卡，模式与参数
  来源已询问、待回复；磁盘仍outer=1/0/0、inner=3.3/0.0033/825。
  /tmp/yaw-instability-audit直接编译真实pid.c和云台应用（桩反馈/服务）复现：零初态速度
  反馈0→1RPM、dt=1/2/4/8ms，D分别约-137500/-117857/-91667/-63462，PID均裁-20000、
  最终命令均-5460；这证明数值过敏/饱和，不是物理模型或实车根因证明。普通模式目标落后
  时松手角度目标不变，但速度目标可300→500RPM；静止反馈满杆连续累加，超过半圈后最短
  路径误差换号（5次分频第61调用采到），两者已用实际应用复现。修正临时测试采样窗口后
  专项全部通过，初版60次未覆盖换号后的第61次外环更新。
  排查优先级：停用发散配置的推杆测试；内环先清I/D并清历史、限制速度/电流，核对同坐标
  电流/RPM/编码器变化符号，先调速度环再接低增益位置P环，稳定后按秒单位少量加速度I。
  Ki=.0033在500RPM恒误差仅积1.65刻度/秒，不能仅凭“越摆越大”认定积分饱和。
  普通目标应按dt积分、限加速度及追赶误差，回中不应仅凭rate==0提高限速；PID输出限与
  协议裁剪须匹配。若仅spin异常，再查IMU角度/角速度/命令符号和时效；direction配置在yaw
  APPLICATION发流链上未乘入，不能靠盲翻该字段修正反馈方向。Ti/Td若来自毫秒必须换秒，
  参数来源未证实，不推荐猜换算后直接运行。仅解释/源码与数值核对、文档差异检查，无控制
  代码/接口修改、ARM重建、烧录或硬件操作；振荡波形、实际dt/反馈方向仍待验证。
- 最新用户调参已编译烧录：yaw outer=1/0/0、限50000/0，inner=3.3/0.0033/825、限20000/2000；
  CAN1/ID5、TX2FE槽0、协议±5460及pitch禁用限位保持。步兵Debug编译/ELF检查通过，
  FLASH/RAM=138256/49328 B。普通用户USB打开失败发生于init、未擦写；随后sudo执行同一
  OpenOCD受检流程，目标0x413/1024KiB两次核对通过、reset halt、写入及verify_image成功，
  日志有FIRMWARE_VERIFY_OK/FIRMWARE_FLASH_OK。沿用当前run_after=false，烧录后保持暂停，
  未运行或实车验证。ELF SHA256=738c1f463920041caeb2b5f5dc1ce803f146300ba16c5ba51a041ec4066469d8，
  bin SHA256=f521fc18c298b90964fa00163c6c5d685a819a0b4784520a9e56d1ab6fa8547e。
  成功日志：build/linux-aarch64-306202d6/inf-debug/openocd-flash.log。未代改参数、接口或冻结底层，
  文档差异检查通过；未重跑已有分频断言失败的主机套件，实际周期和新PID稳定性仍未知。
- 上次yaw PID链路检查：磁盘与用户截图一致，outer=1/0/0、限50000/0，inner=6/0.5/40、
  限20000/2000；此前调参数字仅属历史。配置经MotorDriver_Init装入两环，APPLICATION
  路径由云台计算角度误差→5次调用分频外环→限速→每次速度内环→协议限幅→服务/适配器
  →CAN1 TX2FE槽0；RX209角度/RPM回填同一上下文，未见断环或二次PID。
  手动非零输入限300RPM，回中/视觉/spin限500RPM；最终电流仍±5460，PID内部±20000
  不能感知下游饱和，抗积分饱和限值不一致。Kd40在1ms、反馈跳变1RPM时滤波D约-6667，
  总输出约-2224（零初态）；Reset把last_measure置0，运动中重置也会产生微分瞬变。
  配置“限30RPM/禁用积分微分”注释已失效。普通目标仍每调用累加70*rate，未乘dt；
  主循环每轮命令/派发后HAL_Delay(1)，NowUs实际为HAL_GetTick()*1000，不能保证1kHz/200Hz。
  普通模式反馈为电机RPM，spin为-g_gz*30/π；当前只检查CAN反馈时效，未见独立IMU时效联锁。
  步兵Debug增量构建及ELF检查通过（无待编译项），FLASH/RAM=138256/49328 B。
  完整主机套件CAN恢复通过，control recovery第61行断言失败：旧测试要求下一调用即响应，
  未适配外环分频；后续未执行。/tmp/yaw-chain-audit专项用真实应用/PID、桩电机服务验证
  外环保持/5ms dt、300/500限速、内环到协议裁剪、失联双轴归零与分频/积分重置，全部通过。
  未修改控制代码/接口或冻结底层；未核对板上镜像、烧录或实车测试，实际周期/运动方向/稳定性待测。
- 用户要求自行实现yaw/pitch外环按内环1/5更新且比例可调，新增算法逻辑集中pid.c/h，
  助手只给说明与检查，不代改控制代码。最新磁盘pid.c/h已补齐PID_CalculateDivided
  声明/实现、两轴5U分频宏、独立状态及Init/Reset；两轴外环已接入，内环每次调用，
  pitch使用speed_rpm反馈、重力补偿位于内环之后。接口缺失的旧编译错误已解决。
  pitch inner配置仍全零，驱动跳过初始化；即使补齐分频接口，当前参数也只剩重力补偿。
  pitch outer仍为5/0/0.1、限10000/15000，须按新RPM输出单位重新配置；actual=0使外环D为0。
  建议每实例分频：首调用/Reset后立即计算，其后每N次更新，中间保持输出和时间/历史
  状态，内环每次执行；N默认5、1不分频。算法不识别电机角色，应用只接入接口。
  当前由云台命令消息触发计算，分频只保证调用次数比例，不证明内环1kHz或外环200Hz。
  临时主机测试直接编译当前pid.c通过：第1/6/11次更新、保持完整历史状态、5ms dt、
  实例独立、变更比例、Reset后首算、0/1分频及空指针。CC=gcc sh tests/host/run_tests.sh：
  CAN恢复通过，control recovery仍因pitch第189行缩进触发-Werror失败，后续未执行。
  临时测试未写入仓库；文档差异检查通过；无控制
  代码修改、ARM构建、烧录或硬件验证。未保存编辑器内容不可见，实际控制周期待测。
- 用户最新报告yaw振荡收敛变快，但锁位不牢、到目标不稳，明确只要修法、不代改。
  最新磁盘outer=0.6/0/0.001、输出300、积分限0；inner=30/0/0.05、输出2000、积分限0，
  协议仍5460；实际软件最终上限min(2000,5460)=2000（约0.366A）。注释仍写限30及禁D，
  已不符合数值；未核对板上镜像或采运动波形。静止小误差时P乘积18原始刻度/编码器刻度，
  两环无I，对恒定负载可能留偏差；这不是已确认的唯一根因。
  手动yaw目标每条命令累加70*rate，未乘dt；外环以actual=-angle_error调用PID，故其D
  也响应目标变化。遥控死区3，须先排除回中目标漂移；普通模式锁编码器相对位置，spin
  用IMU锁绝对航向。建议逐项验证：目标固定、低速收敛，再有限内环积分改善静差；
  速度目标应按真实dt积分并限加速度。仅源码复核和文档更新/差异检查，无参数修改、
  构建、烧录或硬件操作；具体负载、稳态误差、饱和时段和控制周期仍未知。
- yaw震荡任务在上轮结束前被用户打断，已执行部分补记：诊断前板上bin e0355d64，
  用户yaw位置PID50/0/0.1、输出30000，速度PID30/0/0.1、输出5000。`yaw-oscillation`
  100次/4509ms时遥控已离线，输出0、CAN无错、yaw反馈在线，未捕获振荡波形。
  静止比例增益乘积1500/编码器刻度，约3.3刻度即可能到5000，增益导致饱和换向是疑点，
  尚未证明方向/机械或IMU问题不存在。助手将outer改0.2/0/0、限30RPM、积分限0，inner
  保留Kp30、I/D=0、输出限5000、积分限0，保留电流协议5460和pitch禁用限位。
  步兵构建/ELF检查、CAN与control recovery测试通过；新bin仅yaw PID 21字节变化。
  已烧录并FW_RUN_AFTER=1重启，bin0792f22cc2239337f18ae2fff0c28bd1f5a67898402d25c6d7193c1f10c0ca13，
  ELF b30013d7b9f92aa82c319b592af503790042950723aa310700b3421a559719fd；`yaw-damped`
  读回一致，CPU运行无fault、CAN无错、反馈在线，但遥控/armed/startup=0，不能宣称消震。
  再启用需双拨杆下、摇杆回中≥500ms解锁，再验证回中和小幅跟随。
- 最新“小陀螺/转向定义”查询时用户磁盘又更新：yaw outer.output_max=300（注释仍30），
  平移0.90m/s、旋转1.20rad/s，本地bin e98045ef；本轮未读板，不能把本地构建认定为已烧录。
  未覆盖用户新修改；上段30RPM只对应助手实际烧录0792f22c。本轮只查源码并更新文档，
  差异检查通过，无额外硬件操作。
- 最新yaw通信/遥控状态只读复查`yaw-remote-status`：30次、5088ms，Flash仍匹配
  4ff20979；yaw/CAN1 RX209上下文时间持续更新，两路ESR与故障结构全零，CAN1提交
  +403次且发送错误0，邮箱33次可见TX2FE槽0命令498–500。遥控收到364帧、约71.5Hz，
  remote_online/outputs_armed/gimbal enabled/startup全程1。CPU运行、CFSR/HFSR=0。
  本窗口yaw摇杆和yaw_rate均0，证实接收在线，尚未直接验证推杆映射。yaw保持目标
  3869.712，反馈3250–3256、转速均0，内环输出498.586–500、电流反馈-13..513；
  当前出力/位置跟随仍需排查，不能把链路在线等同于控制正常。仅诊断与文档更新，
  无复位、烧录、调参或主动测试命令，文档差异检查通过，Tcl/log/JSON/Flash已留档。
  同一日志进一步解析PID：角度环输出9183.531–9296.168 RPM，经过代码限速后速度环
  target恒500、actual恒0，pout恒500、iout恒0、dout=-1.097..0，故最终约500。
  实际命令由yaw速度PID计算，再受协议5460裁剪；限流不是目标电流。Ki=0使静止误差
  不继续累加出力；这解释500的来源，机械负载/阻力是否为唯一不动原因仍未证明。
  本轮仅源码和现有日志交叉核对并更新文档，未重采硬件、改参或烧录，差异检查通过。
- **历史再次不动（09-11已按新测量值恢复限位）：禁用pitch限位的配置又被覆盖**。`logic-recheck`纯读取30次，
  板上镜像匹配用户最新bin `df174c2dae8b5feb45ea4c1df824a63c7213aadd8733b766e609d6c01046c1df`，
  已不同于上次修复74826299；源码和Flash中的angle_limits_disabled再次缺失/为false。
  pitch反馈5635–5636超出旧1000–4000，startup全程0，两轴PID输出0；遥控/总使能/云台
  enabled均1，两轴反馈持续更新，两路ESR=0，CPU运行无fault。本窗口摇杆回中，未采到推杆。
  可确认配置覆盖和镜像更换，无法确认是编辑器缓冲区、复制文件或其他保存动作造成。
- 当时按用户“先不要开pitch限位”恢复`.angle_limits_disabled=true`，补充省略此项
  会恢复旧限位并阻断两轴的注释；保留用户新PID、5460电流限幅和底盘参数。
  新bin相对本次修复前板上镜像仅0x08020D50一个字节0→1。沿用保存烧录授权，经目标
  核对、reset halt、烧录、校验并FW_RUN_AFTER=1重启；本机默认run_after=false不变。
  最新bin SHA256=`4ff20979018dba5b3038ab04d76990059b312fd4f84c71fd86896ecdd4fbfdc2`，
  ELF SHA256=`7dab9544a47fa3dcbe6707479b12468660dc4bf369fb16a64eba8df8c9340475`。
- `logic-repaired`复查30次，Flash与构建逐字节一致、禁用限位字节1；startup/遥控/总使能
  全程1，两路ESR=0、两轴反馈持续更新，CPU运行无fault。yaw目标3038/反馈3038–3039，
  pitch目标5635/反馈6557–6558，约922刻度静态偏差仍存在；只证明启动联锁解除，
  不宣称摇杆跟随或出力调参已完成。重新安装后的重力/耦合补偿零位仍未校准。
- 历史500刻度输出窗口：yaw电流限5460（约1A），位置PID15/0/0.45，速度PID1/0/0.1，两环输出/积分限30000。
  yaw的outer是角度环、inner是速度环，两环固定串联；pitch只用outer角度环加重力补偿，
  inner全零不会阻断pitch。启动要两轴反馈均≤100ms且稳定100ms，任一失败归零两轴。
  另外yaw速度目标在控制器中限制手动300/回中500RPM；当前速度Ki=0、Kp=1，静止稳态
  若到此限速，只产生约300/500原始电流刻度，而非5460。是后续出力排查点，尚未采到
  本配置的非零摇杆窗口；不能把增大协议上限等同于实际电流自动增加。
- 本次步兵Debug构建/ELF检查及CAN恢复、control recovery主机测试通过，覆盖启动、
  跨零、重开限位及失联双轴归零；FLASH/RAM=137968/48760 B。未跑完整主机套件，
  车型/底盘/DJI测试仍硬编码旧参考PID及546，待与调参解耦。源码/文档差异检查通过，
  冻结底层未改，无新接口。日志前缀为`build/diagnostics/yaw-20260910/logic-*`。
- 上次同一故障：`both-stopped`pitch4909、禁用限位字节0，bin9818523e；助手修复并烧录
  bin74826299后`pitch-limit-fixed-check`证实startup=1。这次新镜像又恢复了该字节0，
  不能把上次修复后的状态套用到新烧录。编辑器若仍显示缺少开关的版本，应核对完整路径
  和磁盘差异，保存时保留该项；工具无法读取未保存编辑器缓冲区。
- 历史0.1A饱和证据：先前`short-motion`30次、4879ms，bin ccaca1a8、电流限546，
  两轴已启动，yaw目标1070.803、反馈2272–2277、输出持续-546、转速0。
  `clean-check`曾捕获推杆和非零电流反馈响应，证明电流指令链路能产生出力；负载/机械
  阻力与所需电流未分离。这些是旧调参窗口，不代表目前仍限546。
- **诊断工具事故与恢复**：本次最初`yaw-check`/`remote-on`/`timing-check`误用
  OpenOCD `verify_image`，其CRC算法在目标未暂停时仍先覆盖工作区RAM，破坏
  0x20000000起的.data（uwTickFreq低字节变11，时基约快11倍），导致人为遥控掉线。
  已向用户说明并执行`restore-runtime`复位运行恢复，读回SystemCoreClock=72000000、
  uwTickFreq=1、CPU运行；未擦写Flash。上述三份受污染日志不用于原故障判断。
  脚本已删除verify_image，后续纯dump_image+主机比对；有效结论只用恢复后的纯读取日志。
  **运行中诊断禁止verify_image/checksum等会在目标执行算法或占用工作区的命令**。
  只读诊断不等于任意OpenOCD“校验”都只读，必须核对命令副作用。
- 下述2026-09-09状态保留作历史，当前以本节最新有效采样为准。

- 最新 `chassis-vibration` 运行中只读40次、5599ms：**yaw/CAN1 RX209已恢复在线**，
  九台反馈均更新；CAN1/CAN2接收+27928/+22375帧，两路ESR及故障结构全零。
  pitch角度5398–5399，超出旧配置1000–4000，`capture_startup_position`拒绝启动，
  两轴仍发零。用户确认pitch电机重新安装，随后明确“先不要开pitch限位”。
  此前缺yaw反馈与当前pitch越界是不同窗口的阻断条件，不再把yaw称为当前离线。
- 本次源码新增 `limits.gm6020.angle_limits_disabled`（默认false），仅步兵pitch设true；
  跳过启动范围检查及应用/旧电机驱动的目标裁剪，旧上下限留存但不生效。
  无限位目标按8192刻度归一化；pitch角度换算改用真实一圈8192，不能用机械上限4000。
  保留两轴反馈稳定等待、失联停机、上电锁存位置及协议出力限幅。新安装机械行程、
  重力/耦合补偿零位未校准；不再等待上下限采样，也未自动驱动寻找机械端点。
- 两次最新Flash读回均匹配已换算yaw PID的bin（SHA256=e0b9132c41d13a443133c05992f0a0b8276ce0c69a50cf7387b808649e5f4aaf），
  故旧“yaw PID未烧录、板上全零PID”已失效；本次助手仅只读诊断，未执行烧录。
  板上yaw仍CAN1/ID5、TX2FE、±546约0.1A，底盘仍旧速度单环，pitch仍旧限位。
  此为09-09采样；09-10纯Flash读回已确认上述修改在板上。
- `gimbal-can-recheck`较早窗口6次、5256ms仍缺yaw，其余8台更新，两路ESR/故障全零；
  Flash同样为上述yaw PID版本。后续`chassis-vibration`已经另一启动，不能跨窗口相减计数。
- 用户此前确认：两台故障电机均为 APP **1.0.11.2**，当时电流环均关闭，用于 yaw；
  正常电机为 **1.0.6.2**，用于 pitch。两台故障不表示同时接入，也不表示分别属于两轴。
- 最新变化：用户开启 CAN1/ID5 电机电流环，称“控制模式是电压指令”，并确认官方助手
  可以驱动电机。助手测试的具体通道未确认，不能当作 C 板 CAN 链路成功证据；
  随后确认**橙灯常亮**，官方 v1.4 第 5 页定义为“电流模式收到电压指令”，与主控
  当时 TX2FF 电压配置冲突。现已按用户要求烧录电流通信测试程序：yaw TX2FE、
  限幅546（约0.1 A），位置/速度环增益及上限均为零，只发送零电流；未写电机参数。
- **新程序已正常收到 ID5 反馈**：用户确认烧录后首次采样期间误断电；恢复供电后
  `current-firmware-powered` 在5126 ms内 RX209 +5107帧、约1 kHz，反馈时间持续更新，
  CAN1 ESR全零且故障计数不增长。断电窗口不能用于判断新程序或电机固件不兼容。
  这证明当前程序能接收解析该台1.0.11.2电机反馈，不代表已验证非零出力。
- 用户随后要求“验证电流指令”：已向 CAN1 发送 20 帧 **0 A / TX2FE**，硬件 TXOK
  20/20、ESR=0，并取得 20 条 RX209 原始反馈。仅验证零指令传输，未测试非零出力。
- 最新接线：用户确认**其他电机已经接回**，随后手动reset。复位后
  `post-reset-check` 只读采样5198ms：CAN1/CAN2分别新增15503/20798帧，软件编号
  1、2、4、6、7、8、9反馈持续更新；CAN1 RX203（编号3）及RX209（yaw编号5）
  时间戳始终为0。两路没有采到Bus-Off，CAN1一次ESR=0x00440000（TEC68），
  保存的最近错误为填充错误；CAN2 ESR始终为0。复位未恢复缺失节点，不能用空CAN2解释。
- reset后遥控恢复约71Hz。最新用户称摩擦轮正常、底盘不动，`chassis-check`实测
  remote_online=true、outputs_armed=true、配置有效，已解除此前总输出锁定。
  底盘轮序3/2/1/4，对应feedback_seen=0/1/1/1：CAN1 ID3/RX203始终未收到反馈。
  `ChassisController_ComputeCurrents`要求全向轮四轮反馈齐全且均不超过100ms，
  任一缺失就归零四轮电流；这是确定的底盘阻断条件，摩擦轮不受此底盘局部联锁影响。
  本窗口双拨杆下/下、摇杆回中，底盘命令也为零，未采到推动摇杆时的非零目标。
  yaw/RX209仍缺反馈、云台上电对齐未完成；yaw PID仍全零。两路ESR快照全零，
  但CAN1保存的最近填充错误值变化，不能宣称链路已完全无错。
- 用户交叉测试：相同 CAN ID、终端拨码均 OFF，分别接 CAN1/CAN2；正常 pitch 电机
  两路均正常，故障 yaw 电机两路均异常。第二台故障电机的独立交叉实验细节未记录；
  是否完全复用正常支路的 CAN 线、电源线和供电端口仍未确认。
- 先前电流环关闭时已排除模式不匹配；此结论不覆盖上述新设置。现有公开协议及收发
  代码未发现版本不兼容；不据固件版本号改 PID、命令 ID、CAN 波特率或恢复策略。
- 故障随电机移动降低了 C 板单路及公共解析代码作为根因的可能性。固件与故障有对应
  关系，但固件、其他保存参数、硬件批次和随行支路仍未分离；尚不能认定固件 Bug 或
  某个收发器损坏。没有两版电调源码/镜像、完整变更记录或同一电机版本 A/B 证据。
- 历史采样确实出现 Stuff/Form/Bus-Off；控制指令语义或应用层字节解码不能解释这些
  总线错误。若与固件有关，应验证内部 CAN 位时序、时钟、错误恢复等，而非猜测改帧格式。
- 用户已确认供电开启、线序正常、yaw 绿灯每秒闪 5 次。断电测得 yaw 单独约 75 kΩ
  对应终端退出，不能据此判断上电通信；早期“yaw 单独和总线均 120 Ω”的测量矛盾
  尚未解释，整总线终端及电机端波形/24 V 实测仍缺证据。

### 当前步兵配置与兼容性

- 2026-09-10 用户报告 yaw 临界增益 Ku=5.5、振荡周期约2 s；本次源码内环为
  5.5/0/0。按经典闭环 ZN：Kp=3.3、Ti=1 s、Td=0.25 s；本库 dt 已换算为秒，
  积分乘 dt、测量微分除 dt，因此配置增益应为 Kp/Ki/Kd=3.3/3.3/0.825。
  历史3.3/0.0033/825相对此结果积分小1000倍、微分大1000倍，符合误用毫秒的换算。
  本次仅计算并核对源码/NI公式及算术，未修改控制参数、构建或烧录；文档差异检查通过。
  Ku是否在速度内环单独闭环、I/D为零、无明显限幅的等幅持续振荡下测得仍未确认；
  不将此结果视为已验证稳定参数，也不据此计算外环增益。

| 轴 | 软件编号 | 总线 / 硬件 ID | 反馈 ID | 控制 ID / 槽位 | 最终限幅 |
|---|---|---|---|---|---|
| yaw | 5 | CAN1 / 5 | `0x209` | 电流 `0x2FE` / 0 | PID±20000，协议最终±5460；已烧录校验，用户后续报告运行发散 |
| pitch | 8 | CAN2 / 4 | `0x208` | `0x1FF` / 3 | ±25000 |

- 配置见 `config/robots/infantry_standard.c`；详细模式和现有 PID 说明见
  [GM6020 控制模式](../gm6020-control-modes.md)中的历史调参记录。当前pitch PID输出上限10000，
  叠加重力补偿后，应用与CAN发送边界最终仍裁到25000。
- 1.0.6.2 与关闭电流环的 1.0.11.2 在公开协议层面可共用当前电压配置。
  驱动也支持 `GM6020_COMMAND_CURRENT`、`0x1FE/0x2FE`、最大 ±16384（±3 A）；
  若主动切换，必须同步配置模式、发送组和限幅，并重新核定 PID/重力补偿。
- 软件不读取固件版本或电调保存模式；校验仅验证本地模式、地址、槽位的一致性。
  `CommandCurrent` 等旧接口接收所选协议原始刻度，不是统一的安培值。
- 用户指定参考库 `NYUSH-Robotics-Club/nyush-rm-control`，GitHub默认main在本次读取为
  `2b7ca720e856f1efda9f6de6259faea42654720d`，Makefile默认infantry。
  `application/robot_configs/robot_infantry.h:134` 的yaw角度PID=28/0/1.3，死区0.008°、
  积分限100、输出限2200°/s；速度PID=150/135/0，积分限4000、输出限26000。
  启用速度前馈（增益0.85、LPF系数0.40、限650°/s），主控闭环为角度+速度。
  该库yaw为CAN1/ID6，DJI驱动实际发送TX2FF电压指令，非本项目TX2FE电流模式；
  其角度/速度单位为°/°每秒，本项目普通yaw环用编码器刻度/RPM，参数不能直接照搬。
  随后按用户要求采用此P/I/D：位置环增益乘(360/8192)/6，得到0.205078125/0/
  0.009521484，输出限2200/6 RPM、积分限100/6 RPM；速度增益乘6，得到900/810/0。
  速度环输出与积分限546（约0.1A），未照搬电压26000/积分4000；单位换算不等于
  电压/电流出力等价。本库反馈来源、死区、滤波、手动限速保留，未移植速度前馈。
  主机测试验证非零正负输出/限幅、yaw单轴反馈过期双轴归零及单位换算；未操作硬件。
  用户再次指定yaw使用此参考PID后，源码与现有测试断言复核一致，无需重复修改；
  上述为历史参考调参；当前已被用户新参数替代，以本节最新读回记录为准。
- 历史核对：`ddac446` 的 yaw 用电压；`4029c98` 曾改为电流（`0x2FE`、4096/0.75 A），
  `5a3315f` 改回电压。历史配置不代表实物曾成功转动。官方 v1.4 明确电流功能要求
  APP >=1.0.11.2、Assistant >=2.7；v1.2 电压范围为 ±30000，v1.4 为 ±25000，
  尚不知限幅变动对应的精确 APP 版本。完整固件变更清单仍未找到。
- 本地英文 v1.4 第 7 页 `0x2FE` 表将 Motor ID 写为 1/2/3；本次检索官方中文
  v1.4 对应表明确为 **5/6/7**，与现有分组一致。零电流测试按中文版的 ID5 槽位执行。
- 反馈按总线和 RX ID 匹配；角度为大端 uint16，转速/电流为大端 int16，无版本分支。
  GM6020 温度字节未向业务事件转发，是共有的观测缺项，不是解码错位。

### 关键采样与证据

原始日志、JSON、Flash/RAM文件按日期保存在 `build/diagnostics/yaw-20260909/` 与
`build/diagnostics/yaw-20260910/`。09-10受污染窗口和恢复后的有效窗口须分开。
此前接收诊断为运行中 SWD 快照，不是 CAN 分析仪抓包；多字段非原子，不跨启动比较计数。
此前只读诊断未 halt/reset/resume/烧录；本次零指令主动测试短暂 halt/resume，详情如下。

| 证据前缀 | 有效结论 |
|---|---|
| `live.log` / `snapshot.json` | CAN1 曾持续 Bus-Off；yaw 无有效反馈。两路 BTR=`0x00180002`，结合 RCC/标称 12 MHz 晶振对应 1 Mbps。 |
| `termination-off` | 两路无新增错误，但用户随后确认 yaw 已拔线；不能归因为关闭终端。 |
| `reconnected`、`line-check` | 接回后 CAN1 再次出现填充/格式错误，`line-check` 直接采到 Bus-Off。 |
| `callback-check`、`communication-check2` | 后续窗口异常转到 CAN2，前者直接采到 Bus-Off；当时物理接线未确认。 |
| `link-check` | 6 次、约 5.1 秒，两路 ESR 全零且故障/恢复计数不增长；CAN1 收到 `0x208`，但 pitch 配置在 CAN2，故其上下文未更新。 |
| `current-ring-check` | 开启电流环后读取，6 次跨 5115 ms；两路 RX 始终 0，CAN1/CAN2 故障分别 +155/+154，两路发送提交失败各 +930；当时接线未确认。 |
| `single-id5` | 仅接 ID5，6 次跨 5116 ms；CAN1 RX 与 ID5 反馈时间始终 0；直接采到 Bus-Off，故障 +155、恢复 +154，发送提交失败 +928。 |
| `lowlevel-audit` | 6 次跨 5085 ms，RX/ID5 反馈仍为 0、CAN1 故障 +154/恢复 +153；读回确认 GPIO AF9、时钟/位时序、过滤器分区与 RX0 中断使能正确，再次直接采到 Bus-Off。 |
| `orange-led` | 橙灯常亮时 6 次跨 5118 ms；CAN1 RX 92679→97779（+5100，约 1 kHz），末帧均 RX209；角度 4785–4786、转速 0、电流原始值 116–134，反馈时间持续更新。CAN1 ESR 全零、故障计数不增长。 |
| `current-zero-test` | CAN1 标准 DATA8、TX2FE、8 字节全零；20 次硬件 TXOK、ESR 全零；20 条原始 RX209，末条电流原始值 −2、温度 30℃。恢复后 RX209 与时间戳继续更新。 |
| `current-firmware-sample` | 新程序首个窗口 RX=0、位显性错误；用户随后确认误断电，不用于软件兼容性判断。 |
| `current-firmware-powered` | 新程序恢复供电后6次、5126 ms；CAN1 RX53863→58970、末帧均209，角度5073、速度0、电流原始值−8～8；ESR全零、故障/发送提交失败不增长，邮箱可见2FE零电流。 |
| `remote-can-check` | 其他电机接回，6次跨5199ms；7台反馈有效，CAN1 RX203/RX209未进入对应上下文，遥控计数883不变、输出锁定。Flash读回与当前构建bin SHA256一致。日志及JSON已保存。 |
| `remote-uart-check.tcl` / `remote-can-check.json`的uart_followup | 后续3次串口/DMA快照均等待18字节、无接收进展；CAN1 ESR出现0x007d0001后归零，CAN2 ESR为0。未读USART DR，未暂停CPU或写寄存器。 |
| `post-reset-check` | 用户reset后6次跨5198ms；CAN1/CAN2新增15503/20798帧，仍缺CAN1 RX203/RX209；遥控恢复约71Hz，双拨杆下/上、未解锁。Flash读回匹配当前bin，CPU运行、CFSR/HFSR=0；只读，无复位/烧录/寄存器写入。 |
| `chassis-check` | 6次运行中只读采样；总输出已解锁、遥控在线、底盘配置有效；轮序3/2/1/4中ID3未见反馈，四轮电流全零。采样时摇杆也回中，无非零目标实测；Flash读回匹配当前bin，日志/JSON已保存。未改代码或绕过联锁。 |
| `gimbal-can-check`（历史） | 6次、5259ms；仅yaw反馈时间为0，pitch与其余7台持续更新，ID3已恢复。两路ESR/故障结构/发送提交失败均0，遥控在线且输出解锁；云台未完成对齐，两轴发零。Flash与当前bin一致，日志/JSON/Tcl/读回已保存。 |
| `gimbal-can-recheck` | 6次、5256ms；已装yaw PID镜像，yaw仍缺反馈，其余8台更新；两路ESR全零。 |
| `chassis-vibration`（最新） | 40次、5599ms；yaw恢复，九台更新，pitch5398–5399超旧限位，两轴启动受阻；两路ESR全零。摇杆全程回中、底盘disabled、目标/电流均零，未捕获行驶颤动。Flash匹配e0b9132c；Tcl/log/JSON/读回及对应ELF已归档。 |

- `single-id5.log/.json/-flash.bin`：Flash 前 147456 字节与原诊断镜像一致，
  9 个软件配置 ID/通道核验通过；两路 LEC 均为 5（位显性错误），TEC 快照最高 248、
  REC 均 0，末次 ESR=0x00f80057 含 BOFF。输出锁定、CPU 运行、CFSR/HFSR=0。
  整车程序仍向空 CAN2 提交发送；`bsp/can/bsp_can.c` 任一路故障会中止两路发送并
  锁定输出，单电机测试受该联锁影响，但不能据此单独解释 CAN1 无接收。
  尚未获取物理波形，不能仅凭寄存器判定断线、具体器件损坏或固件版本根因。
- 底层复核 `lowlevel-audit.tcl/.json/-flash.bin`：镜像仍一致；CAN1 PD0/PD1、CAN2
  PB5/PB6 均 AF9；标称 PCLK1=36 MHz、BTR=0x00180002，即 1 Mbps/83.3% 采样点。
  过滤器 0/14 均启用、32 位掩码全零、FIFO0、CAN2SB=14、FINIT=0；IER=2、NVIC
  RX0 使能、目标向量及 IRQ 调用的 hcan 地址核对通过。采样 CPU 运行、CFSR/HFSR=0。
  `CAN_Manager_ProcessCallback` 在 ID/DLC/厂商筛选前累加 RX；全零不能由反馈解码差异
  单独解释。未开 CAN 错误通知，HAL error 值不能代替 ESR；`tx_ok` 仅表示提交邮箱成功。
  自动重发/自动 Bus-Off 恢复关闭，已有软件恢复，并非缺失恢复；空 CAN2 联锁中止
  发送、不直接禁用 CAN1 接收。GPIO 输入高电平仅为稀疏快照，不证明线路无跳变。
  已询问 C 板自身是否有外部供电（区别于电机 24 V），尚待回复；完整代码复核未发现
  新版专属接收分支缺失，物理链路及电机 CAN 内部行为仍待独立测量。
- `orange-led.log/.json` 已保存；CAN2 无节点、ESR=0x00d00033（ACK 错误/错误被动），
  输出仍锁定。恢复 CAN1 接收前用户做了什么接线/供电操作尚未知，不能把橙灯告警
  当作先前位错误的原因；模式冲突与空 CAN2 联锁需分别解决。没有复位、烧录或解锁。
- `current-zero-test.tcl/.log/.json`：先核对 Flash 与原诊断镜像一致及输出锁定、CAN1
  无错，再暂停 CPU 避免电压帧混入，临时允许 CAN1 在调试暂停时运行，通过邮箱 2
  发送零电流；硬件 RQCP2/TXOK2 检查成功。随后恢复调试配置和 CPU，联锁仍关闭。
  无 Flash/应用配置/电机参数修改；原程序恢复后仍发送电压帧，不能据此宣称已永久
  切换电流模式或消除橙灯。FIFO 原始读取会消耗反馈，暂停窗口不用于反馈频率统计。
  发送/超时/错误的 Tcl 模拟检查通过；恢复后日志摘录为 `current-zero-after-excerpt.log`。
- `link-check` 中 yaw/pitch 反馈时间均为 0，其他 7 台持续更新；输出锁定、CPU 运行。
  无法仅凭 `0x208` 判断发帧的是正常 pitch 还是改过 ID 的 yaw，不能把该窗口无错
  当作故障已修复。历史观测不是当前接线状态。
- 诊断目标 Flash 前 147456 字节与当时四个本地 bin 不匹配；后续快照读回与该诊断
  镜像一致。使用本地 ELF 地址前必须重新核对目标镜像。
- 当时通过读回 Thumb 代码确认：反馈函数 `0x08012cf0`，电机上下文基址 `0x20009ad8`、
  步长 232、反馈时间偏移 16；CAN 管理器 `0x20000534/0x20000594`，uwTick `0x2000052c`，
  故障结构 `0x2000b524`。仅适用于已核实的历史镜像，不可盲用到新构建。
- 本次新 ELF 的 GDB 类型/符号重新核对：上下文、管理器、uwTick、故障结构地址及
  布局恰与旧镜像相同，但 yaw 配置已移到 `0x080208f8`。新 Flash136968字节与构建
  bin完全一致，SHA256=`36d34e433ba21475e883b0c73e37a541465466529366ffb0d1f73ccc0f16c526`。
  空 CAN2 ESR=0x00b00033（ACK错误/错误被动），输出仍锁定；未清除或绕过联锁。

### 下一步与官方资料

1. pitch三点测量及原有限位恢复代码已完成；下一步实车验证1971启动目标与1566～2205
   范围内跟随、制动和补偿。不能用目标裁剪替代实际机械不越界保证。yaw继续依据实际板上
   限幅采推杆目标和输出，核定速度内环出力及机械阻力，不沿用旧快照推断当前调参。
   yaw最新已在线，若反馈再次消失再检查供电/支路及原始CAN帧；此前间歇根因仍未确定。
   底盘需采推动摇杆行驶时的目标/实际RPM、电流、反馈时效，静止无错不能代替带载验证。

2. 查看实际 RoboMaster Assistant 的 GM6020“固件升级”页是否提供 1.0.6.2；
   仅在官方支持时用同一故障电机做版本 A/B，保持其他参数一致并验证能否回切复现。
   **尚未确认可降级**，未进行电机升级/降级或参数写入，不套用其他 DJI 产品教程。
3. 若助手无旧版本，向官方询问回退/返厂支持、硬件/BootLoad 限制及两版 CAN 差异。
   产品售后：`robomaster.support@dji.com` / `400-700-0303`；未代用户发送消息。

资料：本地 `docs/official-docs/RM_GM6020_Docs.pdf` 为 v1.4（2023.10）。
第 5 页有模式警告，第 6–7 页为协议，第 9 页只说明在线升级，未公开降级步骤或旧包。
[官方助手 v2.7 下载页](https://www.robomaster.com/zh-CN/products/components/detail/4643)、
[官方售后](https://www.robomaster.com/zh-CN/contact)已于 2026-09-09 核对。

## 架构与当前实现

依赖方向：`application -> core contracts/interfaces -> services/adapters -> modules -> bsp -> HAL`。
保留的 `runtime/rtos` 依赖 application、消息中心和 FreeRTOS，但当前未启动。

- 09-16 修复logger错误绘图：dashboard v8保留v7的288字节前缀，尾加12字节（能力位、
  yaw/pitch flags、四轮软件ID、云台enabled/startup_ready），载荷300/帧310，长度字段仍取低8位。
  接入遥控/IMU/底盘命令消息和电机服务快照；ChassisApp新增只读GetMotorIds，GetController声明
  为const，依赖方向不变。RPM仅接DJI；轮位改按软件ID显示，归一化底盘命令不冒充m/s。
  CAN灯表示配置电机100ms内有反馈而非bus-off/解锁；遥控200ms、其他源100ms超时。
  未接入/过期/未启用浮点填NaN，速度旁路不发布位置目标，yaw速度与内环实际反馈同坐标；
  旧整数模式/裁判/发射/视觉等在v8网页标N/A，未补底层未知协议。IMU新鲜仅指应用消息。
  Python与网页支持v8并保留v3～7兼容；日志附能力/轴flags/电机ID，连续yaw误差不折回±180度。
  修复末点无效导致前面线段漏画、NaN/250ms空窗跨接、调整窗口尺寸未重绘；断流1s显示STALE。
  桥接把真实WebSocket端口注入网页，兼容单端口/分端口/自定义端口，不再固定8765。
  真实C生产端→Python/Node执行网页解码、字段展示、Canvas调用回归通过，覆盖过期、NaN、
  速度旁路、单位/ID顺序、拆包/CRC、v7兼容、丢帧计数、日志列和端口；共14项Python测试通过。
  原有全套GCC主机回归通过，修补其dashboard依赖为独立桩，生产端另由真实C测试覆盖。
  步兵/哨兵Debug构建/ELF校验通过，FLASH150968/151792、RAM均57544B；步兵SHA256
  5ff7ad2c10268ebe2ca78e71c31a5ff4495b420bad67980d8a544d79aa697127。
  Python编译/差异检查通过，说明见docs/guides/logger-guide.md；验证日志/tmp/logger-final-*、
  /tmp/logger-host.log、/tmp/logger-ui-test.log。未烧录/连接硬件或做浏览器实景验证；需更新固件、
  重启logger刷新页面验证实车曲线，RTT耗时/丢帧仍待实测。用户外P1/内P120及全部控制参数保留，
  冻结底层无修改。此前“仅填云台/其他为零”的记录描述修复前v7。
  09-17 提交前审查：13个已跟踪文件修改、10个未跟踪文件（3个测试、7份logger日志），
  无暂存项；另含yaw外P10→1、内P300→120，speed_loop_only仍true，外P暂不参与控制。
  逐项差异及取数调用链未发现其他控制输出/模式/联锁/协议/底层改动；GetSnapshot/GetConfig
  不初始化或命令电机，新增订阅未超过每主题8槽，电机ID复制数量受4元素上限约束。
  原有20ms遥测调用间隔未变，RTT仍满即丢帧；新增采集/打包耗时及中断延迟增量未上板量测。
  全套GCC主机回归、14项Python/Node RTT测试、双车型Debug构建/ELF校验及差异检查通过；
  步兵增量构建无待编译项，FLASH150968/151792、RAM均57544B，日志/tmp/rm-precommit-*-20260917.log。
  本轮只补记忆，未改实现/参数、提交/连接硬件/烧录；编辑器未保存缓冲区及板上版本未核验。
  日志管理：.gitignore新增/monitor/logger_*.txt，自动采集默认仅本地保留；7份未跟踪日志
  已被忽略，既有2份已跟踪历史日志不受规则影响、未移除。忽略匹配及差异检查通过，未改控制代码。
  用户仍见logger列表后复核：git status及暂存/未暂存diff均无采集日志，7份新日志规则匹配；
  2份历史日志已跟踪但无修改。界面可能未刷新或显示会话差异，未直接查看UI，不能确认其具体原因。

- 09-16 解释RTT电机映射：role与总线/RX地址由infantry_standard.c指定，云台初始化按
  MotorService_FindByRole找到yaw软件5/CAN1 RX209、pitch软件8/CAN2 RX208；快照经
  Dashboard_Step写固定字段，网页按协议字段显示，不自动发现电机数量/安装位置。
  当前Dashboard_Step仅填双轴位置/速度/编码及部分状态，底盘motor_rpm/target和发射等
  字段仍为帧零初始化值，不能把页面预设标签/0当作实测。底盘物理轮位以wheels坐标映射
  左前2/右前1/左后3/右后4为准，电机条目旧轮位注释不一致；本轮只解释未改实现。
  源码数据流及文档差异检查通过，无构建/连接/烧录；未核对用户最新板上镜像。

- 09-16 修复`just logger`依赖与目标支持：当前仓库已有scripts/dashboard网页/终端入口、
  scripts/rtt_common传输及CMake RTT/dashboard源登记，下段09-15检索只代表当时版本。
  原根requirements.in未包含scripts/requirement.txt，本机.venv缺websockets/pyocd/rich；
  现根清单引用该子清单并重新解析requirements.txt，原有包版本保留。桥接用新版asyncio
  serve/process_request接口，按官方迁移说明将websockets下限12→14；安装到本机.venv的
  websockets17.1、pyocd0.45.1、rich15.0.0及传递依赖已完成，常规安装覆盖仪表盘。
  启动仍沿用PATH中的python3及sys.executable，新终端先source tools/activate.sh；使用说明
  已加入logger-guide；两入口--help、模块导入及uv pip check（41包）通过。
  后续安装Keil.STM32F4xx_DFP3.1.1；原stm32f407ig短名也在包中，直接原因是缺包。
  pyOCD入口按.ioc的STM32F407IGH6TR明确选stm32f407ighx，J-Link保留原名；缺包提示安装命令，
  auto未发现J-Link时不再用缺DLL错误覆盖pyOCD失败。normal映射attach、关闭auto_unlock及
  resume_on_disconnect，并禁用会直接写DHCSR的包DebugCoreStart，改用保留状态的通用实现；
  原已暂停则报错不恢复，显式halt/under-reset保留原先干预后恢复行为。RTT消费会写读指针。
  实际枚举ST-Link0669FF535548877187254949，附加成功但无RTT控制块；运行中纯读Flash146256B
  完全匹配构建前无_SEGGER_RTT/Dashboard符号的旧ELF，bin SHA256=52d23aeb513942d186d29a6b8d8e81e81f3db7dc671f43b6028a30a1d2c6af8c，
  读取前后CPU均RUNNING。当前源码虽有Dashboard_Step调用，CMake引用的SEGGER_RTT.c/h缺失。
  已从SEGGERMicro/RTT提交4d8feab3150f86f37a9d323ddc88d6cdf5673072补齐三份原样C/头文件、
  LICENSE及source.json哈希；保留旧Conf并设RTT_USE_ASM=0。两车型构建/ELF检查通过，
  FLASH148296/149144B、RAM57400B，步兵ELF777895d06934e0d7ea6f5c8b66f5e57bc987982d7badd4ba704eee874776f7b8，
  含Dashboard_Init/Step和_SEGGER_RTT；9项模拟传输测试、Python语法/差异检查通过。
  构建日志/tmp/rm-logger-{build,sentry-build}-20260916.log。无控制参数/冻结底层修改、
  暂停/复位/恢复/烧录；须用户烧录新固件后再验证实际RTT帧和网页曲线，当前仍是旧镜像。

- 09-15 用户按截图查RTT/RRT dashboard：当时工作区及本地已有main/group2/group3/group4/py
  分支引用未找到dashboard、截图四图标题或SEGGER_RTT实现；未刷新远端，不推断其他仓库情况。
  当前tools/gimbal_monitor.py与just monitor提供双轴目标/实际位置、速度、电流的终端显示/CSV，
  没有截图式实时曲线界面。script/deprecated/plot_yaw_data.py、plot_pitch.py是旧串口CSV+
  Matplotlib曲线，分别依赖YAW_CSV/PITCH_CSV输出，不能当作当前开箱即用的RTT工具；yaw相关
  输出已不在现行控制函数中，pitch打印仍注释。archive/湖南大学代码.md的“已有RTT”属旧参考
  描述，不能作为当前工程已接入的证据，现行logger-guide明确无RTT通道。源码/本地引用检索及
  文档差异检查完成；未修改实现、构建/烧录、启动工具或连接硬件。

- 09-11 新增[yaw/pitch实时监控](../gimbal-monitor.md)：上层云台命令回调结束发布
  `g_gimbal_monitor`，v1固定148 B，含双轴实际电流/位置/RPM、目标位置/内环RPM、
  含前馈与重力且经应用限幅的服务命令请求及返回状态、回调计数/间隔和各轴反馈时间。
  单写者偶/奇序号及编译屏障供SWD读取前后校验；禁用/连续性失败分支也发布，
  不把PID_Reset保留的目标当有效值。yaw速度调试时位置仅备忘，spin另标IMU反馈来源。
  新服务接口`MotorService_GetCommandUnit`只读查询原始命令单位，未知协议/模式返回UNKNOWN；
  步兵pitch保持电压模式，目标电流N/A并单列电压刻度，不换算A/V或修改电调设置。
  请求成功不代表CAN已发送或电调已执行。原控制路径/PID/车型参数及冻结底层均保留。
  电脑端`tools/gimbal_monitor.py`/`just monitor [Hz]`默认20Hz终端刷新、CSV/元数据/log存
  `build/monitor/`，按当前ELF的nm符号定位，Flash纯dump读回与ELF生成bin主机比对；
  禁止目标算法/工作RAM、关闭examine-end及GDB端口，无halt/reset/resume/烧录/应用写入。
  版本/镜像不符或读取错误退出；反馈过期、MCU时钟或回调停止由反馈时间和电脑计时提示。
  这是控制回调最新快照，不是逐CAN帧记录；未采回调数不代表总线丢帧，双轴反馈时刻可不同。
  两车型just build/ELF检查通过，FLASH146256/147112 B、RAM均54152 B；GCC全套主机回归
  及50项Python工具测试通过，覆盖快照布局、启停/失联/服务失败、命令单位、序号撕裂、
  有符号数/跨圈/时钟回绕、RPC分段/失败及Flash只读匹配/拒绝。prepare-only/just入口验证
  无硬件操作，差异检查通过。日志/tmp/rm-gimbal-monitor-{host,python,build,sentry-build}.log。
  未烧录/连接实机；须先烧录新固件后监控，回调耗时增量和SWD实测采样性能仍待验证。

- 09-11 按用户“2006持续高电流反转一次”接入拨弹角色堵转恢复，见
  [拨弹退弹说明](../feed-stall-recovery.md)。ShooterController新增独占FeedRecovery状态，
  仅feed_enabled且三电机反馈/CAN联锁通过时检测电流反馈绝对值；初调阈值800原始刻度、
  持续500ms，反向目标−500转子RPM持续200ms，到时本轮零电流、下轮从零按原斜坡恢复1000RPM。
  同一次拨弹最多反转一次；重堵再持续500ms则拨盘零电流锁定，取消拨弹清除次数与锁定。
  失联/总线锁定撤销反转但保留已用次数及重堵锁定，防止恢复补发；摩擦轮原斜坡/零速闭环保留。
  换向/恢复清拨盘PID并用当前RPM初始化微分；低电流或控制间隔>100ms打断连续计时，
  uint32毫秒回绕受支持。检测/截止依赖控制回调，非独立硬件保护；只看高电流，不要求低速。
  电流阈值已按用户要求改为shooter_controller.c中显式变量
  int16_t g_feed_stall_current_threshold=800，头文件extern声明；范围1..32767原始刻度，
  主循环取消拨弹后可赋值，运行时修改不持久保存；非正值锁定拨盘，修正并取消拨弹才解锁。
  其余三个时间/速度FEED_*宏保留；800基于现有1000RPM/Kp1静止输出约1000选取，
  非额定值或实测阈值；反转是限时目标，不是转一圈或已成功脱困。未新增堵转声光报码。
  C610官方V1.0手册第8页已核对：控制指令±10000对应转矩电流±10A，因此指令800=0.8A。
  第9页反馈DATA4/5只标“实际输出转矩”，没有明确安培换算比例；本检测用反馈值，
  不能仅凭指令比例断言阈值800就是实测0.8A，反馈标度仍待确认。本次仅查官方资料/算术、
  更新记忆并检查文档差异，未修改控制参数或操作硬件。
  用户称M2006，当前拨弹CAN2硬件ID3/软件9仍配置M3508；已询问映射，尚未回复。
  本次按现有拨弹角色实施，不擅改型号/CAN/PID，实物型号及正常/卡弹电流、退弹量待确认。
  完整GCC主机回归通过，覆盖阈值/短脉冲/正负极值、反转截止/恢复/重堵锁定/取消、
  三台逐一失联/CAN锁定及恢复次数保留、不规则/重复时间/回绕和独立计算入口；
  阈值变量追加1600/400自定义正负反馈、0/负值停机测试通过。最新步兵just build/ELF检查通过，
  FLASH144984 B、RAM53992 B，无未解析符号、未链接RTOS启动；日志/tmp/rm-feed-threshold-{host,build}.log。
  已基于另窗口最新pitch重力零点1971版本工作；重力控制/配置/车型测试文件前后SHA256一致，
  共享control_recovery测试中的重力零点回归保留且通过，未覆盖该窗口改动。
  差异检查通过，冻结底层及其他控制参数未改；未烧录/连接硬件，阈值和机械效果尚未实测。

- 09-11 按用户要求补充发射失联联锁和电机声光报警，见[功能说明](../motor-offline-alarm.md)。
  发射任一电机缺首帧/反馈年龄>100ms、配置缺少三台角色或CAN未armed，三台一起零命令，
  清目标/斜坡/输出和全部应用PID；Update及Compute两个入口检查，恢复全反馈后按当前命令
  从零以500RPM/命令爬升。普通下档斜坡及零速闭环保留。新增feedback_seen[3]/feedback_fault；
  ShooterApp_GetController声明为只读指针。全部源改动仍未烧录。
  新增application/diagnostics/motor_offline_alarm及bsp/alarm：全配置电机快照缺失/无效/>100ms
  计入软件ID位图；蜂鸣CAN1一次/CAN2两次，红灯取MotorConfig_t.offline_alarm_id已知硬件ID。
  两车型显式填写标签，0或非法标签不猜协议、用常红组及unmapped_mask暴露。
  红灯100ms亮/100ms灭，每组max(1000,200*m)ms；组后蓝灯400ms静音，多故障按配置轮询。
  蜂鸣每组开头n次各100ms、中隔100ms；组总长可调，不保证总体每秒n次。ID含义暂按硬件ID，
  用户已允许降低频率并要求蓝灯分隔，尚未单独回复硬件ID/软件ID选择。
  全恢复立即静音亮绿；当前节点恢复/循环卡顿>=100ms用蓝灯结束残组，不压缩补发。
  绿灯仅代表电机反馈在线；遥控单独失联、yaw20ms连续性异常仍不属于此100ms离线报警。
  BSP复用PH12/11/10与原Buzzer_Start/Stop非阻塞PWM，只在声音状态变化时启停；旧冻结头文件
  缺Start/Stop声明，由BSP本地按已有实现声明，不改底层。报警Task使用派发后的新鲜毫秒时间。
  报警主循环接入已完成：用户仅为本次灯语允许修改Src/main.c，实际只增加一个头文件引用，
  并把主循环恒绿调用换成MotorOfflineAlarm_Task(HAL_GetTick())；启动阶段灯音流程未变。
  [两处最小补丁](motor-offline-main-hook.patch)已应用，源码与该补丁严格相符；本次授权结束，
  Src/main.c及其他底层继续冻结。两车型链接图均确认main→报警Task→BspAlarm_Set已进入最终ELF。
  上轮全套GCC主机回归通过：逐台首帧/离线/恢复/零输出/清PID/从零爬升、CAN联锁、时间回绕；
  1..16红灯计数/两总线蜂鸣/蓝灯完整分隔/多故障轮询/恢复/非法配置/卡顿及BSP引脚/PWM边沿。
  本轮仅灯语接入与文档，没有修改这些模块；重跑两车型just build与ELF检查通过，无新增警告。
  FLASH144352/145208 B、RAM均53976 B；步兵SHA256=
  7d51a853054a906aaed337030b107a397815230d41992b7f2e5e9b37b677fefc；哨兵SHA256=
  b44cf0d0b97f521a314ddb122f266de4b128472eb1f7963b019310148cbdb6a1。
  主机日志/tmp/rm-offline-host.log；最新构建/tmp/rm-offline-hook-{infantry,sentry}-build.log。
  差异检查通过，冻结目录中只有上述获准main两处变化。未烧录/复位/断线实测；实际声光节奏及
  逐台失联联锁仍须上板核对，编译产物不代表已在板上运行。

- 当前[故障核查](../error-handling-audit.md)已同步发射新策略。遥控daemon约100～110ms清上游
  缓存/重试DMA但不发清零消息；Cmd完整帧过期>200ms禁用命令。遥控失联不撤销CAN armed，
  重连底盘可直接响应，云台等100ms重对齐，发射需右拨杆重新下档；电机恢复无统一回中确认。
  单底盘反馈缺失/>100ms停四轮；yaw运行连续性/>20ms停双轴，中档方位过期还停底盘；
  pitch>100ms停双轴。仅Bus-Off锁全车/abort两路邮箱，两CAN健康500ms后再双拨杆下/五轴±3内
  500ms解锁，EWGF/EPVF单独不撤销已解锁状态。RC/CAN/CHA日志默认关闭；遥控warning只计数。
  Error_Handler关IRQ死循环、HardFault等死循环，无主动归零/自动复位；未启用独立硬件watchdog，
  软件超时依赖主循环/派发继续工作。未到速禁止拨弹、云台失联禁止发射仍未实现。

- MCU 为 STM32F407；车型为 `infantry_standard`、`sentry_swerve`。main 裸机循环派发
  消息，未调用 `RobotRtos_Start` / `AppRuntime_Step`；SVC/PendSV 为空，SysTick 只走 HAL。
  消息中心不是调度器，电机发送由 after-dispatch hook 集中刷新。
- 应用层不拼 CAN 帧、不访问 HAL、不判断厂商协议；共享载荷放 `core/contracts/`，
  运动学放 chassis strategy，协议放 motor adapter/codec，BSP 只处理板卡 I/O。
- 模式策略集中在 `application/cmd/command_router.c`；可选应用登记在
  `application/runtime/app_manifest.c`，通过标准主题、服务和 BSP 接口扩展。
  运行时控制模式用 `MotorConfig_t.control_mode` / `MotorService_SetControlMode()`，
  与电调内部电流环开关不同。
- 遥控采用 nyush USART/DBUS/daemon 与快照桥接：USART3 ReceiveToIdle DMA、18 字节、
  100K/9B+EVEN，保留 WT61C 分支；无新帧 200 ms 禁用输出，小陀螺 yaw 按真实时间积分。
- 云台两轴等待真实反馈后重置 PID；yaw锁存实际角，pitch使用非负initial_angle，
  负数则锁存实际角。步兵当前pitch固定1971，启动和恢复对齐均检查1566～2205范围。
  CAN 故障恢复后需回中解锁，过期反馈使输出归零并重新对齐。
- 步兵全向轮实物参数：用户最新确认左前2、右前1、左后3、右后4，几何行序应为
  左前/右前/右后/左后=2/1/4/3；此前3/2/1/4已被纠正。X 形 ±45°、轴距/轮距 0.54 m、轮半径 0.07 m、
  M3508 P19 减速比 3591/187；方向和速度环仍需实车验证。
- DJI 三类电机已用于现有车型；DM MIT、本末 BM1505B、瓴控广播电流已有编解码和
  适配器，但未接入现有车型或实车验证。未知型号仍需范围、ID、波特率等参数；
  同总线地址/槽位冲突应拒绝初始化。瓴控广播须先用厂商工具开启。
- 麦轮策略保留、哨兵使用旧双舵方案；通用舵轮几何未知。旧 USB/Seasky 视觉仍在用，
  Jetson 新视觉仅预留接口。云台/哨兵转向依赖 DJI 旧上下文，不能仅换 vendor 替换电机。

## 底盘行驶颤动与参考PID

- 09-11 用户提出yaw速度指令叠加底盘转速抵消，随后明确只评估、不改代码。
  平面/同轴且统一正方向时：云台世界角速度=底盘角速度+yaw相对角速度，故相对目标=
  遥控期望世界角速度-底盘实测角速度；换成电机RPM前须核定安装符号。4555用于相对朝向
  和平移坐标转换，本身不能分离底盘运动与主动yaw运动；底盘指令不能当作实测速度。
  当前c_gz来自WT61C.gz由度/秒转rad/s，可作为候选，但安装/更新率/延迟未验证；其
  last_update_ms由多类帧共同刷新，SensorData未携带角速度独立时效，不能据此保证gz新鲜。
  评价：底盘速度补偿+电机速度闭环可抵消旋转，能沿用内环，但仅速度控制不追回积累航向误差；
  云台IMU航向/角速度直接闭环更适合稳瞄，仍需零偏、滤波、时效与安装方向验证。
  若融合，保留电机速度内环时可用云台航向外环修正世界速度目标，再减底盘实测速度；
  若速度内环已改用云台陀螺仪世界角速度反馈，不能照搬再减底盘速度，避免重复补偿。
  数学/源码与MathWorks前馈反馈资料核对完成；没有实车A/B比较，不宣称当前IMU方案已稳定。
  当前speed_loop_only对spin强制0的分支尚需单独适配，不能只填现有电流前馈参数完成。
  本轮仅更新记忆并检查差异，未修改代码/参数、构建、烧录或操作硬件。

- 09-11 当前源码模式复核：用户确认此前在左拨杆下档测试跟随。下档直接车体坐标，
  仅中档使用yaw实际编码减4555旋转左杆平移；上档spin仍走旧IMU差值与轴交换。
  因此已有中档跟随，但不满足所有档位始终以云台为前向。当前磁盘yaw_ccw_sign=-1，
  对应编码减小为左转；配置旁注/旧说明仍写+1语义，安装符号仍待物理确认，未擅改方向。
  Cmd→电机服务→DJI angle_raw快照→Router旋转→全向轮运动学/轮电机输出链已核对。
  完整GCC主机测试通过，覆盖全圈每5°解算及回推、±90°/跨零、模式/反馈失效；
  日志/tmp/rm-follow-audit-host.log。未连接板卡，不能把源码判断当作板上模式或镜像确认。
- 同轮检查小陀螺yaw：上档spin首次锁存BMI088/QEKF的yaw_total_angle，左右杆-ch0/660
  按120度/秒乘dt累加spin_hold_yaw_deg，回中冻结；底盘wz固定0.33，当前1.20rad/s配置
  对应0.396rad/s目标（轮速共同限幅之前）。完整yaw模式用航向误差作外环、
  -g_gz*30/pi作速度反馈，转入spin仅选一次误差圈数；视觉有效时优先于spin。
  但当前步兵speed_loop_only=true会把spin/vision降为手动且rate强制0，随后用电机RPM
  闭合0RPM目标；遥控虽改变航向备忘目标但不进入控制，不能实现世界航向保持/左右调整。
  4555仅是电机相对底盘前向参考，不提供世界航向。IMU必须实际随云台运动且轴向/极性匹配，
  本轮未确认安装；BMI088航向为惯性参考而非绝对地理航向。g_gz目前直接取未减零偏原始值，
  QEKF航向用减零偏值，spin仅检查有限值、未独立检查IMU时效，恢复完整模式前须核定。
  现有主机回归覆盖spin杆量随dt积分、速度调试屏蔽spin、完整模式航向超过半圈保持误差。
  此次只核对源码及测试、更新记忆，未改控制逻辑/PID/模式开关、构建ARM、烧录或驱动实机。

- 09-11 已补充步兵中档云台方向平移跟随，见[使用说明](../chassis-follow.md)。
  新增`ChassisFollowConfig`及`RobotConfig_t.chassis_follow`，步兵前向编码4555；
  `yaw_ccw_sign=+1`暂按编码增大为俯视左转，用户明确尚未确认，必须实车核定正负。
  Cmd按yaw角色通过MotorService读取DJI GM6020的实际单圈angle_raw，换算相对底盘角，
  Router中档以R(theta)旋转左杆平移向量，去掉该分支的IMU差值与额外-90°轴交换。
  沿用当前用户映射vx=-ch3/660、vy=+ch2/660及wz=ch4/660；无自动转车头对齐闭环。
  下档、上档spin、哨兵未标定分支保持原路由，不把步兵4555套到其他车型。
  CommandRouterInput增加编码跟随选择、方位有效位、相对角度及实际反馈时间。
  标定/角色/协议/反馈非法或超过20ms时禁用整条中档底盘命令，禁止坐标系回退；新反馈可恢复。
  未改轮序2/1/4/3、几何、方向、速度/PID参数，yaw仍speed_loop_only=true。
  完整GCC主机回归通过，含真实Cmd消息链4555/±90°/180°/8191→0/反向配置、过期/非法停机；
  Router与实际步兵运动学组合每5°遍历全圈并由四轮速度反算，平移方向和幅值均符合云台角。
  `just build`及`just build sentry_swerve`和ELF检查通过，FLASH141856/142744 B、RAM均53944 B；
  步兵ELF SHA256=0ef3d8173871a6aeb77faadc7ad0bdb8fc57992736a15284f5c589a3aa44823c。
  日志`/tmp/rm-follow-{host,infantry-build,sentry-build}.log`；差异检查通过、冻结底层未修改。
  未烧录/复位/驱动实机，轮子安装符号及推杆实际正负仍需实车核定；不能宣称物理方向已验证。
  同轮复核yaw源码：8190→2展开为+4，位置误差不逐轮wrap，相关主机回归通过；外环仍旁路，
  未核对当前板上固件是否含此前修复，不把源码修复等同实车问题已消失。
- 遥控映射查询仅提供自行修改说明：command_router.c的route_chassis用ch[3]前后、
  ch[2]左右，归一化vx/vy均取负；桥接明确ch[2]来自左水平。普通下档直接赋vx/vy，
  中档及spin在云台坐标变换后另赋vx=chassis_vy、vy=-chassis_vx，即额外-90°旋转。
  若目标为对齐时左杆左推对应左移，应区分横移正负反向与轴交换；后者核对两处额外
  旋转及实际航向零位。未采推杆原始正负或实物方向，不宣称简单翻符号必然修好。
  只读源码、更新记忆并检查文档差异，未改控制代码、构建或烧录。
- 用户要求自行修改底盘方向/运动学，当前任务仅解释，不代改或烧录。最新磁盘direction
  已为ID1/2/3/4=+1/+1/-1/-1（用户修改；此前为-1/+1/+1/-1），实物是否正确未验证。
  运动学核心omni_compute的lever=x*drive_y-y*drive_x，旋转轮速=lever*wz/r*减速比*60/(2π)。
  坐标+x前、+y左、+wz俯视逆时针；drive为轮子主动滚动方向，不是滚子轴方向。
  当前几何轮序3/2/1/4对应lever=-/+/+/-，大小约0.38184m；乘最新direction后正wz下
  四轮转子目标符号均正，这本身不证明实物错误。修正geometry的位置/ID映射要依据实物，
  drive向量和direction会同时影响平移；不能任意只翻转两轮旋转项。源码/数值核对与
  文档差异检查通过，未连硬件、改控制代码、构建或烧录。随后用户确认左前2、右前1、
  左后3、右后4，已说明自行将几何行ID改为2/1/4/3；未代改，尚未核验其保存/烧录结果。
- 小陀螺/转向定义在command_router.c：rc.s[1]上档spin，中档只切换云台坐标系平移
  （名为follow，未闭环自动转底盘对齐yaw），下档普通手动；spin固定wz=0.33，普通
  转向wz=deadband(ch[4])/660。最新磁盘max_rotation_radps=1.20，spin目标0.396rad/s，
  普通满旋转输入±1.20rad/s（均在运动学轮速缩放前）。yaw输入=-ch[0]/660；spin时
  以120°/s满杆修改绝对航向目标，gimbal_controller通过IMU角度/角速度保持航向，
  有效视觉目标优先于spin hold。本轮只定位，不改模式规则或参数。
- 历史底盘限速查询：`g_omni_infantry.max_translation_mps=0.30`控制满杆平移速度，
  `max_rotation_radps=0.60`控制满旋转输入；小陀螺固定输入0.33，当前目标0.198rad/s。
  运动学投影后还按最小轮速上限整体缩放，目前min(CHASSIS_DEMO_TARGET_SPEED=8050,
  四轮speed_limit=10000)=8050转子RPM。提高平移目标应改max_translation_mps；例如
  0.30→0.60仅为两倍目标的说明示例，本轮未修改或烧录。PID输出15000是电流命令限幅。
  本轮源码核对、文档差异检查通过；实际高速跟随和行驶颤动仍未验证。
- 最新输出查询`chassis-output`只读30次、5554ms，Flash匹配4ff20979；控制器轮序
  3/2/1/4，running=0、底盘三轴命令与四轮目标/输出全0，遥控在线/总使能1、CAN1 ESR=0。
  实际RPM为ID3 -3..0，其余0；原始反馈电流ID1 -32..32、ID2 -43..41、ID3 -67..70、
  ID4 -254..150。本窗口未捕获行驶，不能用零输出代表推动摇杆时输出。
  当前速度单环Kp15，稳态近似命令=15×RPM误差，限±15000。C620官方本地手册第32页
  确认±16384对应±20A，故±15000约±18.31A，属于转矩电流指令而非电源总电流。
  本轮只读硬件与文档更新，未改参数/烧录/主动驱动；差异检查通过，Tcl/log/JSON/Flash已归档。
- 当前用户磁盘配置且随本次修复烧录：四轮速度环Kp=15、Ki=Kd=0，
  输出限15000、积分限7500；四轮pid_inner全零，实际启用速度单环。
  下述90/0/0与电流0.5/0/0为此前参考配置，当前未使用，本次不改用户底盘调参。
- 用户询问环启用方式：`ChassisController_Init`始终由pid_outer初始化速度环，反馈是
  电机转子RPM，输出为原始电流刻度。pid_inner.output_max>0且参数/应用模式校验通过
  才启用软件电流内环，以速度环输出为目标、CAN原始电流为反馈；output_max=0直接
  发送速度环输出。此开关不写电调模式，修改配置需构建/烧录/重新初始化后生效。

- 用户明确颤动发生在推动摇杆行驶时。最新采样只有静止数据，不能认定CAN或PID为根因。
  已装镜像四轮旧速度PID：ID1/4为4/0/0.1，ID2/3为4/0/0，速度单环；D项与配置不一致
  是排查点，尚无行驶波形证明其造成颤动。
- 按用户要求采用 `NYUSH-Robotics-Club/nyush-rm-control` main/infantry提交2b7ca720：
  速度15/0/0（度每秒）与电流0.5/0/0串级。本库转子RPM需速度增益乘6，四轮统一90/0/0，
  输出限12000、积分限3000；电流内环0.5/0/0，输出限15000、积分限3000，均为原始电流刻度。
  参考出处为`application/robot_configs/robot_infantry.h:120`及`application/chassis/chassis.c:70`；
  参考驱动反馈RPM乘6，电流使用原始反馈，未复制源码或移植其功率管理/反馈滤波。
- `ChassisController`新增拥有的每轮current_pids/current_loop_enabled，`pid_inner.output_max>0`
  启用应用电流内环，0保留原速度单环；非法内环配置停机，禁用/反馈过期重置双环。
  保留轮序3/2/1/4、安装方向、几何和四轮失联联锁；PID参数相同不保证两库实物响应相同。
  配置结构与控制器RAM布局已变化，烧录后必须重新用新ELF核对诊断地址。

## 构建、烧录与验证状态

- 09-18 just logger网页无数据：旧PID14675的HTTP8080/WS8765正常，174252日志
  持续追加v9，seq97841/末帧年龄14ms；服务端无断流，客户端实际故障尚未直接观察。
  用户要求解决后，tools/firmware.py默认改为HTTP/WS共用8080；桥接同监听时注入
  wsPort=null，使网页沿用浏览器实际端口，兼容SSH本地改号；显式--ws-port仍支持双端口。
  36项Python/Node/传输/入口回归通过，覆盖转发改号、HTTPS及双端口兼容；无需ARM构建。
  已正常SIGINT退出旧logger，并以normal/attach模式重启，桥接PID17548仅监听8080。
  实际HTTP200、同端口WS成功解码v9连续seq105900～105902，新记录
  monitor/logger_20260918_174839_860969.txt持续采集约18.4KB/s。用户刷新现有网页即可
  使用同源连接；客户端显示仍待确认。测试/运行日志/tmp/rtt-single-port-{tests,live}-20260918.log。
  未改控制参数/协议/冻结底层或烧录/暂停/复位MCU；RTT重连会消费读指针，文档差异检查通过。
  随后用户要求释放busy探针：fuser确认ST-Link /dev/bus/usb/001/004仅PID17548持有，
  已SIGINT正常停止该logger；复查USB无持有进程，8080/8765/3333无监听。未重开logger，
  网页采集现已停止；本轮无源码/接口变更、烧录或MCU复位，进程/占用及文档差异检查通过。

- 09-17 21:35:43再次flash失败复核：日志仍在目标识别前空FIRMWARE_ERROR退出，
  无写入/校验成功标记；激活环境后just入口及步兵build/ELF检查通过，无待编译项，
  ELF SHA256=1a29af5ec39fca0575d1740d931d554e95d047de67903340f3bb68d5f2f6a29d。
  fuser再次确认同一logger PID40967持有ST-Link /dev/bus/usb/001/004；检查身份后
  已发送SIGINT正常退出，随后fuser无占用、8080/8765监听解除，网页实时数据暂停。
  just flash-plan通过、hardware_commands_executed=0。用户可直接重试flash，成功后
  再开logger；空日志未提供底层错误码，释放后实际烧录尚未验证。未复位/烧录MCU
  或修改控制参数/接口；本机构建、入口计划及文档差异检查通过。

- 09-17网页访问排查：用户在自己电脑浏览器访问，经VS Code SSH连接Ubuntu。
  宿主只读核查logger PID40967监听127.0.0.1:8080/8765，HTTP返回200，WebSocket
  收到438字节二进制消息；沙箱内curl失败/ss权限不足不能当作宿主服务故障。
  指引在VS Code转发8080网页与8765数据端口，并使用实际本地转发地址；用户端
  转发/浏览器恢复尚未验证。未重启logger、创建新探针连接或更改控制/接口/固件。

- 09-17 just入口排查：未激活终端PATH缺已安装just；source tools/activate.sh后
  just 1.58.0及默认帮助正常。lg/lc配方原写just logger/just logger-cli，但本项目
  shell是firmware.py --just，复现invalid choice: 'just'；现改原生alias lg/lc。
  just --list、两别名--help及带参dry-run通过，文档/justfile差异检查通过；未连接
  硬件或烧录，无控制接口/参数修改。用户终端仍需自行source，实际RTT连接未测试。

- 09-17 19:35:52烧录失败核查：openocd-flash.log只有空FIRMWARE_ERROR，尚无
  FIRMWARE_TARGET/写入/校验标记。当前just build通过且无待编译项，ELF仍475a9fa6，
  FLASH152272/RAM57696B，不能归因PID语法或容量。宿主机just logger→桥接PID3372
  持有ST-Link0669FF535548877187254949的/dev/bus/usb/001/004（fd13），权限0666可读写；
  探针占用是当前明确需排除的连接阻断，空错误未给出底层USB错误码，未做释放后A/B重试。
  指引logger终端Ctrl+C退出释放探针，再just flash，成功后重开just logger；关闭网页不退出桥接。
  本轮未终止用户logger、连接MCU、复位/擦写/烧录或修改控制代码/接口。
  证据build/diagnostics/flash-blocked-20260917/；日志阶段断言、构建/ELF及文档差异检查通过。

- 09-13 日常烧录流程复核：保存源码后`source tools/activate.sh`、`just flash`即可；
  flash内部先构建/检查ELF并核对哈希，再选探针、检查芯片、复位暂停、写入/校验及按配置运行，
  不要求先单独build或提交Git。`just flash-plan`通过，当前步兵Debug、run_after=true、
  hardware_commands_executed=0；本轮未构建/烧录或连接硬件，无控制代码/接口修改。
  quickstart前部“现在能烧录”仍有默认暂停的旧描述，与当前配置及后部日常命令不符，待统一。
  文档差异检查通过；当前用户参数是否已烧录及运行效果未核验。

- 09-11 修复用户调参后的步兵编译失败：infantry_standard.c只留下开头/*，未在#include前闭合，
  一直吞到前向参考4555同行注释的*/，导致第7行.yaw_ccw_sign成为孤立语法、类型未知，
  文件末尾RobotConfig_t字段报错均为连带错误。恢复闭合的文件头及精简前馈/报码填写说明；
  同时恢复该文件丢失的九个offline_alarm_id标签，避免报警降为未映射常红。
  用户当前yaw内环50/0/0、output_max20000/integral_max2000原样保留（不是此前建议的500/0）；
  已逐字比较#include之后的正文，除九个报码字段外与修复前完全一致，未改其他PID/方向/几何。
  gcc语法检查复现原首错；just build及ELF检查、全套GCC主机回归通过，FLASH144352/RAM53976 B，
  ELF SHA256=2e9f6a843f4a82da5618da9aca25e5d756565e149acf31e149cd551ac053f6d3。
  原文件及原错误保存在build/diagnostics/config-build-repair-20260911/；成功日志
  /tmp/rm-config-repair-{build,host}.log，差异检查通过。未烧录/复位，冻结底层无本轮改动。

- 09-11 用户报告VS Code文件菜单“保存/另存为”同时灰色。已核对工作区、yaw源码及
  .vscode/settings.json均属nyu且可写，磁盘余约209GiB；工作区未配置只读/自动保存。
  尚不能访问其当前编辑器焦点、缓冲区或本地客户端设置；优先从资源管理器打开真实源码并聚焦
  编辑区，区分无活动文件、只读比较页与无未保存修改。未改权限/编辑器设置或控制代码，
  尚未确认具体UI根因及恢复；文档差异检查通过。
- 09-11 用户要求flash后自动reset：本机`.firmware.local.json`已设`run_after=true`，
  Python configure及just configure默认均改为yes；仅校验成功后由现有OpenOCD脚本执行
  `reset run`，显式no仍可用于调试暂停，失败不启动。成功提示显示复位运行/保持暂停状态，
  快速开始文档已同步。38项工具测试通过（含真实Tcl解释器模拟顺序及校验失败不运行），
  `just flash-plan`确认FW_RUN_AFTER=1、after_verified=reset run、hardware_commands_executed=0。
  文档/源码差异检查通过；本次未实际烧录或连接MCU。此前false记录仅描述历史烧录窗口。
- 历史关闭pitch限位及底盘双环版本：步兵/哨兵Debug构建与ELF检查通过，FLASH/RAM分别为
  137968/48760 B、138896/48760 B；09-10已读回确认步兵版本在板上。步兵ELF SHA256=
  `4d440c8a0d9e1bec8dc44585cd01c88ee61662e494ae2171ba26226b2b9ad3c4`。
  `CC=gcc sh tests/host/run_tests.sh`全部通过，覆盖pitch5398启动、跨零、重开限位和失联停机，
  底盘电流反馈正负作用、双级限幅、单轮失联归零/双环重置、禁用内环兼容及非法配置拒绝。
  源码差异检查通过，冻结底层未修改。测试断言已计入float转int16_t的一刻度截断误差。
- 工作入口：`source tools/activate.sh`，再用 `just build` / `just flash-plan` / `just flash`。
  默认 `infantry_standard`、Debug、ST-Link/SWD、唯一探针、`run_after=true`。
  `.firmware.local.json` 与 `.vscode/settings.json` 保存本机配置；详情见
  [快速开始](../quickstart.md)和[环境验证](../environment-validation.md)。
- OpenOCD 检查 MCU ID `0x413` / 1024 KiB，再 reset halt、写入、校验；默认在校验成功后
  reset run，显式 `run_after=false` 时保持暂停。失败不自动重试/运行，不解锁、不改保护位
  或探针固件，不假定 NRST 接线。`doctor` 不连接 MCU。
- 2026-09-09 已安装：just 1.58.0、CMake 4.4.3、Ninja 1.13.2、Arm 15.3.Rel1、
  xPack OpenOCD 0.12.0-7、Python 3.14.7；来源/摘要在本机配置及构建 manifest。
  OpenOCD 包内 banner 为开发快照；旧 Python 环境保留为 `.venv-python313-backup`。
- 历史零电流通信测试程序已烧录、校验并按当时用户要求 `FW_RUN_AFTER=1` 启动；
  成功日志为 `build/diagnostics/yaw-20260909/current-firmware-flash.log`（标准构建目录的
  openocd-flash.log留存的是此前USB打开失败，不是成功日志）。ELF SHA256=
  `f10f54c7bb03c08e7d38726b9ceb9814cdbca17dd6f21da2df8f0a2039b0f6f0`，CPU运行、
  CFSR/HFSR=0。本机默认run_after=false未改；冻结底层及其他电机配置未修改。
- 为完成烧录，修复 `tools/stlink_usb.py` 对 Linux 旧ST-Link/V2原始12字符序列号的
  识别，按OpenOCD同样规则转24位十六进制，保留异常/重复探针拒绝；本探针为
  `53FF6F067187485514522487`。普通用户USB打开失败发生在init阶段，未擦写；随后
  使用sudo和同一目标校验脚本成功烧录。新增5项描述符测试及原16项工具/6项OpenOCD
  测试通过；DJI发包与车型主机测试已同步电流测试配置且全部通过。
- 合并后的两车型 Debug ARM 构建与 ELF 检查通过，无未解析符号，RTOS 启动/调度器
  未链接；FLASH/RAM：步兵 136968/48384 B，哨兵 138360/48384 B。
  合并任务未烧录；这些产物不等于上方诊断镜像。构建日志在
  `/tmp/rm-py-merge-infantry.log`、`/tmp/rm-py-merge-sentry.log`。
- GCC 主机测试、Python 工具/环境及 OpenOCD 模拟检查通过；最近兼容性复查运行
  `CC=gcc sh tests/host/run_tests.sh` 全部通过，含 DJI 电压/电流/混合发包、限幅、
  错误配置拒绝与双车型配置。反馈解析是源码核对，未冒充两版电机实测。
- Linux ARM64 新流程有实机验证；Windows/macOS 新 OpenOCD 流程主要为实现/模拟验证。
  `/dev/ttyACM0` 曾仅见 ST-Link VCP，C 板 USB CDC 日志和 GDB 断点尚未实测。

## 历史入口与来源

- 2026-09-09 合并 `main 81b9922` 与 `origin/py 5a3315f`；原有未提交工具/环境改动
  保留，备份 stash 为 `b766c728`。用户仅为此次合并授权了遥控所需的 5 个冻结文件，
  现已恢复冻结。py 诊断历史可用 `git show 5a3315f:docs/project/PROJECT_MEMO.md` 查阅。
- 新增接口包括 `OmniChassisConfig`、`MotorDriver_GetCommandLimit`、BspCan 恢复接口、
  `MsgCenterDiagnostics`；第三方遥控源保持原样，CRC 包装补了 `stddef.h`。
- FreeRTOS V11.3.0：MIT，提交 `9b777ae5...`；HNUYueLuRM/basic_framework：MIT，
  `6813c72b...`，参考分层及 DM/瓴控协议；RM_Ecat：LGPL-2.1，`89a86d88...`，
  核对 BM1505B 协议，其 OpenOCD 配置另标 GPL-2.0-or-later 并保留许可。
  Dart `2048b42b...` 未找到根部许可证，未复制代码。

## 其余待办

- 09-25 Kp240最新yaw日志`logger_20260925_173608_140243.txt`显示回调3--5 ms、反馈年龄
  1--2 ms，目标+100°/s时实际-474..678°/s，P输出反复超过±2万并被±6000限幅；
  目标0窗口同样限幅。速度反转重复集中在raw6033--7994（约265--351°，尤以303--343°），
  与角度相关但不是8191/0零点跳变。相较Kp120的约±2000停住，Kp240将同一负载/反馈
  扰动放大为P-only饱和极限环。断电手转顺滑不能排除通电姿态负载、线缆/齿轮预紧或反馈/换相
  扰动，当前证据不能单独定责；建议Ki=0、P回120--180复测并保留原始编码/命令/电流。
  本轮无源码修改、构建、烧录；运行参数仅由该日志窗口确认。
  另核对确认`yaw_pid_pout`不是目标量：满杆目标仍为100°/s=16.67RPM，P项按RPM误差计算。
  例如目标100、实际678°/s=113RPM时，240×(16.67-113)≈-23120；目标-86.8、实际
  -750°/s=-125RPM时，Pout≈26527。大数由实际速度超调/反馈进入误差产生，随后限幅换向，
  并非摇杆目标突变。
  最新`logger_20260925_175323_947967.txt`仍为P240/I0；目标+100°/s时实际30--108°/s、
  中位66°/s，Pout约-320..2800且未饱和。P-only下输出由速度误差承担负载补偿，负载随
  姿态变化就会出现同指令不同转速；反馈RPM为整数，显示换算为每6°/s一级，进一步造成
  台阶状不均匀。Ki=0时没有积分消除持续负载误差，不能期待恒速精确跟踪。

- 09-25 用户要求从`NYUSH-Robotics-Club/nyush-rm-control`复用步兵遥控链路和电机PID；
  首次网页/API/raw访问受沙箱缓存和DNS限制而失败，期间未猜测外部配置；随后已通过受限Git
  读取到仓库并完成逐项差异。

- 随后通过受限网络读到该公开仓库HEAD `2b7ca720e856f1efda9f6de6259faea42654720d`。
  当前工程的遥控解码器已是同源`modules/remote`/`third_party/nyush_remote`链路，现有适配器
  使用USART3 DBUS；参考步兵默认USART1 VT03/VT13，但本板USART1由冻结底层配置给IMU，故
  保留DBUS链路，未冒险改UART或引入冲突。仅在`config/robots/infantry_standard.c`按参考
  步兵PID迁移当前结构可表达的底盘、拨弹、摩擦轮、yaw和pitch参数；保留当前CAN ID、方向、
  协议限流、speed_loop_only及机械限位等本机硬件配置。
  主机回归、infantry Debug ARM构建/ELF检查及差异检查通过；未烧录。
  遥控复核确认：DBUS解码器/daemon与参考逐字相同，但参考云台使用左摇杆、底盘使用右摇杆；
  当前command_router反过来使用右摇杆云台、左摇杆底盘，yaw额外取负。当前满杆yaw目标为
  100°/s（16.67RPM），pitch每回调增加60编码刻度；参考为yaw 0.003*raw、pitch 0.001*raw
  度/回调，底盘raw×20，死区20，而当前通用死区3。协议链路一致不等于输入映射和放大一致；
  本轮只记录差异，未改变映射。按参考RobotTask的5 ms周期折算，参考yaw满杆约396°/s，
  而当前yaw满杆为100°/s；参考pitch约132°/s，而当前pitch在最新约4 ms回调下约659°/s。
  参考是每周期角度增量，当前yaw是速度目标、pitch是每回调编码增量，单位不同，不能直接
  复用0.003/0.001数值。
  最新`logger_20260925_183154_649048.txt`运行P150/I135/D0，回调4 ms、反馈年龄1--2 ms；
  目标+100°/s实际中位54°/s，目标-100°/s中位-60°/s，Pout/Iout约1150/358和-1000/-761，
  最终命令约+1437/-1621，远未触及PID26000或协议12000限幅。跟速不足来自目标窗口较短、
  P+I电流尚不足及角度/方向负载和积分偏置，不是目标值不足或logger滞后。建议先单向满杆保持
  3--5 s；仍有稳态误差时Kp小步至180，再评估Ki或速度前馈，避免直接回到240或先放大限流。
  长时间最新日志`logger_20260925_183826_150370.txt`验证：+100°/s保持15.6 s时实际中位
  102°/s，-100°/s保持23.1 s时实际中位-90°/s；命令约+1232/-1332，未触及PID/协议限幅。
  正向平均已跟上，反向仍有约10%静差；-100段末Iout约-1901，切回0后仍见约-1439残留，
  说明方向负载和积分偏置是主要改进点。建议Kp先150→180小步验证，Ki保持135并在归零/换向
  时清理或渐退积分；可按稳态命令试60--80原始刻度/RPM的yaw前馈，勿提高限幅或直接回240。
  Kp200最新日志`logger_20260925_191906_832001.txt`的+100最长2.3 s、实际中位72°/s，
  -100最长4.6 s、实际中位-78°/s；相比Kp150长段+102/-90，短段不足以证明改善。此次未
  出现明显饱和或颤动，输出中位约+1343/-1315；目标0长段仍见约-1271积分残留。建议先用
  Kp200做10--20 s同条件长段，再决定Ki/前馈，不继续盲目提高Kp。
  最新Kp300日志`logger_20260925_193756_569297.txt`已完成长段验证：+100保持18.1 s时实际
  中位96°/s，-100保持27.6 s时中位-96°/s，标准差约14--16°/s；相较Kp200明显改善，
  且未出现命令/协议饱和或高频换向。目标0仍见约-1039积分/命令残留，建议暂时保留Kp300，
  优先处理归零/换向积分清理和角度相关波动，不再继续增大Kp。
  积分清理方案尚未实施：应在yaw速度目标计算后、PID_Calculate前检测零目标和带迟滞的方向
  反转；回中可硬清理并用当前反馈初始化last_measure，换向先保留约20%旧iout或按100--200
  ms渐退，同时清零iterm/误差历史。用正转10 s→回中5 s→反转10 s在易扰动角度和普通角度
  验证，观察iout、命令和RPM，不先继续上调Kp。

  最新`logger_20260925_202601_201462.txt`在积分清理后验证：运行Kp300/I135/D0，回调主要4--5 ms、
  反馈年龄1--2 ms；+100/-100°/s长段实际中位约+96/-96°/s，标准差约14--15°/s，命令约±1200--1300，
  无PID或协议限幅。目标0段Iout已接近0，清理生效；剩余速度波动主要受整数RPM反馈量化和角度/方向
  负载影响，部分慢响应来自目标本身的摇杆渐变。后续只建议保持Kp300并单独试60--80原始刻度/RPM
  的速度前馈，必要时再加8--12 ms短滤波，继续记录目标、实际速度和iout；本轮未改控制代码。
  前馈的具体试验入口是`config/robots/infantry_standard.c`中yaw电机`.feedforward`（当前约第169行）：
  保持bias为0，按velocity_gain=60→70→80原始刻度/RPM分三次测试，output_max先取1500；每次只改
  该字段并重采样，比较iout、最终命令、实际RPM均值/标准差及换向过冲，未授权时不直接修改。
  最新`logger_20260925_203508_876252.txt`已完成第一组60测试：+100/-100°/s实际中位约+102/-102°/s，
  标准差约13.7/14.7°/s；iout中位约+254/-268，较无前馈时大幅下降，最终命令约±1250且无限幅。
  前馈改善了稳态跟速和积分负担，但速度波动未明显降低；下一组只比较velocity_gain=70，观察均值、
  标准差和换向过冲后再决定是否继续到80。
  最新`logger_20260925_203931_734782.txt`已完成velocity_gain=70测试：+100/-100°/s实际中位约+102/-102°/s，
  标准差约13.6/13.9°/s；iout中位约+118/-104，较gain=60继续下降，命令约+1231/-1283且无限幅。
  回调约4 ms、反馈年龄约2 ms。gain=70作为当前基线优于60，但速度波动改善有限，不建议仅为降iout
  继续盲目加到80；下一步应验证换向冲击和角度相关负载，必要时再做短反馈滤波或分方向补偿。
  对gain=70日志按编码角度分箱发现明显角度相关负载：正转60--120°速度中位约84--87°/s且命令
  +1.9k--2.1k，150--180°升至约114°/s且命令约+0.7k；反转120--180°约-78到-90°/s且命令
  -1.8k到-2.5k，210--270°约-99°/s且命令约-1.3k。回调/反馈时序正常，后续优先校准分方向、
  分角度前馈，不把短滤波当作负载补偿。

  控制链路复核确认：遥控器经`command_router.c::route_gimbal()`发布`GimbalCmd.yaw_rate`；
  只有`speed_loop_only=false`时，gimbal控制器才通过`YawReference_Advance()`将其变成连续角度
  目标并运行位置外环。当前infantry配置为true，且控制器会将VISION/SPIN降级为MANUAL，因此自瞄
  的现有`yaw_error_rad`（相对角误差）不会真正驱动yaw位置环。若改为显式角度消息，需同步修改
  `core/contracts/control_messages.h`、router、gimbal controller、vision bridge和相关host测试；
  角度坐标、圈数展开和失联回退必须由controller负责，不能直接复用裸RC值。
  自瞄当前路径已确认：legacy vision 的`yaw`原样成为`yaw_error_rad`，经router写入
  `GimbalCmd.vision_yaw_err_rad`，20 ms无新数据后清除有效标志；但`speed_loop_only=true`会在
  gimbal控制器入口把VISION降级为MANUAL并把速率置零。因此若要让遥控器与自瞄完全共用角度环，
  至少要恢复位置环并保留vision误差语义；若改成显式角度目标，还需同步扩展控制消息、router、
  vision bridge、controller及host测试。

  当前配置文件已改为`speed_loop_only=false`，但最新已有logger仍为改动前版本，`yaw_flags=15`不含
  `GIMBAL_MONITOR_POSITION_ACTIVE`位16；需重新构建/烧录并采集新日志，不能仅凭源码配置确认运行时已接入。
  新日志`logger_20260925_210054_876185.txt`已确认位置环接入（`yaw_flags=31`），但外环`Kp=28/Kd=1.3`
  过大：目标速度频繁达到±180°/s限幅，实际速度约-342..+396°/s，命令触及±12000，角度误差最大约
  46.5°。因1°约22.76编码器ticks，Kp28使极小位置误差就饱和；应先外环D=0、Kp降至约0.3--0.7，
  target_lead_deg降至3--5°并用10--15 RPM低速验证，再逐步恢复速度，暂不调整已验证的内环Kp/Ki。
  最新`logger_20260925_210909_714462.txt`已验证外环降至Kp0.3、Kd0后仍会饱和：目标速度多次贴±90°/s
  （15 RPM），实际速度约-162..+174°/s，命令峰值约±12000，运动时角度误差约±5°。Kp0.3在5°误差
  下仍要求约34 RPM，故晃动来自外环限速后的反复制动；目标速度接近0仅1个样本，尚不能判断静态锁定。
  下一步先试Kp0.05--0.1并做回中5 s锁定测试，若要保持100°/s手动跟随需增加目标速度前馈，不继续加Kp。

  最新`logger_20260925_211421_412484.txt`验证Kp0.1后静态锁定稳定：目标141.46°时实际141.46°、
  误差0..0.20°且命令为0，其他锁定片段误差不超过约0.68°；全局误差约-7.38..6.15°，±8.9k命令峰值
  只出现在运动/换向段。跟手慢主要来自低Kp下仅靠位置误差生成速度，没有手动目标速度前馈；下一步
  应优先加入手动目标速度前馈，再小步提高Kp至0.12--0.15，不回到0.3。
  若要放大相同遥控器输入对应的目标角度变化量，应调`YawControlConfig.manual_rate_deg_s`（当前
  100°/s），不是先调`target_lead_deg`；后者只限制目标相对实际位置的最大超前。建议100→150逐步
  验证，必要时再将lead从5°调到8--10°，并保证`manual_speed_rpm >= manual_rate_deg_s/6`（当前30 RPM）。
  底盘路由复核：左摇杆横/纵分别是`rc.ch[2]`/`rc.ch[3]`，当前普通模式输出`vx=-ch3`、`vy=+ch2`，
  拨轮`rc.ch[4]`直接作为`wz`，仅有3计数死区。前后左右若都需反向，应同时反转vx/vy符号；拨轮小
  指令换向应在router增加独立死区、滞回和斜率限制。当前yaw正前方记录值为4555 ticks；最新日志末值
  7652不能视为正前方，需实物对正后重新采集确认。

  用户确认最新`logger_20260925_214755_968832.txt`采集结束时云台正对底盘前方，末尾连续编码器值
  为4968，因此新的正前方候选值是4968 ticks；本轮未写入配置。拨轮修复应在command_router层增加
  独立30/15计数滞回死区、2--4/s归一化斜率限制及掉线清零状态，不能只扩大通用摇杆死区。

  对当前未提交拨轮改动的复核：infantry固件构建成功；spin模式仍更新拨轮平滑状态但输出固定
  `SPIN_WZ_NORM`，退出spin可能继承旧`dial_wz`，应在spin切入/期间清零或明确单独语义。host测试
  因环境缺少clang未运行；`git diff --check`通过。
  完整修改还需同步`tests/host/test_remote_chain.c`：旧测试仍假设yaw_forward_ticks=4555和旧平移
  符号，必须改为4890及当前映射，并补充拨轮滞回、斜率换向、spin退出无残留和掉线复位断言。
  用户确认这些`test_remote_chain`系列测试文件可在下次测试时同步修改；本轮不提前改测试。
  最新`logger_20260925_224020_152653.txt`中`chassis_cmd_wz`在旋转段长时间保持±1，换向经过0，
  仅2次相邻样本符号翻转，说明拨轮路由命令没有高频反向。当前日志缺少四轮目标/反馈/电流，
  无法直接定位电机颤动；应先补齐轮级诊断，重点验证小wz下静摩擦粘滑和单轮速度环。
  具体方案是给`CommandRouter`增加`dial_wz`/`dial_active`，让`route_chassis()`接收router和dt，
  对`rc.ch[4]`按30进入、15退出滞回处理，再按3.0/s斜率逼近；普通和跟随模式使用平滑wz，spin模式
  保留固定旋转。启用判断要包含平滑wz非零，避免回中衰减期间提前关闭底盘。
  后续最新`logger_20260925_220404_755881.txt`末尾yaw编码器稳定在4889--4890 ticks，与上一轮
  候选4968相差约78 ticks（3.43°），因此不能自动确认仍是同一正前方；若该轮实物确实正对前方，
  4890是新的候选值，待确认后再更新配置。

- 硬件：解决 yaw 通信根因；验证云台无突跳、全向轮方向/速度环及新厂商逐型号测试。
- 实时性：控制循环最坏耗时、1 ms 抖动和栈水位未测；普通事件保留64条派发预算，
  最新反馈/控制已独立存储并增加分主题计数，修复后运动内/外环中位4/20 ms，命令未覆盖。
  日志耗时仍待测。
- 初始化：main 未检查 `CAN_Manager_Start` 返回值，电机服务延迟初始化；
  `Kalman_Filter_Init()` 首次调用使用 libc heap 且未检查分配失败，需核对内存余量。
- 扩展：通用舵轮、Jetson 新协议仍缺实物参数；本末启动两帧的总线负载需确认。
  RTOS 通知/调度器启用不属于当前任务，继续保持冻结与裸机约束。

- 云台跟随坐标修复：`CommandRouter`的编码器跟随分支此前把`yaw_relative_deg`直接用于云台到
  底盘的旋转，实测云台指向底盘正负90度时前后方向反转。该字段由底盘相对云台的逆时针角
  定义，转换回底盘坐标应使用逆旋转；现改为传入`-yaw_relative_deg`，普通底盘、拨轮和spin
  分支不变。同步更新host路由测试的全角度符号期望；infantry构建和差异检查通过，尚未烧录/实车验证。

- 最新yaw日志`monitor/logger_20260925_231411_798683.txt`（1189帧，MCU487--81121ms）显示运行时
  参数仍为内环Kp=300、Ki=135、Kd=0、输出限26000、积分限4000；并非用户所述的Ki=60，需先
  核对烧录镜像。manual目标速度已达到约+294/-360deg/s，实际约+426/-420deg/s，目标-实际
  误差标准差约105--142deg/s，yaw命令66/1189帧触±12000且活动段有72次符号翻转。目标角误差
  范围约-63..+22deg，lead=15下仍有较大追赶/换向动态；零目标只有2帧，不能评价静态锁定。
  该窗口不支持继续提高内环或速度上限；先确认Ki实际值，再以Ki0/小Ki隔离积分造成的颤动。

- 2026-09-25为底盘速度环调参新增Dashboard v10尾部诊断：四轮数组顺序与
  `chassis_motor_ids`、既有目标/实际RPM数组一致，新增`chassis_pid_output[4]`（速度PID最终
  输出，原始电流刻度）和`chassis_current_actual_raw[4]`（电调反馈原始电流刻度）。目标RPM和
  实际RPM原已在v9中发布；Python monitor logger新增对应16列，网页解码器同步支持v10。无新鲜
  反馈或控制未运行时保持NaN，不把停机零值当作测量。C主机夹具、Python dashboard测试、
  Python语法检查和infantry ARM交叉构建通过；未烧录、未修改冻结低层。后续用四轮同一时间帧
  比较目标-实际RPM、PID输出、电流和轮序，才能区分Kp不足、积分累积、限幅或机械粘滑。

- 最新`monitor/logger_20260925_225310_924801.txt`已是v10，四轮ID顺序为[2,1,4,3]，共1577帧、
  MCU 487--50836ms。固定目标约+/-1200RPM的平台上，ID2/1/3的实际速度大幅在目标附近反复
  摆动（约+66..2105、-2145..-63RPM），速度PID输出多次触及+/-12000，实际电流同步达到
  数千原始刻度；ID4实际约929..1343或-1317..-994RPM，输出未触限，表现明显更稳。四轮稳态
  平均误差仍约+44..+70RPM（反向约-59..-67RPM），但ID2/1/3的误差标准差约500--630RPM，
  不是简单的目标不足。当前底盘Kp=15、Ki=Kd=0、输出限12000；优先对ID2/1/3分别做Kp
  15→10→8的单变量对照，ID4先保持15，避免全轮一起提高Kp。若降Kp后仍固定角区/单轮异常，
  再查电机方向、轮胎/机械阻力和反馈映射；暂不增加Ki，也不要把输出限幅继续上调。

- 新测试`monitor/logger_20260925_225906_463870.txt`验证将ID2/ID1/ID3的Kp从15降到10后明显改善：
  2782帧、MCU487--72882ms，四轮活动段误差标准差约96--127RPM，较上一轮ID2/ID1/ID3的
  449--494RPM大幅下降；四轮速度PID均未触及±12000，实际电流波动也显著收窄。正向平台误差
  约+63/+64/+90/+92RPM，反向约-72/-74/-80/-85RPM（按日志数组ID2/1/4/3）。当前已经从
  饱和换向改善为稳定但有静态欠速，下一步不要继续降Kp；可先保持该基线，或仅把Kp10轮逐步
  试到11/12，比较误差均值与标准差。Ki仍保持0，确认Kp档位后再考虑极小Ki消除剩余偏差。

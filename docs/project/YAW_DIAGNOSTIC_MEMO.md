# Yaw 抖动与目标滞留专项 memo

本会话每次执行任务前必读，与 PROJECT_MEMO.md 一起使用；AGENTS.md 已登记此要求。
这是持续更新的专项状态，不用历史快照推断当前板上状态。

## 目标与约束

- 定位原始遥控输入到速度PID及电流请求的链路，区分输入滞留、参考角保持和反馈振荡。
- 冻结底层与项目依赖方向遵循 PROJECT_MEMO.md；用户现有调参/控制修改保留。
- 09-17用户确认过振动的机械原因；09-25再次报告转动后颤动，当前分析新日志并对齐0刻度。
  不把此前归因自动当作本次根因或已完成维修的证明；此前logger扩展和P调整已完成。
  本轮保留用户参数，未要求烧录或驱动电机。
- 当前工作区command_router.c的>1000跳变拒绝分支已被用户注释；本轮不恢复或改写它。
  未核实该修改是否已烧录，历史缺陷复现不代表当前源码仍启用该分支。

## 已有证据

- 09-26 最新 pitch logger `monitor/logger_20260926_003355_589540.txt`（v10，2872行，
  序号0～12192）显示：有效控制段826帧、`pitch_flags=31`，反馈新鲜且控制/位置环已运行；
  其余2046帧为`pitch_flags=7`，只有反馈有效、没有控制输出。控制段中目标确实到达软件
  限位：最低编码1566对应68.818°，最高编码2205对应96.899°；实际只到约77.70°和
  90.75°，分别仍差约8.9°和6.2°，不是目标被限在中间。到达两端后实际速度回到0，目标
  速度仍约-606或+420°/s（约-101/+70RPM），说明控制器仍要求运动，实际更像出力/负载
  不足而不是遥控死区或软件限位失效。当前pitch配置为外环{Kp=.5,Ki=0,Kd=1.1,
  output_max=950}、内环{Kp=12,Ki=0,Kd=0,output_max=26000}，重力补偿和前馈均为0。
  日志尚未记录pitch PID最终输出/实际电流，不能仅凭此文件区分电压命令不足、线束/减速
  机构阻力或电机内部限制。下一步应增加pitch原始命令、P/I/D、实际电流字段，做低速单
  方向测试；不要先扩大软件限位或盲目提高PID上限。该结论仅适用于此采样窗口，未修改
  源码、参数或烧录。

- 09-26 当前目录中时间最新的 `monitor/logger_20260926_011810_028845.txt` 仍是 v10
  （1043帧、载荷444），因此没有新增的pitch原始命令、P/I/D或实际电流数据；v11诊断
  改动尚未体现在这份实车记录中。有效控制帧1039（flags31），目标仍到达两端：
  68.818°时末段实际约77.52°、误差-8.70°、目标速度约-594°/s、实际速度0；
  96.899°时末段实际约90.83°、误差+6.06°、目标速度约+414°/s、实际速度0。
  两端都持续有速度目标而反馈停住，继续支持出力/负载不足或机械提前卡住，不支持软件
  限位把目标截在中间。少量角度跳到约77°的样本待v11用原始反馈和电流复核；未修改或烧录。

- 09-26 用户报告剧烈颤动后，最新 `monitor/logger_20260926_131037_786950.txt` 已是v11
  （693帧、载荷552）。pitch反馈标志持续新鲜，但有效控制656帧中636帧实际角度已越过
  配置范围1566～2205刻度（68.818～96.899°），实际遍历约1.54～358.42°；控制命令
  502帧达到负向25000，内环输出500帧达到-26000，内环D项约-118695～+118697，实际
  电流峰值约24371。外环目标速度频繁达到±950RPM，说明是高增益/高D导致的饱和换向，
  同时运行时没有实际角度越界断力，软件限位只裁剪目标而没有保护反馈位置。已在上层
  `gimbal_controller.c`加入pitch越界即双轴停机并重新等待安全反馈的门闩；infantry配置
  暂降为外环{0.20,0,0,30,0}、内环{8,0,0,6000,0}。步兵构建通过，ELF SHA256
  `38ec366f08e8a735a31e5fa222cb7da8f2ebc5106dd14ee6b6c513a3c81430b0`；仅改源码，尚未烧录。

- 09-26 随后的 `monitor/logger_20260926_131708_009553.txt` 显示越界保护已阻止失控：
  开始3帧因pitch实际约124.54°越界而为flags7/零命令；约73.5秒后机构回到86.9°安全区，
  才恢复flags31。恢复后目标速度为±30RPM，但实际速度仍约0，命令仅约±240、实际电流约
  百级，正好对应运行镜像pitch内环Kp=8且无I/D，静止时输出不足以克服摩擦。该日志运行参数
  是外{0.2,0,0,30,0}、内{8,0,0,6000,0}；当前源码配置已变为内Kp20，说明Kp20尚未由此
  日志证明已烧录。需先把机构放回1566～2205刻度，再烧录匹配源码并做低速单方向测试。

- 09-26 最新 `monitor/logger_20260926_220409_305524.txt`（v11，881帧）中，flags31有效
  控制878帧，pitch实际始终在79.45～87.89°安全范围，说明越界保护没有提前切断。软件
  目标确实到达68.818°和96.899°，但实际分别停在约79.85°和86.75°；对应误差约-11.03°
  /+10.15°，实际速度均值为0。运行参数为外{0.5,0,0,3000}、内{20,2,0,10000,2000}；
  下限末段命令/实际电流约-960/-930，上限末段约+3630/+2886，均未触输出上限。目标仍在
  追赶而电机停转，当前证据偏向机械端部、线束张紧或减速机构静摩擦/卡滞；不能继续靠盲目
  提高PID确认。下一步应断电手动核对两端机械行程，再做低速短时单方向逐级出力测试。

- 09-26 最新 `monitor/logger_20260926_221911_136205.txt` 显示新的边界振荡：有效控制720帧，
  但flags7/31在约186.9～208.2秒间反复切换49次；实际角度反复越过安全范围，约65.96～
  98.92°，命令和实际电流同步反复换向，命令多次达到±10000，实际速度约±44RPM。运行
  参数为外{1,0,0,3000}、内{70,0,0,10000,2000}，I/D均为0，因此这次不是积分或D项导致，
  而是Kp70配合大输出限幅在机械阻挡/限位附近饱和；越界保护越界后自动重新允许控制，形成
  反复断力-重启的边界极限循环。应先停机，改为故障锁存（需重新使能才可恢复），并把内Kp
  降至20左右、输出限幅先降至3000，外环限幅降至30RPM后重新做单方向测试。

- 09-26 最新 `monitor/logger_20260926_222314_513245.txt` 是重新手动保持位置的无控制记录：
  全部1957帧为flags7，不能把过渡运动当作限位。稳定段一集中在约70.62°，由编码换算得
  raw约1607；稳定段二集中在约104.24°，raw约2374。中间约80.33°以及开头98°是移动/过渡
  样本。按当前方向约定，程序建议`angle_min=1607`（上限/抬头）和`angle_max=2374`
  （下限/低头）；写入前仍需确认这两段确实是机械安全端点，而非手扶保持位置。

- 09-26 最新 `monitor/logger_20260926_222858_288650.txt` 已证明新限位进入运行镜像：
  目标端点约70.620°（raw1607）和104.326°（raw2374）；高端目标段实际中位约100.68°、
  低端目标段实际中位约72.99°，仍未到目标。低端附近flags7/31多次切换，实际曾到68.3°，
  是越界保护反复断力/重启；高端命令中位约5478、低端约-3564，实际电流为数千级且未达
  ±9000总限。运行外{1.1,0,0,3000}、内{60,0,0,9000,2000}且I/D为0，结论偏向机械端部、
  线束/静摩擦与自动重启边界循环；应先锁存故障并断电核对机械行程，不能继续盲目增益。

- 09-26 按遥控限位需求修改上层 `gimbal_controller.c`：保留遥控器的归一化pitch增量输入，
  在位置目标裁剪之外增加4编码刻度端点方向拒绝；实际接近`angle_min`且杆量继续向更小
  编码时直接清零并复位PID，接近`angle_max`且继续向更大编码时同样处理，反向杆量仍可
  脱离端点。这样不改变GimbalCmd接口，也避免到端点后持续顶机械。步兵构建通过，ELF
  SHA256 `cc96485a583b58468ca204623d09ed3c9b7ab12458e92bdd1afdad907e20435e`；尚未烧录或实车验证。

- 09-26 最新 `monitor/logger_20260926_224055_137979.txt`：I/D均为0，向上正命令段中位
  约+5873、实际电流约+4960，峰值触+9000；向下负命令中位约-4629、实际电流约-2263。
  正向目标速度最高约+426RPM，负向约-264RPM，flags7/31仍反复切换，实际角度约66.6～
  103.7°。这不是积分或D项，而是Kp70/外Kp1.1在方向性负载下令向上输出过大，越界后自动
  重启造成抖动。建议先内Kp20、内限幅3000，外Kp0.2、外限幅30RPM，并把越界改为锁存后
  再做单方向测试；不要只降低向下参数或继续提高限幅。

- 09-26 小陀螺逻辑复核：车体自转归`application/cmd/command_router.c`的`SPIN_WZ_NORM`
  （当前0.33，归一化底盘角速度），不是`spin_speed_rpm`；后者在
  `config/robots/infantry_standard.c`当前15RPM，只限制yaw电机头部保持环的速度。进入小陀螺
  时路由器保存一次`spin_hold_yaw_deg=sensor.yaw_total_angle`，随后gimbal控制器以
  `yaw_target_memo-sensor.yaw_total_angle`闭合头部世界航向，反馈使用陀螺`g_gz`，速度目标
  受`spin_speed_rpm`限制。小陀螺行进时，路由器用`yaw_total_angle-c_yaw`把左摇杆平移向量
  从云台/世界方向变换到底盘坐标，并固定发布`SPIN_WZ_NORM`旋转；`SPIN_GIMBAL_YAW_ADJ_DEG_PER_S`
  （当前120）只决定小陀螺中yaw杆改变头部保持目标的速度。

- 09-26 复核到`SPIN_WZ_NORM`即使改为3倍/10倍也无差异：
  `adapters/chassis/omni_chassis_strategy.c`先将输入`wz`硬裁剪到[-1,1]，随后乘
  `max_rotation_radps=1.20`；底盘还按共享`CHASSIS_DEMO_TARGET_SPEED=8050RPM`对四轮整体降额。
  因此当前源码`SPIN_WZ_NORM=10.0`实际等价于1.0，车体角速度上限约1.2rad/s，且必须重新
  构建/烧录才会生效。若需继续提高，应先改底盘`max_rotation_radps`并确认轮速/电流余量，不能
  只放大归一化命令。

- 09-26 用户指出小陀螺行进仍不能参考世界坐标。当前spin分支使用
  `yaw_total_angle-c_yaw`，其含义是“云台方向相对底盘”，适合让平移跟随云台，不是世界
  坐标系的场地固定速度；底盘平移也没有使用`g_gz`做旋转速度修正。若目标是世界坐标，建议
  将左摇杆先定义为世界速度向量，再只用底盘`c_yaw`做世界到车体的逆旋转；为补偿传感器/路由
  延迟，可用`theta_pred=c_yaw+omega_z*tau`（`omega_z`来自底盘陀螺，`tau`先取实测链路延迟）
  作为变换角。必须先验证`c_yaw`方向、单位和时间戳；若目标是云台相对方向，则保留现有
  `yaw_total_angle-c_yaw`，不能两种定义混用。建议新增日志记录`c_yaw`、`yaw_total_angle`、
  `g_gz`、预测角和底盘vx/vy及四轮目标，先做固定世界方向前进与原地旋转对照。

- 09-26 无底盘IMU时的边界：yaw电机速度只表示云台相对底盘的角速度，单独积分无法长期
  保持世界航向，会受初始标定、反馈延迟、机械间隙和漂移影响。若`yaw_total_angle`确实是
  云台IMU世界航向，可估算`theta_chassis = yaw_total_angle - yaw_relative_encoder + bias`，
  并用`omega_chassis = gyro_gz - d(yaw_relative_encoder)/dt`或小陀螺头部锁定时的
  `-d(yaw_relative_encoder)/dt`做短期预测；再以绝对角低频校正积分角。若没有可靠的云台
  世界航向，电机转速只能得到相对/短时方向，不能宣称是真正世界坐标。建议先记录两种角度、
  编码器RPM、`g_gz`和时间戳验证符号及漂移，再决定融合权重。

- 09-26 用户提供的参考仓库URL当前返回404，未能核对其实际源码。若采用常见小陀螺做法，
  可在“云台头部保持世界方向”成立时，用yaw编码器相对正前方角直接把遥控平移向量旋转到
  车体坐标；这等价于用`yaw_relative_encoder`替代缺失的`c_yaw`，不能在头部未锁定或
  `yaw_total_angle`不可靠时宣称世界坐标。速度修正应只用于预测角（编码器RPM换算角速度
  后乘实测延迟），并保留低频绝对角校正，避免单纯积分漂移。

- 09-26 烧录时出现`STLink error (9): Get IDCODE error`。主机`just doctor`能看到ST-Link
  序列号`0669FF535548877187254949`并通过OpenOCD配置检查，但未连接MCU；该错误仍停在
  SWD IDCODE读取，尚无新固件写入证据。项目flash默认走OpenOCD，pyOCD只用于RTT连接；
  优先检查目标供电/VREF、SWDIO/SWCLK/GND及复位线，不能把它当作pitch控制逻辑故障。

- 09-25新颤动/零刻度核查：logger_20260925_162737_609345.txt共4076帧，主机
  16:27:39.864～16:29:13.439；头4帧后MCU751→454436、seq3→19814，缺口不插值。
  有效控制MCU486428～516446ms、1310帧seq连续/遥测19～23ms，运行与源码均
  {300,60,0,6000,2000}，flags15/服务OK/速度单环。四次跨零后首样本时间为
  501549/506599/508598/513445ms，raw8149→12、8126→13、0→8157、45→8168，
  展开+55/+79/-35/-69且连续角增量吻合；速度108→102、48→24、-78→-72、-36→-174。
  附近首个|速度|≥200尖峰相对过零后首样本为-865/-457/+138/-160ms；第一尖峰
  raw6269（275.49°）、420°/s，非0刻度。仅说明采样不支持过零展开缺失，不能排除帧间异常。
  67尖峰中61在原生270～360°，样本非独立事件，回中停留增加角区权重，不据此定位部件。
  513993～514954ms连续44帧原始杆量/路由/速度目标全0，速度-570～492、请求18帧触
  ±6000，P -24600～28500/I -594～-162；515316～516446ms再次零目标，速度-648～780，
  I -175～26、17/51帧触限。I未到2000且趋零仍振荡，不支持积分积满为直接解释，
  但不排除积分瞬态影响。所采回调与PID dt3～5ms、反馈年龄1～2ms，未见采样点失联。
  确认闭环反复饱和换向；优先核对角区扰动与P300闭环放大，具体机械/电机内部反馈或
  出力/闭环根因未分离；IMU安装与底盘运动未核对，不以瞬时幅差判反馈错误。
  已询问机械修复和固定朝向，待回复。下一步候选仅P300→200对照，I60/D0/限幅/目标
  保持，未代改/保证稳定。约43.5Hz遥测非完整回调，不能定更高频频率或先后因果。
  证据build/diagnostics/yaw-chatter-20260925/，快照SHA256
  13c54240fb032f91429b50388e5b9b015d126bb2b0062ba9aa9ea91c8b7be2b4；含分析脚本/
  summary/zero-crossings/analysis.png/md。参数/状态/P项/序号/输出截断/展开断言、
  Python检查、现有yaw_reference GCC/Werror（/tmp/yaw-chatter-reference-20260925）与
  图像/文档差异检查通过。无控制源码/接口/参数修改、ARM构建、探针连接或烧录，
  完整Flash身份未核对，用户新参数效果仅对上述运行窗口有效。

- 09-25最新logger `172928_304264`（主机17:29:30.166～17:30:13.085）运行
  {Kp120,Ki0,Kd0,output6000,integral2000}。正向+100°/s段从约307.6°转到
  423.5°（raw1445）后，860153～864431ms共187帧速度0～12°/s，命令中位+2000、
  实际电流中位+1996，反馈年龄1～2ms；反向-100°/s段到291.1°（raw6624）后，
  872642～878760ms共267帧速度-12～0，命令/实际电流中位-2000/-1999，反馈仍新鲜。
  该窗口没有积分，控制输出未归零，属于两个方向的固定位置停转，不是此前P300/I60
  的积分极限循环。两个停止点相差约132.4°连续角；仅凭断电手转顺畅不能排除带电
  姿态下线束张紧、支架/减速机构端点或静摩擦超过约2000原始电流（配置注释约5460≈1A，
  2000约0.37A）。若提高受控出力后停止raw仍固定，偏向机械限位；若可越过，偏向
  负载/静摩擦不足。证据仍为采样遥测，未独立核对完整Flash、未修改/烧录。

- 09-25用户反馈Ki关闭后电机不转，认为约2000原始调节量是合适启动量。仅按P项计算：
  满杆目标100°/s=16.667RPM、静止时Kp≈120对应输出2000；若运行在60°/s仍需输出
  2000，则Kp≈300，但会显著增加动态过冲风险。该计算不保证克服角区负载或恒速，
  不把P-only启动值当作稳态调参结果。本轮只提供算术，未修改或烧录。

- 09-25最新logger `165823_123823`（主机16:58:24.977～16:59:30.202）显示Kp降为30后
  同目标周期调节。有效控制2117帧，源码/运行{30,60,0,6000,2000}，回调dt约4ms、
  反馈年龄1～2ms。+100°/s平台94986～120815ms内，Iout反复达到2000，速度从0起升至
  约60～72°/s并多次回落，范围0～228°/s；请求620～2500，未触±6000。静止时目标
  100°/s=16.67RPM，P=30仅约500原始刻度；Ki=60约每秒累加1000，约2秒到积分限。
  过目标后P负项不足以快速抵消I，I再按负误差慢慢消退，形成积分主导的极限循环。
  Kp/Ki由此前300/60的5秒降为30/60的0.5秒，积分相对权重增10倍；目标连续为+100，
  反馈新鲜，非位置环/过零原因。下一步候选Kp30配Ki6保持原相对时间尺度，或Ki0先看
  P30能否克服负载；不保证稳定、未代改或烧录。本轮无控制源码或接口修改。

- 09-18双轴PID指导只读核对：yaw源码内300/60/0/6000/2000、外1/0/0/12000/0旁路；
  pitch内20/5/0/10000/0、外1/0/.1/10000/15000，积分限0及外actual=0分别使pitch
  I与测量D不起作用，手动目标仍按回调累加。175142日志7173帧仅4帧控制启用，无yaw
  高目标运动；启用帧yaw参数300/60，pitch误差-1.514°、速度目标-206.733°/s而实际0。
  不凭4帧判断动态根因/稳定性。建议yaw暂保留300/60，按恒速误差/回中/饱和再选择
  小增Ki或振荡降P；pitch先约束外环速度候选60RPM并保持外I/D0，稳定后试内积分限300，
  P20/Ki5不同时提高；均只是候选未应用。源码/秒单位算术及串级官方资料核对、文档检查通过，
  摘要/tmp/pid-guidance-20260918.json；无控制/接口修改、构建或探针连接/烧录。

- 09-18 RTT网页无数据排查：logger PID14675仍运行，宿主8080 HTTP200、8765
  WebSocket连续返回3个438字节包；logger_20260918_174252_392673.txt持续追加
  v9帧，seq95013后继续增长且末帧年龄<5秒。此窗口采集与服务端发送正常，
  不能用网页空白判断yaw遥测停机；优先核对VS Code SSH的8080/8765双端口
  转发与页面WebSocket URL，客户端实际状态尚未验证。仅读现有日志/服务，
  无新探针连接、控制参数/接口修改、重启或烧录；时效/递增、HTTP/WS及文档检查通过。
  后续按用户要求修复主机入口：默认HTTP/WS共用8080，同监听不覆盖浏览器转发端口，
  显式双端口兼容。36项Python/Node/入口/传输回归通过；已正常退出旧logger并以
  normal/attach重启PID17548，同8080 HTTP200且WS解码v9 seq105900～105902连续。
  新录制logger_20260918_174839_860969.txt，用户刷新后显示待确认；未修改控制参数/
  固件协议、烧录或暂停/复位MCU。验证日志/tmp/rtt-single-port-{tests,live}-20260918.log，
  文档差异检查通过；此前“无重启”仅指首轮诊断。
  用户随后要求释放ST-Link：确认唯一占用者为上述PID17548，已SIGINT退出并核验
  /dev/bus/usb/001/004无持有进程、8080/8765/3333无监听；当前logger停止，不自动重启。
  本轮未改控制/接口或烧录/复位MCU，占用检查及文档差异检查通过。

- 09-17积分开放后仍慢：210550日志参考135.582397461°/实际125.5078125°与图匹配，
  源码与运行参数均200/30/0/6000/2000。固化MCU64037～86013ms共958帧，序号连续、
  最大间隔23ms，状态有效/P项符合200*(目标-实际)/6。图末三段满目标平台：
  76307～78998ms目标+100，Iout -54.084→435.215，去首300ms速度中位66，
  末300ms中位72°/s；80056～81482ms目标-100，Iout +362.182→+45.722，63帧
  全为正积分，抵消部分负向P，去首300ms速度中位-60；83000～83828ms目标+100，
  Iout124.739→274.754、去首300ms速度中位66。三段短，不能称已验证最终稳态误差。
  单独截75019～86013ms共479帧，请求-2223～2789、反馈电流-2214～2652、
  Iout-108.055～440.970，未触6000/2000；反馈年龄1～2ms，PID dt3～5ms。
  较宽窗口67～69秒另有尖峰/6000饱和，不混入此三段判断，不重新否定用户机械归因。
  积分确在积累且尚未到限；40°/s误差Ki30仅200刻度/秒，短时换向还需消退旧积分。
  建议下一档仅Ki30→60（同误差400刻度/秒），P200/D0/限6000/2000与目标100保持；
  以同向恒目标和回中观察改善/超调，不保证此候选稳定，未代改/构建/烧录或硬件操作。
  证据build/diagnostics/yaw-pi30-followup-20260917/{snapshot.txt,summary.json,latest-three-platforms.json}，
  快照SHA256=ee91961425f10250bfed8034deaae2b0190d0da4a1e52f342b58085c6e5924a6。
  参数/状态/P项/连续序号/限幅断言、秒单位算术、源码/NI积分说明与文档差异检查通过；
  完整Flash身份、长平台跟随及Ki60稳定性仍未验证。

- 09-17再次询问慢是目标不够还是跟不上：205842日志匹配参考153.061187744°/
  实际125.991210938°，运行与源码均{200,30,0,6000,0}；Ki30但积分限0，956帧
  Iout全0，源码PID_Calculate对积分的±integral_max限幅解释此现象。固化MCU
  118978～140937ms、序号连续、最大间隔23ms；正常±100平台去前300ms后速度幅值
  中位60～72°/s，末两平台-66/+60，请求绝对值最大1977<6000；127694～128448ms
  平台有-282～294°/s尖峰及6000饱和，单独看待，用户已确认机械振动原因。
  当前首先是跟随不足，目标100°/s约3.6秒/圈，实际60约6秒/圈；位置环非到速必要条件。
  候选保留用户P200/Ki30/D0/6000，仅开放积分限2000，再看恒目标误差、Iout与回中；
  40°/s误差Ki30每秒约积200电流刻度，短平台不保证立即到速。此建议替代前轮
  P190/Ki50候选，尚未应用，不能把待调PI当作稳定性证明。
  logger仍持续追加，观测seq7555→12382、最新mtime年龄0.005秒；此前无数据原因
  未复现，不能把沙箱ps/USB/网络权限限制解释成宿主故障。证据
  build/diagnostics/yaw-integral-disabled-20260917/{snapshot.txt,summary.json}，SHA256
  1c509f354bedb50dffca692c14b0e29e2c4be1549055fb24aa363d054c1a69ec。
  参数/状态/P项单位/Iout/序号断言、积分限源码与文档差异检查通过；本轮未改控制
  参数/接口/源码、构建、连接硬件或烧录，未独立核对Flash哈希，PI效果待实测。

- 09-17最新用户确认已找到机械振动原因，未说明具体部位/维修方式；不再将角区相关或
  CAN/IMU差异推定为电机反馈故障。前轮连续旋转分析在被打断前已完成，现补记：
  201121日志20:47:24.028～20:49:04.883，4394帧；-100°/s平台98.492秒4291帧、约15.8圈，
  运行P190/I/D0/限6000。15完整圈中11圈出现|速度|≥200尖峰，114尖峰中98在210～300°；
  16次过零邻点速度-72～-54°/s。CAN全平台-828～612°/s，陀螺Z为-178.7～85.8，
  两者时序/带宽不同，安装与底盘运动询问未回复，不从差异推断反馈失真或先后因果。
  证据build/diagnostics/yaw-continuous-20260917/含capture/analyze/summary/turns/analysis.png；
  capture SHA256=60592364af7dab385a26526961f53d1f654bc9b589dba56712efec8a678c2e9b。
  日志状态/序号/P项/目标单位及范围断言通过，绘图已检查；只读既有logger，无硬件操作。
  最新半速图匹配末角-2504.399414°、参考-2531.927979°。截取201121日志652帧到
  build/diagnostics/yaw-speed-offset-20260917/{snapshot.txt,summary.json}；末个完整-100平台
  MCU2467356～2469127ms共78帧，去首300ms后64帧速度中位-57、范围-72～-48°/s，
  电流请求中位-1356.5、反馈-1357.5；PID190/0/0、Iout/Dout0、dt4ms，反馈年龄1～2ms。
  窗口控制有效/服务OK、序号连续、未触6000，P项逐帧符合190*(目标-实际)/6。
  源码用户P200与运行P190分开；速度单环目标确为-100，模式限30RPM不会裁成约半速。
  短平台符合纯P负载持续误差，不能把“约一半”当固定比例限制；位置环不是速度精确跟随
  的必要条件。若机械问题已处理且运动稳定，速度PI候选{190,50,0,6000,2000}，保持
  speed_loop_only=true；40°/s误差时积分约333刻度/秒，先看恒速误差/iout和回中，
  不是保证稳定或保证本次约1.8秒平台内到速。位置环后续再接，不能直接开启现有外P1。
  本轮仅解释/建议，未改参数/控制接口、构建或烧录；状态/序号/P项/限幅、秒单位算术、
  NI PID积分原理及文档差异检查通过。机械修复效果、PI稳定性与速度跟随仍待实测。

- 09-17询问yaw过0°是否平滑：当前源码YawReference_Update先以float计算相邻编码差，
  >4096减8192、<-4096加8192，再累计连续角；8190→2展开+4，反向-4。位置误差用连续
  目标减连续实际，不随过零重选圈数；angle_target的Wrap仅为单圈兼容值。
  当前仍speed_loop_only=true，速度反馈直接取CAN RPM，不差分单圈角度，也无过零专用
  换向/减速/重置PID逻辑。共用PID输出有RC=0.002秒一阶低通，非过零专用；没有额外
  加减速/S曲线规划。区分跨零连续处理与运动平滑，不能把源码机制当作实车无抖动证明。
  现有test_yaw_reference以GCC/Werror重跑通过，覆盖双向跨零、多圈/时效失效，执行产物
  /tmp/yaw-zero-review-20260917；源码/文档差异检查通过。未改控制源码、构建ARM或操作硬件。
  随后用户报告每过固定位置振动并给图：201121日志参考-816.585449°、实际约-847.05°
  与截图末值相符；固化79409帧，SHA256=32b4631d33fa5fd1093a3e9b82581c3a8b9c3fc0b38a69406b9fc3a63dca934e。
  MCU1737.116～1760.595秒1023帧，序号连续、最大间隔23ms，enabled/startup1、flags15、
  服务OK、P190/I/D0。按|速度|≥200°/s、间隔>300ms分组：1745.670～1745.693秒
  编码5435～5497、速度-504～-408、目标100、请求6000；1750.889～1751.888秒编码
  4839～7364、速度-468～546、目标100、请求±6000；1756.302～1757.329秒编码5275～5432、
  速度-678～438、目标0、请求±6000。尖峰组不是完整振动持续时段；末组含回中制动，
  不能把三组全当恒速同角触发。尖峰编码均远离8191/0，第一/三组约原生239°/232～239°，
  第二组覆盖213～324°，只凭此记录不能证明每圈严格同角触发或唯一硬件根因。
  同窗口三次跨零8179→17、8179→18、8171→8，连续角分别增1.318/1.362/1.274°，
  速度54→54、60→66、48→54°/s，无采到跨零突变；约23ms抽样不能排除帧间异常。
  电流请求/反馈交替支持闭环反复驱动制动，须继续区分角区负载、电机内部反馈/出力与
  闭环放大；不能把CAN机械零点等同内部电角度零点，也不据此盲加平滑或提升P。
  当前磁盘用户已改内P200（I/D0、限6000），本轮日志仍P190，未独立核对Flash。
  证据build/diagnostics/yaw-fixed-position-20260917/{snapshot.txt,identity.json,analyze.py,summary.json}；
  哈希/状态/序号/P项单位/跨零角增量断言通过，现有yaw_reference GCC/Werror回归通过
  （/tmp/yaw-fixed-position-reference-20260917），文档差异检查通过。未改控制参数/源码/接口、
  构建ARM、连接硬件或烧录。

- 09-17询问角度/速度同时或分别重合的方法：源码仍速度单环、内190/0/0/6000/0，外1/0/0/12000/0。
  分阶段建议：排除已记录的17°阻挡后，先速度单环试有限PI{190,50,0,6000,2000}，依据
  误差/iout/回中表现调节；稳定后才接位置环，低速候选manual_rate30°/s、manual_speed10RPM、
  外{0.02,0,0,10,0}、speed_loop_only=false，保持已验证内环，普通下档先测小角度目标。
  不能直接恢复当前外P1；外P单位RPM/编码刻度、输出限单位RPM，区别内环电流限。
  理想内环下外P0.02等于2.7307/s，10°误差给27.31°/s，30°/s匀速目标仍约11°滞后，
  因而该低增益仅用于初始定位/收敛验证，不保证运动角度重合。进一步方案是平滑且一致的
  角度/速度轨迹，速度需求=轨迹速度+位置误差校正，再由速度PI给电流；已有feedforward
  配置是RPM→电流前馈，不是这一位置到速度前馈，需改应用链而非直接填同名增益。
  轨迹速度须对应实际受领先限制后的目标变化，含加减速/限速和模式切换重置；串级启用后
  右图target表示外环给内环的需求，含追赶速度，不再独立保持原方波。实际角度与速度受
  同一运动约束，不能任意分别指定并要求瞬时精确重合；启停动态误差需给容差。
  上述均方法/候选，未修改源码或烧录。源码链/遥测语义、单位换算算术、Synapticon官方
  串级/速度前馈说明及文档差异检查通过；没有实车稳定性证明，物理卡阻询问仍待回复。

- 09-17询问下一步调什么：同201121日志继续追加，固化20973帧；最后非零运动MCU422030ms，
  其前20秒至后1秒913有效帧均P190/I/D0、无6000饱和。195帧|目标|≥99，实际速度幅值
  中位60、范围12～90°/s，无零速帧；末角171.5625°与截图一致，但不据此否定先前17°停转。
  建议先确认无卡阻/外力，再以速度PI候选{190,50,0,6000,2000}试调，P/总限/目标不变，
  同时开放积分上限；40°/s误差时Ki50约累积333电流刻度/秒，Ti3.8秒，只是温和起点，
  不保证短脉冲跟随或消除误差。看恒目标下速度误差、iout与回中漂移/振荡；出现停转不继续加力。
  源码仍{190,0,0,6000,0}，本轮只提供建议未代改/构建/烧录；之前物理卡阻询问仍待回复。
  证据build/diagnostics/yaw-next-tuning-20260917/，快照SHA256
  3a4c0bff256090eff2c79f8e624a2660536d7cbdaa6bbe67dd67c9969955112d；运行P/I/D、P项单位、
  无饱和断言、积分秒单位算术、NI积分原理和文档差异检查通过。速度基本一致仍需实车验证。

- 09-17用户提供下一张图：201121 v9日志固化3990帧（20:11:23.311～20:12:54.985），
  3986有效帧均P190/I/D0，服务OK、无6000饱和，证实新P已进入运行；活动平台中位速度
  幅值48～60°/s，仍未跟上100。MCU41.150～41.863及42.645～43.749秒两段连续样本，
  目标-100、速度0，原始ch0=660/route=-1、enabled/startup1/flags15，反馈年龄1～2ms，
  电流请求中位-3166、反馈电流中位-3169/-3170。编码387～390和401～403，
  角度17.01～17.14及17.62～17.71°，不是只因RPM量化而漏掉明显运动。
  对照上轮P180的17个高目标零速样本，角度也在17.3～18.0°；提示同角区阻力/出力异常，
  不能据此确定机械挡块、电机故障或外力。已询问用户该处是否挡块/线束/手扶，待确认。
  本轮保留P190及其他参数，先核实停转原因，不继续加P/积分；速度跟随目标尚未达到。
  证据build/diagnostics/yaw-p190-review-20260917/{snapshot.txt,summary.json}，SHA256
  abda8b4f29bf7eb30ff3330ec831ede5fb45e060e9ffc4bd376fd5a144f25910。
  逐帧P项/状态/无饱和与两段序号连续、输入、反馈时效/角度断言及文档差异检查通过。
  仅日志分析/记忆更新，未改控制源码/接口、构建、连接硬件或烧录。

- 09-17要求target/actual基本一致：固化200428 v9日志4803帧，主机20:04:30.555～20:06:20.916，
  有效4799帧P180/I/D0/限6000/积分限0、服务OK，无采样输出饱和。连续±100°/s平台去前200ms，
  多数实际速度幅值中位54～60°/s、请求约1200～1400；另有506ms负目标平台实际0、
  请求约-3000/反馈电流约-2994。负载/阻力未分离，不能认定仅为P不足或已达稳态。
  证据build/diagnostics/yaw-tracking-p180-20260917/{snapshot.txt,summary.json}，快照SHA256
  a1af572e48e3f3fd60d269354dd8474eca91f1c99618137127d467ce126c29df；哈希/有效帧数、
  逐帧P项单位、参数/服务状态和无饱和断言通过。最新源码180→190，同误差出力+5.56%，
  其余参数不变。历史P200振荡，故不大幅跳增益；190待实测，未承诺或实现基本重合。
  持续误差可能需有限I，本轮按用户只调P范围未加；参考角不参与闭环。
  步兵构建/ELF与全套GCC主机回归通过，FLASH152272/RAM57696B，ELF
  bf794b3832063c3349d44d7950a5545f49281591dcc6e7c45ab9bf2d80dcfa05；日志
  /tmp/yaw-p190-{build,host}-20260917.log。无接口/冻结底层修改，未烧录或操作硬件；
  待用户更新后采P190双向起停记录，出现振荡退回上一档，速度基本一致目标尚待实车闭合。

- 09-17再次授权提高P，已将源码步兵yaw内150→180，I/D0、限6000/积分限0、
  协议12000及满杆100°/s/速度单环保持，相同误差比例出力+20%。新195811 v9日志
  固化前3446帧P全150（build/diagnostics/yaw-p180-20260917/），确认前轮增益已出现在运行记录。
  最新截图目标±100°/s而实际约±30～60°/s；没有P180实测，不据此承诺速度或稳定性。
  步兵构建/ELF与全套GCC主机回归通过，FLASH152272/RAM57696B，ELF
  a475ed5ae1feca14f7f27fe20e9ee330d7fc6ba676a786484df070251b11740b；
  日志/tmp/yaw-p180-{build,host}-20260917.log。参数/文档差异检查通过，未改接口/冻结底层，
  未连接硬件或烧录；出现振荡需退回已用P150，进一步增益须基于新采样。

- 09-17启动改善但速度慢的限幅复查：源码满杆100°/s、模式限30RPM=180°/s，
  当前内PID150/0/0/6000/0的最后两项为总输出/积分项限幅；Ki0不靠提高积分限出力。
  固化194343 v9日志13068帧（主机19:43:45.298～19:48:45.740），13064有效帧
  实际运行P120/I/D0/限6000/积分限0、服务OK；361帧|目标|≥90°/s，实际-48～0，
  PID/请求幅值最大2000，电流反馈幅值最大2026，没有采到6000饱和。
  MCU131146ms起2.231秒98帧平台实际中位0、请求中位-2000、反馈电流中位-2003；
  不能解释为只有响应延迟或限幅过低，负载/阻力与出力不足原因未分离；日志有缺口不跨越推断。
  19:52:02烧录日志随后已VERIFY_OK/FLASH_OK，旧P120采样早于此次烧录，不代表当前参数。
  当前源码P150先用新记录验证；只有实际跟上100仍嫌慢才提高manual_rate_deg_s（例150，
  现有30RPM限允许），本轮仅解释未代改。证据build/diagnostics/yaw-speed-limit-20260917/，
  有效帧P项/单位/参数/输出限断言、源码/NI积分原理与文档差异检查通过；未构建/连接硬件/烧录。

- 09-17最新截图后用户明确要求代改P：步兵速度内环120→150，I/D0、限6000/积分限0、
  协议12000及速度单环/100°/s保持，其余轴未动；相邻注释同步说明小步改善响应。
  相同误差比例出力+25%，满杆静止P项2500刻度；P150不等于已验证稳定参数。
  图中还有目标-100°/s而实际0的平台，不只表现为起步延迟；无同步电流不能排除其他阻断。
  步兵构建/ELF检查、全套GCC主机回归和差异检查通过，FLASH152272/RAM57696B，
  ELF f16ecc01318116aa7725f4c036e8963a5afe676d1980fd4eb875f4998c7062fa；
  日志/tmp/yaw-p150-{build,host}-20260917.log。只改源码配置/注释和记忆，未烧录/操作硬件，
  未更改控制接口或冻结底层；实际响应与振荡仍需更新固件后验证。

- 09-17调参后询问无法flash：19:35:52日志在目标识别前空FIRMWARE_ERROR退出，
  本轮just build/ELF检查通过，无待编译项，仍475a9fa6（不代表已烧录）。宿主机
  logger桥接PID3372/fd13持有ST-Link的/dev/bus/usb/001/004，权限0666可读写。
  先在logger终端Ctrl+C释放，再flash，成功后重开logger；空日志不足以独立确认USB错误码，
  尚未停止进程或做释放后重试。证据build/diagnostics/flash-blocked-20260917/；
  日志阶段断言、构建/ELF及文档差异检查通过，未改控制/PID、操作MCU或烧录。

- 09-17随后用户截图报告yaw响应慢并问上调P建议。截图显示速度环已启用，
  目标平台约±100°/s、实际多数约±30～50°/s；不再沿用前次startup0判当前停机。
  截图不含PID/电流/时间身份，不能据此定量辨识增益或确认饱和。磁盘仍内120/0/0、
  限6000/积分限0、协议12000、speed_loop_only=true。条件性建议若板上同值，
  仅内P先150（同误差出力+25%），双向起停无抖动/反冲且仍慢才试180；
  历史P200已捕获回中振荡，暂不直接跳200/300/360，出现振荡退回上一稳定档。
  满杆100°/s静止时P120/150/180分别约2000/2500/3000刻度（滤波前），
  I/D/限幅/目标速度保持；参考角不参与闭环。仍跟不上时采最终电流/实际参数再判，
  不把纯P不足或机械阻力当作已证实根因。源码/算术、MathWorks调参权衡与文档差异
  检查通过；本轮仅建议和记忆更新，未改参数/控制接口、构建、读板或烧录。

- 09-17晚用户报告底盘能动、yaw不动，确认左拨杆下档。固化正在追加的
  `logger_20260917_191326_469029.txt`共13178帧，截止北京时间19:18:18.523；
  19:15:18.686～19:16:14.439有918帧右水平杆非零（覆盖±660），均enabled1、
  startup0、双轴flags7，pitch约2582～2583刻度。最新19:18:08.538～18.523
  连续455帧同样未启动、双轴反馈有效；pitch2582～2583，超过源码上界2205约16.57°。
  当前capture_startup_position在pitch越界时拒绝双轴启动，与本次阻断一致；
  已确认的是启动未通过，尚未独立匹配板上Flash/限位，不把源码配置当作已读板参数。
  帧仍为v8/300B，虽TXT已有117列，v9诊断项无效；不能据此读出实际电流或PID。
  优先在输出禁用后核对pitch机械姿态/标定；未修改限位、PID、控制接口或操作硬件。
  快照/summary/input-check在`build/diagnostics/yaw-no-motion-20260917-evening/`；
  最新窗口序号连续、状态/目标NaN/角度换算断言及文档差异检查通过。

- 历史生产链测试证明原跳变过滤可无限拒绝合法反向输入；离线也未清旧输入历史。
  详见[YAW_COMMAND_CHAIN_AUDIT.md](YAW_COMMAND_CHAIN_AUDIT.md)，属于特定源码/镜像行为。
- 最新实测文件logger_20260917_163217_510100.txt：2116帧/2110有效控制帧。
  MCU27.897～28.821秒target=-100度/秒、反馈-456～534；29.041～29.415秒target=0、
  反馈-474～234。这些连续窗口均enabled/startup1、flags15、遥控消息新鲜。
- 69.279～71.061秒速度目标/反馈全0，参考角778.081度、实际约747.949度；位置环未启用，
  约30度参考残差不能解释为速度PID继续追角度。回中停止参考角积分是现有行为。
- v8 TXT缺少同步原始ch0/路由yaw_rate/电流/PID项/控制dt，不能证明具体发生输入滞留。
  也没有固件/PID身份，不能把历史P360当成这次参数。详见[YAW_LOGGER_ANALYSIS_20260917.md](YAW_LOGGER_ANALYSIS_20260917.md)。
- 15:59 SWD窗口未使能只代表该窗口；后续logger确实捕获使能后的运动和振荡。

## 已完成：同步遥测（尚未烧录）

- 09-26 为验证pitch端点而扩展Dashboard v11：在v10帧（444字节）后追加108字节pitch诊断，
  载荷552/帧562。字段包含pitch命令单位、服务状态、原始命令、实际电流反馈、内外环速度、
  P/I/D/总输出、两级PID增益/输出限幅/积分限幅及内环dt；原始命令/电流保持协议刻度，
  未猜测安培/伏特换算。数据在Dashboard_Step从当前pitch MotorContext和同一回调快照读取，
  无效项写NaN；Python logger和网页“All → Pitch PID Diagnostics”同步显示，v3～v10仍兼容。
  步兵v11构建通过（FLASH154704/RAM57704，ELF SHA256
  `5de144c7ce58f159282609b0cf1f9724cf29aa545c886c3e536860ad7a796e7d`），哨兵构建通过
  （FLASH155536/RAM57704）。真实C生产样本、Python/Node解码、拆包/CRC、旧版本和网页端口
  回归共18项通过；`CC=gcc sh tests/host/run_tests.sh`在既有control_recovery断言处失败，
  原因是当前用户配置已将speed_loop_only改为false而该旧测试仍断言true，与本次logger字段无关。
  尚未烧录或实车采集v11，下一步需用v11日志低速单方向观察命令/PID输出/实际电流。

- `GimbalCmd.trace`按值携带路由使用的原始ch0、RC消息序号/派发时间、路由序号/时间、
  RC在线/CAN解锁/请求模式位；只观察，不参与控制。新RC先于旧命令派发也不会覆盖其来源。
- 云台回调结束复制上述来源、yaw_rate、PID目标/实际反馈、P/I/D/output、运行PID参数、
  应用最终请求/服务状态/单位、电流反馈、编码、反馈时间、回调计数/间隔与PID dt。
- `GimbalMonitorSnapshot` v2为216字节，保留v1的148字节前缀；SWD监控兼容两版，
  使用匹配ELF大小读取，不能复用旧固定偏移/37字读取脚本。SWD显示公共双轴，RTT记录扩展项。
- Dashboard v9载荷412/帧422字节，保留v8的300字节前缀；Python/网页兼容v3～9。
  TXT新增39列（共117列）：同步来源/闭环诊断、三种来源年龄、最新遥控通道、编码及丢帧数。
  同步输入使用`yaw_rc_ch0`，不是可能较新的`rc_rocker_r_x`。详见[logger指南](../guides/logger-guide.md)。
- 无来源、过期、未运行PID的字段分别输出nan；禁用时真实零请求/服务状态仍可记录。
  `yaw_trace_flags`：1来源、2见过RC、4在线、8解锁、16请求视觉、32请求spin。
  `yaw_mode`是实际模式0手动/1视觉/2spin，速度调试覆盖请求时二者可能不同。
- `yaw_rc_sequence`只计交付Cmd的RC消息，不是UART逐帧计数。仍至少20ms一次非阻塞遥测，
  用callback_count/seq/telemetry_drop_count识别未采回调与遥测缺口，不冒充逐回调或逐CAN录制。
  请求接受不证明CAN发送完成；PID dt可能经过控制器回退，callback_dt_ms独立记录。
- 没有改用户PID、速度/模式规则、已注释的跳变过滤或冻结底层；没有连接硬件/烧录。

## 验证与当前构建

- 全套GCC主机回归通过；补充RC较新消息先派发时来源保持、RC保持/超时、云台同回调PID/电流/
  dt对应断言。真实C帧→Python/网页解码→TXT保存含符号、单位、有效/过期/禁用、CRC/拆帧、
  v7/v8兼容、来源缺失及时钟回绕；RTT/网页/传输17项测试通过。
- SWD监控13项测试通过，覆盖v2完整读取、写者重叠拒绝、v1兼容及不完整/未知布局拒绝。
  `--prepare-only`核实本地ELF监控大小216，hardware_commands_executed=0。
- 步兵/哨兵Debug构建及ELF检查通过：FLASH152272/153104 B、RAM均57696 B。
  步兵ELF SHA256 `475a9fa6aa827251220446b67c2bb961533abd24e96309741da7851a70803b73`；
  哨兵 `0699b43d1a6086f444f38a0a8cba6df2e19231cc60820ec0cbe28ed4e2e35cb7`。
  日志在`/tmp/yaw-sync-{host,python,swd-tests,build,sentry-build}.log`，差异检查通过。
- 待实际更新固件、重启logger后验证v9新列和实车抖动；旧TXT不能补录缺失输入。
  运行PID参数已入日志，完整固件哈希仍需与烧录记录配套；新增遥测耗时/丢帧率尚未实测。

## 后续判据

- 同一命令原始ch0已反向而yaw_rate保留旧方向：定位路由；确认模式覆盖及在线位。
- yaw_rate正确但PID目标错误：核对模式、单位与限速；用命令/回调序号关联。
- PID目标稳定或归零但速度/电流交替：检查闭环动态、反馈及控制时序；不只凭幅值归因P。
- 参考角停留但速度目标0且位置环无效：不算速度目标滞留。
- 烧录后才采新日志，记录实际参数；不跨启动连接序号，不插值跨越遥测缺口。

## 2026-09-25 Kp=240 最新日志

- 最新记录为`monitor/logger_20260925_173608_140243.txt`（17:36:10.125--17:37:02.364，
  2281帧）；源码当前内环为P240/I0/D0、输出限幅6000，日志运行参数与之相符。
  本轮未修改源码、未构建、未烧录，故运行参数只代表该日志窗口，完整Flash身份仍未独立匹配。
- 回调间隔主要4 ms（3--5 ms），反馈年龄主要1--2 ms；目标+100°/s窗口实际速度
  -474..678°/s，P输出约±2.3万，命令多次触及±6000；目标0窗口也出现±6000。
  因此当前颤动不是日志跟不上或Ki累积，属于P-only高增益下的饱和/反向极限环。
- 大幅速度反转集中在编码器原始值约6033--7994（连续角约265--351°），其中
  约6900--7800（约303--343°）最明显；同一角度区间在目标0和+100窗口重复出现，
  说明与姿态/角度相关，但不是8191↔0零点越界（跨零仍连续）。角度区间较宽，不能据此
  指认单个齿位或某个固定零刻度。
- 结合Kp120日志当时命令约±2000并在不同角度停住，Kp升至240后同一负载/反馈扰动被
  放大到限幅并来回换向。断电手转平滑不能排除通电时的静态负载、线缆姿态张力、齿轮
  预紧或电机换相/反馈扰动；仅凭此日志尚不能在机械负载与反馈异常之间定责。
- 后续诊断应先保持Ki=0，将P降回120--180做同一路径复测并记录原始编码、命令和电流。
  若低P可通过而240才颤动，主因是增益放大/饱和；若低P仍在同一编码区间以固定命令卡住，
  再优先查角度相关负载、线缆/限位和反馈质量。不要继续盲目上调P。
- 进一步核对表头和实现后确认：日志中的`yaw_pid_pout`不是速度目标。速度目标仍由满杆
  `100°/s ÷ 6 = 16.67 RPM`限幅；P项使用RPM误差。比如17:36:59.875处目标100、
  实测678°/s（113 RPM），误差-96.3 RPM，故Pout=240×(-96.3)≈-23120；
  17:36:35.914处目标-86.8、实测-750°/s（-125 RPM），误差约110.5 RPM，Pout≈26527。
  也就是说突变来自实际速度反馈/超调进入误差，不是摇杆目标突然变成2.3万；随后输出限幅
  又促成反向超调。需把`target_deg_s`、`actual_deg_s`、`yaw_speed_raw_rpm`分开看。
- 最新`logger_20260925_175323_947967.txt`中仍为P240/I0，目标+100窗口实际约30--108°/s
  （中位66），对应反馈5--18 RPM；Pout约-320..2800，命令未饱和。目标保持不变时，
  输出随实测速度和姿态改变，说明当前是P-only速度环：负载越大速度误差越大，P才给更多
  电流，速度不会自动精确等于目标。反馈RPM为整数，仪表换算成6°/s一级，离散量化也会
  让曲线看起来一档一档地不均匀。相同目标下的速度差主要来自角度相关负载/反馈量化，
  不是目标速度在变化；Ki=0也意味着没有积分去补偿持续负载。

## 2026-09-25 外部步兵配置复用请求

- 用户要求参考`https://github.com/NYUSH-Robotics-Club/nyush-rm-control`复用步兵遥控链路及
  电机PID。首次网页/API/raw访问受沙箱缓存和DNS限制而失败，期间未猜测外部配置；随后已通过
  受限Git读取到仓库并完成逐项差异。
- 已通过受限网络读取公开仓库HEAD `2b7ca720e856f1efda9f6de6259faea42654720d`。其遥控
  解码器与本工程已复用的同源模块一致；参考默认USART1 VT03/VT13，而本板USART1仍由冻结
  配置接IMU，所以本轮保留现有USART3 DBUS适配。PID已按参考步兵值写入当前配置结构，未改变
  yaw控制模式或CAN/机械参数；构建、主机测试、差异检查通过，未烧录。
- 进一步核对遥控放大和通道后发现两者并不完全一致：DBUS解码器和daemon文件逐字相同，
  但参考步兵将左摇杆`rocker_l_ / rocker_l1`用于yaw/pitch、右摇杆用于底盘；当前
  `command_router.c`将`ch[0]/ch[1]`（右摇杆）用于yaw/pitch，将`ch[2]/ch[3]`（左摇杆）
  用于底盘，yaw还额外取负号。当前满杆yaw速度为100°/s（目标16.67RPM），pitch每回调
  直接增加60编码刻度；参考遥控云台增量为yaw `0.003*raw`、pitch `0.001*raw` 度/回调，
  底盘为raw乘20，死区参考为20而当前通用死区为3。因此链路协议一致，但通道分配、正负号、
  死区和放大倍数均有差异；本轮仅核对，未擅自修改。
- 按参考RobotTask的5 ms周期折算，参考yaw满杆增量为0.003×660=1.98°/周期，约396°/s；
  当前yaw满杆为100°/s，约为参考的25%。参考pitch为0.001×660=0.66°/周期，约132°/s；
  当前pitch为60×360/8192=2.64°/回调，最新回调约4 ms时约659°/s，约为参考的5倍。
  两者单位和控制方式不同，不能把0.003直接填到当前manual_rate_deg_s。
- 最新`monitor/logger_20260925_183154_649048.txt`运行参数为P150/I135/D0，回调固定4 ms、
  反馈年龄1--2 ms。目标+100°/s时实际中位54°/s（0--108），目标-100°/s时中位-60°/s
  （-156..-6）；Pout约+1150/-1000，Iout中位约+358/-761，最终命令约+1437/-1621，
  均未接近PID 26000或协议12000限幅。故当前跟速不足不是目标只有一半、也不是logger延迟，
  而是短目标窗口内P+I给出的电流不足以克服负载，且负载/积分状态有方向和角度差异；目标0时
  Iout仍可约-677，说明切换后还带有积分偏置。
- 建议先做单方向满杆持续3--5 s复测，确认积分是否有时间建立；若仍稳定低速，先把Kp小步
  提到180再观察，避免直接回到240。若无振荡但长期有静差，再小步增加Ki或加入按目标RPM的
  前馈；前馈/积分需同步观察零速回中时的残余电流，不能只看速度峰值。当前不建议先改位置环
  或盲目提高输出限幅。
- 用户随后完成了长时间采样；最新`logger_20260925_183826_150370.txt`中+100°/s连续约
  15.6 s，实际中位102°/s（54..168），-100°/s连续约23.1 s，实际中位-90°/s
  （-162..-12）。两段回调均4 ms、反馈年龄1--2 ms，命令约+1232/-1332，未接近
  PID/协议限幅；说明积分建立后正向平均已跟上，反向仍约10%静差，且速度按6°/s整数
  RPM量化并随raw角度变化。-100段末Iout约-1901，+100段末约+1216；切回0后仍可见
  约-1439的积分/命令残留，方向切换存在积分偏置。
- 改进建议：先不要提高输出限幅；首选Kp从150小步到180验证反向静差，若出现原先角度区
  间颤动立即退回。Ki暂保持135，避免继续增大造成零速残流；应在目标归零或换向时清理/渐
  退积分。若希望减少积分依赖，可根据长时间稳态命令估算yaw速度前馈，先试约60--80
  原始刻度/RPM并设独立小限幅，再用P修正；这是建议值，需以双向长段日志校准，不能直接
  复用参考仓库的0.85（单位不同）。
- 最新`monitor/logger_20260925_191906_832001.txt`确认运行P200/I135/D0。该次连续目标段
  较短：+100最长约2.3 s，实际中位72°/s（24..102）；-100最长约4.6 s，实际中位
  -78°/s（-114..-36）。相比Kp150的长段+102/-90，不能据此证明速度跟踪改善，短段内
  积分尚未完全建立；但本次未出现±6000/±12000饱和，输出约+1343/-1315中位，颤动没有
  明显复发。Kp200提高了P项范围（约±3333），却未解决负载补偿不足。
- 目标0长段仍出现约-1271的Iout/命令残留而速度接近0，说明静摩擦可能暂时托住电机，
  但换向时仍会带入偏置。评估Kp200前应做单方向至少10--20 s的同条件长段，并记录起始
  积分状态；若长段仍低于目标，再考虑小步调Ki或速度前馈，暂不继续上调Kp。
- 最新`monitor/logger_20260925_193756_569297.txt`确认Kp300/I135。+100连续18.1 s时实际
  中位96°/s（54..162，标准差13.9），-100连续27.6 s时中位-96°/s（-138..-30，
  标准差16.4）；相较Kp200的+72/-78，跟速明显改善且正反向更对称，也没有出现命令或
  协议限幅、先前Kp240那种高频饱和换向。Pout峰值约+2300/-3477，最终命令中位约+1282/-1321。
  但速度仍有角度相关波动（约±14--16°/s标准差），不能称为完全恒速。
- 目标0段仍可见约-1039的Iout/命令残留，虽速度中位0，说明积分残留尚未根治；后续应先
  保持Kp300做归零、换向和不同角度复测，再处理积分清理/渐退或前馈，不建议继续上调Kp。

## 积分清理/渐退实施建议（仅方案，未改代码）

- 逻辑位置：在`GimbalController_YawControlWithCompensation()`计算并限幅`speed_target`之后、
  调用`PID_Calculate()`之前处理；当前普通回中仍是MANUAL且只把目标RPM置0，所以现有
  `mode_changed`重置不会覆盖“手动正转→回中→反转”。
- 增加上一次有效yaw目标RPM和状态标志。用约0.5 RPM迟滞判断事件：进入零目标时清理一次；
  目标从大于+0.5 RPM变为小于-0.5 RPM或反向时清理/衰减一次，避免摇杆噪声反复触发。
- 回中策略优先硬清理：重置内环后把`last_measure`设为当前速度反馈，避免D项从0测量产生
  尖峰；零目标期间保持积分为0，速度目标仍为0，不承担位置保持力矩。若实测需要平滑，可
  在约150--300 ms时间常数内将`iout`按`iout *= exp(-dt/tau)`衰减，同时清零`iterm`和误差历史。
- 换向策略先采用软清理：将旧`iout`保留20%或按约100--200 ms时间常数衰减，清零`iterm`和
  误差历史并以当前反馈初始化`last_measure`；保留P项负责立即制动，再让新方向Ki重新建立。
  若仍有明显反向冲击，再改为换向直接清零，不要用提高Kp解决。
- 验证顺序：正向满杆10 s→回中5 s→反向满杆10 s，分别在此前303--343°易扰动区和普通
  角度重复。记录`yaw_pid_iout`、`yaw_pid_output`、`yaw_command_raw`和实际RPM；目标是
  回中约0.2--0.3 s内Iout/命令接近0，换向无旧方向残流和饱和，之后速度仍能回到约±100°/s。

- 最新`monitor/logger_20260925_202601_201462.txt`确认积分清理后的运行参数为Kp300/Ki135/Kd0，
  PID回调主要4--5 ms、反馈年龄1--2 ms。+100°/s连续约10.8 s时实际中位96°/s、标准差
  约14°/s（54..156）；-100°/s连续约9.2 s时实际中位-96°/s、标准差约14°/s（-132..-48）。
  目标0段Iout中位约0、命令接近0，说明归零清理已生效；仍显示约-12°/s时，主要是原始RPM
  整数反馈/滑行或停止阈值造成的量化残留。运动段命令约±1200--1300，远未触及PID或协议限幅。
  日志中的部分“响应慢”发生在目标本身由摇杆逐步变化的区间，不能归因于Kp不足；剩余不匀速
  更符合速度量化和角度/方向负载变化。建议保持Kp300，先试约60--80原始刻度/RPM的速度前馈，
  再视结果增加仅2--3采样（约8--12 ms）的短滤波；不要先继续增大Kp或恢复残留积分。

- 前馈具体试验方案（仅说明，未改文件）：修改`config/robots/infantry_standard.c`中yaw电机
  `.feedforward`初始化（当前约第169行），保持`bias=0`，先设`velocity_gain=60.0f`、
  `output_max=1500.0f`；重新采样后再只改为70和80做两组同样的正/反向长段。该字段单位为
  原始命令刻度/RPM，当前满杆目标约16.67 RPM，因此三组前馈分别约1000/1167/1333刻度，
  不会把零速目标直接变成偏置。前馈已在`GimbalController_YawControlWithCompensation()`
  的内环PID之后叠加，暂不改Kp、Ki、PID限幅或`bias`。每组记录目标/实际RPM、`iout`、PID输出
  和最终命令；优先选择能使长段`iout`下降且实际速度标准差下降、换向不过冲的最小增益。

- 最新`monitor/logger_20260925_203508_876252.txt`对应`velocity_gain=60`、`bias=0`、
  `output_max=1500`。正向目标约+100°/s时实际中位约+102°/s、标准差约13.7°/s，
  负向约-100°/s时实际中位约-102°/s、标准差约14.7°/s；相比前一份无前馈日志的约±96°/s，
  稳态偏差明显减小。`iout`中位约+254/-268，较此前约+1167/-1136大幅下降，说明前馈确实承担
  了主要负载；最终命令仍约+1255/-1238，未接近限幅。速度标准差基本没有下降，故60已改善
  跟速和积分负担，但尚未解决量化/负载造成的不匀速；目标0时iout仍接近0。下一步应先比较
  `velocity_gain=70`，若实际速度均值无明显改善或换向过冲增加，就保留60，不要仅凭命令大小继续加大。

- 最新`monitor/logger_20260925_203931_734782.txt`确认已运行`velocity_gain=70`、`bias=0`、
  `output_max=1500`。+100°/s段实际中位约+102°/s、标准差13.6°/s；-100°/s段中位约-102°/s、
  标准差13.9°/s。相对gain=60，速度均值基本不变，标准差仅小幅改善；但`iout`中位由约
  +254/-268降至约+118/-104，说明前馈70进一步减轻了积分负担。最终命令中位约+1231/-1283，
  仍远未限幅；回调约4 ms、反馈年龄约2 ms，实时链路正常。结论是70优于60作为当前基线，
  但不匀速主因仍是RPM量化和负载变化，继续增加前馈预计收益有限，应先观察换向和角度相关性。

- 对同一份gain=70日志按编码角度每30°分箱后，波动明显具有角度相关性而非全程相同：正转在
  60--120°区间实际中位约84--87°/s、命令约+1.9k--2.1k，明显慢于0--30°的约102°/s和
  +1.1k；150--180°又升至约114°/s且命令降至约+0.7k。反转在120--180°实际约-78到-90°/s、
  命令约-1.8k到-2.5k，而210--270°约-99°/s、命令约-1.3k。该模式与角度/方向负载变化一致，
  不能只归因于反馈量化。各区间回调仍约4 ms、反馈年龄1--2 ms；因此优先做分方向、分角度的
  低幅前馈校准，短滤波只能处理剩余量化噪声，不能替代负载补偿。

- 控制链路复核：`command_router.c::route_gimbal()`将RC yaw映射为`GimbalCmd.yaw_rate`；
  `gimbal_controller.c`在`speed_loop_only=false`时通过`YawReference_Advance()`把该输入积分成
  连续角度目标，再由位置外环生成速度目标。当前infantry配置`speed_loop_only=true`会在控制器
  开头把VISION/SPIN降级为MANUAL，并在后面直接旁路位置环，因此自瞄角度误差当前不能真正闭合
  yaw位置环。现有`VisionTargetMessage`/legacy bridge字段是`yaw_error_rad`相对误差，不是绝对角度。
  若要支持精细角度控制，最小改法是先恢复yaw位置环并保留RC目标渐进；若要让消息显式承载角度，
  应在`core/contracts/control_messages.h`新增带来源/有效标志的角度目标字段，由router维护
  手动目标、vision bridge明确转换坐标，controller统一映射到`YawReference.target_ticks`，不能
  直接把RC归一化值当绝对编码角度。

- 自瞄命令实际路径：`legacy_vision_bridge.c`把`Vision_Recv_s.yaw`原样放入
  `VisionTargetMessage.yaw_error_rad`；`command_router.c`复制到`GimbalCmd.vision_yaw_err_rad`，
  20 ms无更新后清除`vision_valid`。gimbal回调据此选择VISION；但当前`speed_loop_only=true`
  会先把VISION改成MANUAL并将输入速率置零，随后输出0 RPM，因此自瞄角度误差目前不会驱动
  yaw。只有恢复位置环后，控制器才把误差弧度换成编码器刻度并由外环生成速度目标。

- 当前配置文件已将`config/robots/infantry_standard.c`的`speed_loop_only`改为`false`，因此按
  代码路径下一次重新构建并烧录后会进入位置环。现有最新logger仍是改动前的
  `logger_20260925_203931_734782.txt`：`yaw_flags=15`（含控制有效位8，但不含位置环位16），
  `yaw_mode=0`，不能证明已烧录位置环；需要新固件日志中稳定出现`yaw_flags`至少为31，或监控
  位置目标/速度目标随角度误差变化，才能确认运行时已接入。

- 最新`monitor/logger_20260925_210054_876185.txt`已确认位置环运行：`yaw_flags=31`，其中包含
  `POSITION_ACTIVE=16`。但位置外环明显过激：目标速度大量打在±180°/s（manual 30 RPM）限幅，
  实际速度出现约-342..+396°/s，最终命令多次到±12000，角度误差最大约46.5°。当前yaw外环
  `Kp=28`作用于编码器刻度误差；1°约22.76 ticks，单独1°误差就会产生约637 RPM的未限速输出，
  远超30 RPM上限，约0.05°误差即可触发限速。外环`Kd=1.3`还会放大量化/反馈变化。下一步应
  先将外环D设0，并把外环Kp降到约0.3--0.7范围做低速阶梯测试，同时把`target_lead_deg`先降到
  3--5°、manual速度限速先降至10--15 RPM；确认不再触及速度/协议限幅后再逐步提高。内环
  `Kp=300/Ki=135`不应在外环饱和期间继续调整。

- 最新`monitor/logger_20260925_210909_714462.txt`对应外环`Kp=0.3/Ki=0/Kd=0`、
  `target_lead_deg=5`、手动限速15 RPM，位置环确实运行（`yaw_flags=31`），但仍有明显速度
  目标饱和：目标速度多次达到±90°/s（±15 RPM），实际速度约-162..+174°/s，命令峰值约±12000。
  运动段角度误差约+4.8/-5.0°，而Kp0.3乘以5°约34 RPM，仍超过15 RPM限速；因此小幅晃动仍是
  外环饱和和内环反复制动，不是Ki残留。日志中目标速度接近0只有1个样本，尚不足以评估真正的
  静态锁定。下一步先将外环Kp试0.05--0.1、Kd保持0，并单独做摇杆回中后至少5 s静止锁定；
  若要兼顾100°/s手动跟随，应另加目标速度前馈或提高限速，不能靠增大Kp解决。

- 最新`monitor/logger_20260925_211421_412484.txt`对应外环Kp0.1/Kd0，位置环运行（`yaw_flags=31`）。
  静态片段显示锁定已稳定：目标约141.46°时实际保持141.46°，误差0..0.20°且命令为0；
  另一片段误差约-0.15..0.68°，无持续反向大命令。全日志角度误差范围约-7.38..6.15°，
  `yaw_command_raw`全局虽有±8.9k峰值，但出现在目标运动/换向段，不是静态锁定周期振荡。
  目标速度在锁定片段接近0（另有约0.99°/s的量化残留），说明锁定问题已明显改善；跟手慢的
  主因是Kp0.1下位置误差产生的校正速度有限，且目标角速度没有直接前馈到位置环。提升响应应优先
  给手动目标速度加前馈，再小步试Kp0.12--0.15；不要直接回到0.3。

- 遥控器同样输入下希望增加目标角度变化量，应调整`YawControlConfig.manual_rate_deg_s`（当前配置
  约第28行为100°/s），因为`YawReference_Advance()`每回调按`stick * manual_rate_deg_s * dt`
  增加目标ticks。`target_lead_deg`只是目标相对当前位置的最大超前量，不是输入放大倍数；当前5°
  会限制放大后的目标误差。建议先把manual_rate_deg_s从100试到150，观察目标角速度和跟随误差；
  若目标很快顶到5°而实际仍跟不上，再把target_lead_deg小步到8--10°，同时防止外环重新饱和。
  manual_speed_rpm应至少不低于manual_rate_deg_s/6；当前为30 RPM，适合至多约180°/s的目标上限。

- 底盘链路复核：DBUS适配器将左摇杆横轴映射为`rc.ch[2]`、纵轴为`rc.ch[3]`，拨轮为`rc.ch[4]`；
  `route_chassis()`当前使用`vx=-ch3/max_input`、`vy=+ch2/max_input`、`wz=ch4/max_input`，
  普通底盘模式未再交换轴。若需求是前后、左右分别反向，等价于将两个平移符号同时取反，必须
  先用固定单轴输入确认物理方向。拨轮当前只有全局3计数死区，没有滞回或斜率限制，小指令跨过零点
  会直接令所有轮目标反号；建议在router对拨轮单独加入较大进入/退出死区、零穿越滞回和有限斜率。
  当前配置记录的yaw正前方为`yaw_forward_ticks=4555`；最新yaw日志结束时编码器为7652，但未证明
  当时机械上正对前方，不能直接覆盖4555，需在实物正前方姿态下重新读一次编码器再记录。

- 用户确认最新logger采集结束时yaw机械上正对底盘前方；`monitor/logger_20260925_214755_968832.txt`
  末尾连续样本的`gimbal_yaw_encoder_raw`均为4968，故本次正前方记录值为4968 ticks。当前配置仍为
  4555，待用户明确允许写入配置后应替换为4968；本轮只记录观测值和修改方案，未改文件。
- 拨轮修复的具体位置是`application/cmd/command_router.h`的`CommandRouter`状态、
  `application/cmd/command_router.c`的`route_chassis()`及其调用点。拨轮不能复用全局3计数死区；
  建议增加进入死区30、退出死区15（原始RC计数）和归一化wz斜率2--4/s，维护`dial_active`与
  `dial_wz`，目标值先过滞回再按斜率逼近。链路掉线时清零这两个状态，避免恢复时继承旧旋转命令。

- 对当前未提交拨轮改动做静态复核：infantry固件构建通过。发现spin模式仍更新`dial_wz`但固定发布
  `SPIN_WZ_NORM`，退出spin时若拨轮此前有输入，可能带出未清零的旧旋转量；应在进入/处于spin时
  明确清零`dial_wz`和`dial_active`，或为spin单独定义拨轮语义。另有排版空格问题但不影响编译。
  host测试脚本因环境缺少`clang`未能运行，不能把构建通过当作输入路由行为已验证。

- 完整修改清单补充：spin切入/持续/退出时应清零`dial_wz`和`dial_active`，避免固定spin输出后
  带出旧拨轮命令；`tests/host/test_remote_chain.c`必须同步正前方期望4890、更新位置样本和
  前后左右符号断言，并新增拨轮进入/退出滞回、斜率换向、spin退出和掉线清零测试。当前infantry
  构建通过但host测试因缺少clang未验证。

- 最新`monitor/logger_20260925_224020_152653.txt`显示底盘体旋转命令本身没有高频颤动：
  `chassis_cmd_wz`在正/负旋转段分别长时间保持约+1/-1，换向经过0，只有2次相邻样本符号翻转；
  拨轮为0时`wz`还会按斜率逐步衰减。因该logger没有四个轮子的目标RPM、实际RPM和电流，不能据此
  证明某个电机的电流环在颤动。更可能是小拨轮指令产生的轮目标RPM过低，M3508静摩擦导致粘滑，
  或单轮速度环误差来回跨过零。下一步应记录四轮target_speeds、feedback speed和output_current，
  再决定提高轮速Kp、加速度前馈或设置小旋转指令的有效阈值；不要继续只改`wz`斜率。
  用户确认`test_remote_chain`系列测试文件可在下次测试时随当前遥控器映射和拨轮逻辑一起更新；本轮不提前修改测试。

- 拨轮具体改动方案：在`CommandRouter`增加`float dial_wz`和`bool dial_active`；将
  `route_chassis()`签名增加`CommandRouter *router`、`float dt_s`，调用点传入`router,dt_s`。
  删除当前`wz_raw=apply_deadband(ch[4])`和直接归一化，使用进入阈值30、退出阈值15的滞回，
  再以`max_step=3.0f*dt_s`限制`router->dial_wz`变化；普通/跟随分支的`command->wz`都使用
  平滑值，spin分支仍保留固定`SPIN_WZ_NORM`。`command->enabled`需根据平移输入或
  `fabsf(router->dial_wz)>0`判断，否则回中衰减阶段会提前禁用底盘。掉线分支清零两个状态。

- 最新`monitor/logger_20260925_220404_755881.txt`末尾100个有效样本中，yaw编码器仅在4889和
  4890 ticks之间变化（最近20个样本主要为4890），对应约214.85--214.89°，位置已稳定；但它
  与上一轮正前方候选4968相差78 ticks（约3.43°），所以不能确认“仍为同一正前方位置”。若本轮
  采集时机械确实正对前方，则新的候选标定值应为4890，仍需用户确认后再写入`yaw_forward_ticks`。

- 2026-09-25底盘诊断接口已扩展为Dashboard v10：在保持yaw v9前缀和偏移不变的前提下，
  每帧追加四轮速度PID最终输出和反馈实际电流；monitor logger同时保存四轮目标RPM、实际RPM、
  PID输出和电流，缺失/过期数据为NaN。该接口只读控制器快照，不改变控制；C/Python测试、
  解码器检查和infantry构建通过，尚未烧录或采集新v10实车数据。后续分析应按电机ID和同一
  时间窗口统计速度误差、输出占限幅比例、I项/输出与反馈电流，避免把旧v9日志误当成轮级诊断。

- 已修复小陀螺切入初始化并加入host断言：首次进入时目标航向必须等于当帧IMU航向，连续帧
  不重置目标。构建和差异检查通过；需要烧录后观察进入瞬间`yaw_target_memo`与实际角度是否连续。

- 云台跟随运动学修复：编码器跟随路径将`yaw_relative_deg`改为逆角传入
  `gimbal_to_chassis_frame()`。原因是该输入表示底盘相对云台的逆时针角，直接旋转会使云台
  指向±90度时前后方向反号。同步调整`tests/host/test_command_router.c`的连续角度符号断言；
  已完成构建和`git diff --check`，未烧录，需实车在+90/-90度分别推动前进确认。

- 小陀螺头部剧烈旋转原因：路由器保存`was_spin`后立即覆盖`router->spin_mode`，切入判断
  `spin_now && !router->spin_mode`永远不成立，`spin_hold_yaw_deg`不会初始化为切入瞬间的
  `sensor.yaw_total_angle`。随后云台SPIN模式将旧`yaw_target_memo`与当前航向比较，位置外环会
  输出大速度目标。当前未修改；应先修正切入沿用条件，再检查spin目标、陀螺反馈和命令输出。

- 最新yaw日志`logger_20260925_231411_798683.txt`运行参数为Kp300/Ki135（不是用户期望的
  300/60），manual目标速率约+294/-360deg/s，实际约+426/-420deg/s；活动段速度误差标准差
  约105--142deg/s，命令触±12000共66帧，活动命令符号翻转72次。目标角误差-63..22deg，
  lead=15的静态锁定样本仅2帧，无法证明锁定稳定。优先核对Flash/配置版本并先将Ki实际降为0
  或小值对照，保持Kp300和lead15；未修改或烧录。

- 最新v10底盘日志`logger_20260925_225310_924801.txt`分析：四轮ID顺序[2,1,4,3]。约±1200RPM
  稳态平台中，ID2、ID1、ID3均出现实际RPM跨越目标的大幅周期性摆动，速度PID输出多次达到
  ±12000；ID4实际RPM波动约±200RPM且未输出饱和。当前Kp15、Ki0、Kd0，说明主要问题是
  前三轮速度环比例增益/负载差异引起的饱和换向，不能通过增加Ki解决。建议先只降ID2/1/3
  的Kp做10、8两档对照，ID4保持15；每档用相同正反1200RPM平台比较误差均值、标准差、
  饱和比例和电流。该轮尚未修改参数或烧录。

- 新日志`logger_20260925_225906_463870.txt`显示Kp10轮（ID2、ID1、ID3）已从周期性饱和换向
  变为稳定跟随：活动段误差标准差约117、108、127RPM，ID4（Kp15）约96RPM；四轮输出饱和
  次数均为0。剩余问题是恒定欠速，正向误差约63--92RPM、反向约-72--85RPM。结论是降Kp
  有效，当前不应增加输出限或Ki；后续只做Kp10→11→12的小步单变量测试，确认误差下降且
  标准差不反弹后，再评估小Ki。

- 最新`logger_20260925_234459_400162.txt`中SPIN模式83帧：进入目标角初始化正常，但目标速度约
  ±60deg/s时实际速度冲到+1340deg/s，命令30/83帧饱和；位置从-545°降至-1072°而速度字段为正，
  暴露`GimbalController_YawControlWithCompensation()`中`speed_feedback = -g_gz*30/pi`与位置/命令
  坐标的符号不一致候选。建议先只做陀螺反馈正负号A/B测试，再调SPIN专用Kp/Ki；当前运行Ki=105，
  logger未显示Ki=60。未修改或烧录。

- `logger_20260926_000348_257553.txt`验证陀螺符号修正后SPIN基本恢复：1290帧目标±60deg/s、实际
  ±69.4deg/s，无±12000命令饱和；但命令21次换向，Ki105的Iout范围约-1428..309，仍可能造成小幅
  颤动。手动模式仅39帧，不能据此判断手动跟手。建议先Kp300不变，Ki105→60→30做单变量对照；
  速度不跟手应提高`spin_speed_rpm`（10RPM=60deg/s，先试12/15）或小幅提高spin目标调整速率，
  不要先提高Kp。

- 小陀螺行进坐标建议：当前`route_chassis()`的SPIN分支使用`c_yaw-yaw_total_angle`并额外交换
  vx/vy，参考的是云台/车辆混合坐标，不能形成稳定的世界坐标行进。应把头部保持控制与底盘
  平移解耦：底盘平移只使用底盘IMU世界航向`c_yaw`（减安装/场地偏置），用一致的逆旋转得到
  chassis vx/vy后直接输出；不要用yaw电机零点或spin_hold_yaw作为平移参考。先以固定前进输入
  在底盘航向0、±90°和原地旋转时验证方向，再决定符号。未修改或烧录。

- 2026-09-26修改位置建议：`command_router.c`约119--145行将SPIN变换改为仅使用底盘IMU `c_yaw`的逆旋转并直接输出vx/vy；不再引用云台连续角或额外交换轴。`infantry_standard.c`当前manual_rate=300、manual_speed=60、lead=12、外Kp=.065、内Kp=300/Ki=105；建议先内Ki 105→60、积分限4000→2000抑制小颤动，再分别试spin_speed10→12RPM和`SPIN_GIMBAL_YAW_ADJ_DEG_PER_S`120→180改善跟手。普通手动若仍慢且稳定，再外Kp.065→.08。本轮仅给出代码位置和建议，未修改或烧录。

- 当前工作区已包含SPIN使用底盘`c_yaw`逆旋转的世界平移分支，但小陀螺仍固定发布`SPIN_WZ_NORM=0.33`，所以车体必然旋转；若用户要求车体不转，应单独将该值置零，这将不再是小陀螺。另发现`gyro_data.c`先记录IMU日志再读取WT61C，日志中的`c_yaw`可能滞后一帧，不等于路由使用的传感器快照错误。

- 用户进一步明确：小陀螺左摇杆前进要沿当前yaw头部的世界指向，而非固定世界前方。SPIN平移
  应采用`theta=normalize_angle_180(yaw_total_angle-c_yaw)`把yaw坐标向量变到底盘坐标，保留
  `command->wz=SPIN_WZ_NORM`；不能只用`-c_yaw`。若云台/底盘IMU零点不同，需增加并校准安装偏置。

- 若左摇杆前进仍走圆，当前代码虽已使用`yaw_total_angle-c_yaw`，但需先判别数据：车体旋转时`c_yaw`应连续变化，`yaw_to_chassis`应约反向变化，`chassis_cmd_vx/vy`应同步旋转；若角度不变，是WT61C/消息更新或时间戳问题；若角度变化方向反了，才将差值改为`c_yaw-yaw_total_angle`。CMD logger已记录这些字段，下一轮应按路由时间逐帧检查，不能先盲改旋转符号。
